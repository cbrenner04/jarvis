- Dismissed pipelines must continue suppressing attention rows for their attributed workflow invocations when stage entry-run IDs differ from invocation IDs. Cover this with production-shaped fixtures. The spec excludes attention-row behavior changes and requires accurate entry-run associations.
- Pipeline detail must retain its project row when resolved stage runs identify one project, while preserving omission for genuinely ambiguous multi-project attribution. Removing the expected project output is a regression and conflicts with the required resolution-aware project derivation.
