- Patch-mode acceptance is not met until every draft→ready path in patch mode honors the base-current rule. `maybeMarkReady` is guarded, but review-final still has direct `gh pr ready` paths, so a behind/diverged branch can still be flipped ready in patch mode. The required outcome is: when a patch-mode branch is behind or diverged from its PR base at ready time, no patch-mode path calls `gh pr ready`, and the PR stays draft.

- Durable docs must match the actual operator-facing scope of the change. The new guard is documented as a patch/plan ready-flip behavior, but committed `intent` runs now inherit the same plan ready path and therefore the same ready-flip semantics. The required outcome is: either committed intent ready transitions are explicitly documented as using the same base-current guard, or that path is excluded so operator-facing behavior remains unchanged. Leaving the behavior changed but undocumented is not acceptable.

- The durable behavior docs are internally inconsistent and must be reconciled. `v2/docs/v1-behaviors.md` now records the base-current guard in one section but still describes unguarded ready flips in later summary bullets, and the run-loop docs still describe review-final as a direct `gh pr ready` path. The required outcome is: all touched durable docs describe one coherent truth about which ready transitions are guarded, when a behind/diverged branch stays draft, and which paths still call `gh pr ready`.
