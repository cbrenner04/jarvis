- Make the acceptance criteria and decisions pin the live `invocation_completed` row contract at the level this first consumer needs: the spec must state the required row identity relationship across quota fallback, including shared logical-attempt identity and distinct per-subprocess invocation identity, rather than leaving that only implied by external docs. This is load-bearing because later exports/parity work depend on stable keys at the shared seam, and the intent explicitly rejects re-keying later.

- Define the shared-seam gating rule for callers outside the write workflow: the spec must say emission happens only when the write-step telemetry context/sink is provided and is otherwise a no-op. This is necessary to keep the slice scoped to write-step invocations only, per the intent, and to avoid accidental behavior changes for other current or future `shared/invocation/execute.ts` callers.

- Decide and document sink-write failure behavior at this seam: the spec must state whether telemetry append failures block the invocation result, are surfaced separately while the invocation still completes, or follow some other explicit operator-visible rule. This is observable runtime behavior introduced by the slice; leaving it undefined would make the first consumer underspecified and costly to reverse.

- Require coverage for rows emitted even when downstream runner classification is non-successful after the subprocess settles, such as contract-miss or invalid-token outcomes. The intent says emission occurs after each write-step subprocess settles at the shared invocation seam, so the spec needs to prove that later runner interpretation does not suppress the append-only fact.
