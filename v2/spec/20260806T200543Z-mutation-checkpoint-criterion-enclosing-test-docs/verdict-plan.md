Verifying key claims in the codebase and docs so the verdict is grounded in the actual artifacts.
## Verdict: required refinements

1. **Operator runbook must triage using the real blocker text.** The hollow checkpoint surfaces `no @mutate directive linked to this criterion; add // @mutate … on the named pin` even when a directive exists but `linkDirectivesToCriterion` found no `includes(pinTitle)` match. The Gate trust bullet and its acceptance criterion must map that symptom to pin-title omission (criterion names the pinning file but not the enclosing `test()` title) and state that the fix is editing the criterion—not adding or repairing the directive, and not a proof-form or directive-syntax problem.

2. **Operator runbook must disambiguate two hollow causes.** § Gate trust already documents premise-smell hollow (second hollow on a different guard → do **not** amend the criterion; rescope the spec). This spec’s failure mode is first hollow from linker miss (criterion omits pin title → **do** amend the criterion). The new bullet and AC must distinguish these opposite fixes on the same symptom word, placed before the premise-smell bullet or with an explicit cross-reference—so operators who hit linker-miss hollow first do not follow the wrong guidance.

3. **Test-scope acceptance criteria must match intent and `ci-test-scope.ts`.** Touched paths are `v1/docs/**`, `v2/docs/**`, and `test/**`; docs are `NO_TEST_IMPACT`, `test/**` maps to `test:shared` + `test:integration:shared`. The subspec AC requiring full `bun run test` conflicts with the intent’s “touched test scope” and strands implement agents between spec and `AGENTS.md`. Align to `bun run typecheck` plus the classifier-resolved shared test scripts.

4. **Doc assertion acceptance criteria must encode the rule, not token presence.** The new `test/spec-guidance-doc-assertions.test.ts` AC should require assertions that cover: every mutation-checkpoint criterion must include the enclosing `test()` title (pin title); matching is case-sensitive `includes(pinTitle)` with a substring of the full title sufficient; loose references go hollow under the no-fallback linker. A bare `toContain("pin title")` guard is insufficient.

5. **Spec-guidance bullet must define linker matching and give examples.** “Linker-matching substring” is shorthand for case-sensitive `criterionText.includes(directive.pinTitle)` with no fallback; a partial title suffices, different casing does not. The authoring guidance and its AC should state this explicitly and include at least one good/bad criterion example (e.g. loose paraphrase like “on the pinned-argv test” vs embedding the exact `test()` title)—matching existing § Mutation-checkpoint criteria style and the recurring failure mode described in the intent.

6. **Close the matching implement-queue row on landing.** `v2/spec/implement-queue.md` row #2 (`seeds/mutation-checkpoint-criterion-must-name-enclosing-test`, recurred 3×) is what this intent consumes. Add a task and documentation-update outcome to remove or close that row when the guidance ships, following precedent from specs that closed queue rows on landing (e.g. `target-absent-mutation-directive-reprompt`).

7. **Use one lead term with explicit equivalence.** Tasks and acceptance criteria should standardize on “enclosing `test()` title” with a parenthetical equating it to pin title / `directive.pinTitle`, avoiding drift between “enclosing test name,” “pin title,” and “directive’s pin title” across bullets.

**Not required (scope held by intent):** CI guard on the operator runbook (intent scopes the assertion to spec-guidance only); `write-behavior.md` cross-reference (authoring rule, not runtime contract change—optional hygiene only); reintroducing all-directives-in-file fallback or plan-review hollow-pin flagging.