Validating staged intents against repo conventions and referenced behaviors.
## Verdict

1. **`existing split-prompt budget test`** — `intent-split-prompt-by-surface` treats a length budget test as already in the tree (decision + AC). Repo has no such test today (`shared/prompts/intent-split.test.ts` and `intent-command.test.ts` only assert wiring/content). Either name creating that guard as part of the prompt work or drop “existing.”

2. **`v2/docs/v1-behaviors.md`** — Surface-based fan-out changes documented intent-split behavior (`v1-behaviors.md` still describes “behavior-level” / slice splitting). Spec guidance requires that catalog on existing-behavior changes; only `spec-guidance.md` and `workflow-runner.md` are listed.

3. **Prompt contradictions** — `prompts/intent/split.md` still says behavior/vertical slices and “do not try to enforce execution order” while the prompt intent adds surface fan-out and cross-surface prerequisite ordering. Decisions should explicitly retire/replace those lines (not only add one sentence).

4. **Surface set vs regression AC** — Prompt intent lists four boundaries (persistence, daemon, CLI, execution loop); regression AC pins three names (persistence, daemon, CLI). Clarify whether the committed fixture is three-surface only or whether execution loop must appear in a fourth intent.

5. **`plan-blocks-unmet-split-dependencies`** — AC says `jarvis1 plan`; v2 plan is `jarvis run workflow plan` with the same draft gate. Name which entrypoint the regression drives (or both) so implementers do not pick the wrong harness.

Otherwise: prerequisite chain (prompt → regression → plan gate), invert/RED ACs, stubbed-agent regression scope, and “no docs” on the pure regression intent are coherent.