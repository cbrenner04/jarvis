# Cleanup prunes the consumed ready-intent it actually archived

`provenIntentPrune` and `archiveCompletedSpec` join the archived spec to its consumed ready-intent as `ready-intents/${spec.name}.md`, where `spec.name` is the spec directory basename. Every registered project stamps spec directories (`20260908T050011Z-persist-operator-failure-records`) while ready-intents are slug-named (`persist-operator-failure-records.md`), so the join never resolves and the prune the runbook promises never fires. Observed 2026-09-08: five consumed ready-intents, each byte-identical to its archived `intent.md`, survived two `jarvis cleanup` passes and were pruned by hand in #3641. Carved from the `detached-pipeline-plan-stage-consumes-ready-intents` seed, whose plan-stage consumption half stays open.
