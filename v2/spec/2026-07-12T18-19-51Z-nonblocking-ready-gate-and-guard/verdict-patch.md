- Guard every supported child-process reach form, including namespace ESM imports and module-object bindings from `require` or dynamic `import`, when they access `execSync`, `execFileSync`, or `spawnSync`. Add fixtures for these bypasses. Otherwise daemon-hosted code can reintroduce blocking subprocesses despite the guard, violating subspec 03’s invariant.

- Preserve `gh pr ready` diagnostics on terminal failure. After retries are exhausted, the error surfaced as `readyFinalizeError` must include captured stdout/stderr as well as failure status/context, while retaining the existing `already ready` / `not a draft` success guard. The async conversion currently discards actionable `gh` output from the persisted error message, regressing finalization failure reporting required by subspec 00.
