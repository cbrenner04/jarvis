Verdict: refine.

**1. AC2's test doesn't actually verify what it claims.** `tui-field-collector.test.tsx` races `collectLaunchFieldsViaInk`'s real promise against a 100ms timeout and asserts only `toBeDefined()` on the result. Because `loadInkUi`'s injected-seam branch (`tui-ink-runtime.ts`) returns no `useInput`, `tui-field-collector.tsx` falls back to a no-op — and since `onDone` is only reachable from inside a real `useInput` callback, the promise never resolves on the injected path. The timeout branch always wins, so the test passes regardless of whether field collection actually completes. This directly fails AC2's own requirement ("drive `collectLaunchFieldsViaInk` through an injected `inkRender` seam, and assert field collection completes") and defeats its stated purpose (catching an accidental fallthrough to the real `import("ink")` path) — a hang on the *intended* path is indistinguishable from a hang on the *wrong* path under this test.

**Required outcome:** the injected-seam test in `tui-field-collector.test.tsx` must deterministically drive the mock through to completion and assert on the real result (e.g. `result.ok` / `result.fields`), not a timeout fallback object. This likely requires either extending the injected-seam contract so a synthetic input/`useInput`-equivalent can trigger `advance`/`onDone` deterministically, or restructuring the mock so completion doesn't depend on a `useInput` no-op. Drop the `Promise.race`-against-timeout pattern once the test can assert on a real, deterministic outcome.

Everything else in the diff (AC1 routing through `loadInkUi`, AC3 single dynamic-import-site, AC4 smoke test, `viewHost` not-applicable reasoning, doc updates) is sound and satisfies the spec as written — no further action needed there.