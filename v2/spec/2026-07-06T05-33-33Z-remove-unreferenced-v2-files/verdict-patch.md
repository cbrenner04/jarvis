No required outcomes. The branch satisfies the completed spec: target files and `v2/test/fixtures/` are gone; scoped `rg` checks are clean; durable docs match the acceptance criteria (revised TUI ink section with surviving `loadInkUi` boundary, fixture purge, inline Biome verification, intentional Yoga-TDZ smoke-guard drop); and the deletion premise is confirmed by zero external consumers. The stale `write-loop-input.ts` comment and vestigial `biome.json` exclude are out of scope for this slice.
