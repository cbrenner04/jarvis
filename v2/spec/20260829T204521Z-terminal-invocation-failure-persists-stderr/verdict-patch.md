- Correct `v2/docs/write-behavior.md` so `message` is documented only as an optional durable `InvocationFailureDetail` field, not as stdout JSON. It must retain the specified final-attempt, verbatim, 2048 UTF-16-code-unit bound and empty-stderr omission semantics. This aligns documentation with the implemented persistence boundary and the completed spec; no code change is required.
