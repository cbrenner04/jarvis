- Clarify the contract boundary for this subspec: whether it changes only the built-in Bun `fix`/`ready` scripts, or also the harness-level `ready` outcome when the tree is dirty after verification. The current invariant is broader than the drafted tasks, so the spec must either narrow the invariant to script semantics or explicitly cover post-ready dirtiness handling. This is necessary because acceptance criteria must describe observable behavior at the intended surface, not imply unowned semantics.

- Resolve the verification-order ambiguity. The spec currently defers exact full-tier order, but other text effectively pins a specific sequence. The subspec must choose whether the requirement is exact CI-order parity or only step-set parity with unchanged install/retry behavior. This is necessary because deferred decisions cannot coexist with acceptance criteria that already make the decision observable.

- State the scope of the “only autofix entrypoint” rule for overrides. The spec must say whether separating autofix from verification applies only to the built-in Bun scripts or also constrains custom `readyCommand`/operator overrides. This is necessary to avoid over-broad wording that changes operator-facing semantics beyond the intent.

- Tighten the documentation outcome around one durable semantic home. The spec must identify `v2/docs/v1-behaviors.md` as the authoritative behavior record for the ready/fix split, and require any other doc updates to align or cross-link without reintroducing conflicting step-by-step semantics. This is necessary because the repo’s documentation standard requires one durable home for behavior changes.

- Include every operator-facing doc whose current text would become false under the new split, including the worktrees/commits guidance if it still describes mutating `ready` behavior. This is necessary because the subspec already commits to doc alignment for changed workflow semantics, and leaving stale operator docs would make the behavior change non-reviewable and reversible only by cleanup follow-ups.
