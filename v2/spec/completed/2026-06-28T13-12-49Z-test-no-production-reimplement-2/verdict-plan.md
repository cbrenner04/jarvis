- Clarify the rule boundary in [00-document-production-logic-double-convention.md](/Users/christopherbrenner/Work/jarvis/.worktree/plan-test-no-production-reimplement-2/2026-06-28T13-12-49Z-test-no-production-reimplement-2/00-document-production-logic-double-convention.md): this slice must ban logic-bearing local replicas when an exported production seam already exists, while still allowing ordinary fakes/spies for external dependencies. This is necessary because the intent is narrowly about “do not reimplement production logic as a local double,” not a broader anti-mocking rule, and the current wording leaves room for overreach.

- Tighten the daemon example so it reads as a drift/anti-pattern example, not as a required live migration or reconstructed before/after diff. The spec must make clear that `daemon-start-list.test.ts` is the expected pattern and that the worked example documents the class of mistake it avoids. This is necessary because the prerequisite already states the test exercises the real handler factory, so the subspec should not imply behavior the repo no longer has.

- Define the seam boundary the doc update must teach: the unit under test is the exported production unit that owns the behavior, and injected fakes are for dependencies outside that logic boundary. This is necessary because “production entry point/factory” and “external seams” are load-bearing terms here; without that clarification, different authors could apply the convention inconsistently.
