Verdict:

1. **Serial ordering must be explicit.** The finish-time seam spans three intents (`store-timestamps-terminal-reconciliation` → `list-row-step-honesty` → `terminal-window-renders-finishless-rows`) via declared prerequisites. The prerequisite gate checks committed code, so planning the two dependents before the store intent lands will append a `## Blocker` and exit non-zero. State the store → list-row → terminal-window planning order so the operator plans serially rather than discovering the block at draft time. This is a required addition, not a decomposition defect.

2. **Row-cap flag deletion needs a proving test.** `terminal-window-renders-finishless-rows` deletes `setInvertTerminalRowCapFilterForTest`, but its Decisions only describe proving the window guard. Either name the cap guard-inversion that proves the deletion or justify removing the flag without one. As written it reads as an orphan deletion.

3. **`blocked`-aging doc target is unpinned.** "Document where to find aged-out blocked rows" names no destination. Pin the doc home (operator-runbook vs. tui docs) so an implementer doesn't guess.

The five-intent decomposition is sound; do not collapse it. Prerequisites are honest and correctly encode the dependency. The `v1-behaviors.md` call is low-stakes (v2-internal TUI/store seams with no v1 parity analog) and left to plan's discretion.