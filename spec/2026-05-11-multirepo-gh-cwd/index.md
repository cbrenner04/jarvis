# Multirepo: `gh` uses registered project root

- [x] [00 - Base branch detection uses project root, not shell cwd](./00-base-branch-uses-project-root.md)

When this index is complete, base-branch detection for new worktrees works even if you start `jarvis run` from a directory that is not a git repository (for example, a parent folder that contains multiple checkouts).
