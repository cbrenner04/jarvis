Checking documentation standards and how ready-gate repair is documented to ground the review.
## Intent review

**Strengths:** Clean fan-out from the seed; problems cite PR #2243; decisions use explicit “rules out …” framing; path-fence intent is the right foundation with inversion + `write-loop.test.ts` preservation; load-sensitive intent has inversion; prerequisites correctly chain markdown-only / sidecars / load-sensitive to staged-path validation.

**Issues**

1. **`ready-gate-red-in-untouched-files-is-out-of-scope`** — Load-bearing gap: when gate output cannot be mapped to failing paths, decide whether to fall back to today’s `ready_gate_failed` + repair or treat as indeterminate. Same intent: decide terminal contract (new `loop_finished` kind vs variant of `ready_gate_failed`, resumable vs not, draft PR unchanged) — operator runbook text assumes a named reason but runtime semantics are unspecified.

2. **`ready-gate-red-in-untouched-files-is-out-of-scope`** — Second AC is preservation (“still follows today’s repair path”); per spec guidance it should anchor a concrete test (e.g. existing `write-loop.test.ts` ready-gate repair cases), not paraphrase.

3. **`markdown-only-workflow-ready-repair-rejects-code-edits`** — “Workflow’s Markdown output roots” needs a durable definition per workflow kind (intent vs plan): which directories count (e.g. `ready-intents/`, timestamped `v2/spec/…`, both). Without that, the fence is not implementable.

4. **`ready-gate-repair-omits-jarvis-sidecars-from-commits`** — “Stripped or rejected” is two behaviors; pick one (silent strip vs failed repair iteration) so ACs and docs match.

5. **Planning seam** — All five intents touch ready-gate repair completion; draft one ordered spec (fence first, then dependents) or enforce serial `plan` per spec guidance — do not parallel-plan off a shared base.

6. **Minor (plan stage)** — Several ACs say “a dedicated test” without a file name; acceptable in intents but subspecs must name failing tests. Consider `v2/docs/v1-behaviors.md` only if you treat ready-gate repair as parity-catalog behavior (likely v2-only; skip if purely harness).

## Verdict

**Not ready** until (1) out-of-scope attribution fallback and terminal/resume semantics are decided, (3) Markdown output roots are defined per workflow, and (4) sidecar handling is strip vs reject. Fix (2) at plan draft; enforce (5) when promoting to spec.