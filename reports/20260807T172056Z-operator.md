# Operator session — 2026-08-07 (TUI slice 6 complete; seed cleanup)

Cursor-first, claude fallback. Single operator (opus-4-8).

## Outcome

**TUI overhaul is DONE (slices 1–6).** Meta index checked off; `implement-queue.md` and `tui-overhaul-brief.md` retired.

| Thread | PRs | State |
| --- | --- | --- |
| **run-steering** (TUI slice 6, 4/5) — split oversized `00-tui` into parser/dispatch/docs | spec-split #2701 → implement #2704 | merged |
| **log-follow** (TUI slice 6, 5/5 — slice complete) | implement #2705 | merged |
| **spec-only-diff ready-gate scope** — test-scope half (no-test-impact diffs skip full suite on unresolvable base) | intent #2708 → plan #2711 → implement #2712 | merged (reduced) |
| **mutation-criterion plan-draft gate** | intent #2702 → plan #2703 → implement #2706 | **shipped then REVERTED (#2709)** |
| **review-actuator staged-md lint** | intent #2707 → plan #2710 | **DEFERRED** (implement incomplete; seeded) |

Every implementation PR: #2701, #2702, #2703, #2704, #2705, #2706, #2707, #2708, #2709 (revert), #2710, #2711, #2712, plus this close PR.

Zero open PRs at close. Main green.

## What landed and why it was non-trivial

- **run-steering split.** The prior session's single `00-tui` subspec timed out one 45-min iteration and shipped tests-failing code. Split into `00-command-parser` / `01-entry-dispatch` / `02-docs` (mirroring the pipeline-steering split); the whole spec then implemented in one chained run. Fully verified (typecheck + `check` + full `test:v2` + `lint:md` green; mutation pin hand-verified to redden; subagent review confirmed real RPC dispatch — the prior owner/runId gap was fixed). Its publication successor dispatched **late** (~5 min after the write runs settled) and auto-published the draft PR — no hand-finish needed.
- **log-follow** — same clean chained pattern; publication successor flipped it ready.
- **spec-only-diff (#2712) reduced during hand-finish.** The implement wrote complete code but settled `iteration_commit_failed` (a biome `noExcessiveCognitiveComplexity` error the completion-commit formatter rejects) and the harness resume then failed `unsupported_resume_context`. On hand-finish, a subagent review — and the harness's own artifact-contract `## Blocker` — showed the drafted **out-of-diff settlement guard was hollow and redundant** (disabling it left both settlement tests green; the pre-existing attributable-untouched-path settlement already produced `ready_gate_out_of_scope`), and a keystone directive false-reddened via a compile error (the directive string matched both the function definition and its call). Dropped that half, kept the verified **ci-test-scope** change (the fix for the recurring flaky full-suite-on-spec-only strand), re-verified (pin reddens behaviorally), re-scoped the settlement to a seed.

## Reversal: #2706 (mutation-criterion plan-draft gate)

Shipped a plan-draft hard gate that rejects a mutation-checkpoint criterion whose enclosing test title is absent from the resolved pinning file — then **reverted it the same session (#2709)**. It false-positives the *standard* pattern: naming a **new** test (authored at implement time) in an **existing** test file, where the title legitimately isn't in the file yet at plan-draft. It immediately blocked both follow-up plans and would have rejected this session's own run-steering plan. A plan-time check can't distinguish greenfield from hollow by on-disk resolution. Advisory hollow-pin (#2660) + authoring guidance (#2655) remain. The reverted spec dir (`20260807T145131Z-…`) was removed at close as a dead-end (implementation reverted; approach unworkable) so it can't be misarchived as `completed`.

## Deferred: review-actuator staged-md lint

Implement run `3d5f8270` ran the full 45 min → `iteration_commit_failed` → resume broke. The subspec is too big (the plan-review verdict expanded it to all promotion admission paths + reprompt + exhaustion + 5 docs) **and** its new gate broke 13 existing `executeWorkflow review dispatch` tests whose staged-Markdown fixtures aren't lint-clean (they now settle `landing_failed`). That's unfinished implementation, not a hand-finish, so it was abandoned and seeded with split guidance. The adversarial review earned its keep: it caught that the plan's mutation checkpoint was **hollow** (the feature has defense-in-depth — two gates both call `lintReviewedStagedMarkdownOrFail` — so any single-guard `@mutate` in `workflow-runner.ts` false-GREENs, masked by the sibling gate). The seed records the one load-bearing pin (`reviewed-staged-markdown-lint.ts` clean-result guard, hand-verified to redden behaviorally).

## Seeds added (3)

- `mutation-checkpoint-verifier-ambiguous-basename-and-descriptive-overmatch-block-meta-specs` — the implement-time verifier blocks specs *about* mutation-checkpoint tooling: ambiguous basename (`write.test.ts` ×2 files → `unresolved_pinning_test`) and descriptive criteria mentioning `@mutate`/`Keystone checkpoint:` as content mis-parsed as real checkpoints. Blocked the #2706 implement run.
- `plan-intent-completion-out-of-diff-repair-settlement-is-redundant-or-needs-isolation` — the settlement half dropped from #2712: verify whether the guard is needed at all before re-adding; if added, isolate it in tests.
- `review-actuator-staged-markdown-lint-subspec-too-big-and-breaks-review-dispatch-fixtures` — split guidance + correct mutation checkpoint + fixture-reconcile note.

## Process lessons (also recorded to memory)

- **#2706 revert** — a plan-time on-disk enclosing-test check false-positives the normal greenfield-test-in-existing-file case; unworkable as a hard gate.
- **Review only terminal worktrees.** I ran a mutation-applying diff review against log-follow's worktree *while its publication run was live* — the two raced on the source file (no harm this time; publication passed its own gate on the committed code).
- **Check for `## Blocker`, not just AC ticks, when hand-finishing.** spec-only-diff had all AC ticked but carried a harness-written `## Blocker` flagging two hollow checkpoints — I missed it initially and only the subagent review re-surfaced it.
- **My own keystone verification was fooled by a compile-error false-redden** (a `@mutate` directive whose string matched two sites → syntax error → "1 error" read as RED). The adversarial subagent review (mutate only the semantically meaningful site) is the real gate. Twice this session a review caught a hollow/redundant guard my own check passed.

## Cost

See `session-costs.csv` / `session-outcomes.csv` / `operator-costs.csv` / `operator-outcomes.csv` / `efficiency.csv` rows for `20260807T172056Z-operator`.
