# Incomplete re-dispatch continues a lane with committed subspec work

Issue #3974 asks 2–3. Surface: stale-workspace preflight (`maybeResetStaleWorkspace` → `resetStaleWorkspace`), shared by CLI and daemon pipeline dispatch.

- [ ] [00 — Continue a clean, base-descended lane with committed work](00-continue-committed-lane.md)
