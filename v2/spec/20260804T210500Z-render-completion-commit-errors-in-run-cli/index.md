# Render completion-commit errors in run CLI output

repo: cbrenner04/jarvis

Daemon `list` / `wait` already project `error.completionCommitError`; the CLI still
omits it from `jarvis run list` rows and has no regression on `jarvis run wait`
stdout.

- [x] [00 - Render completion-commit errors in CLI](./00-render-completion-commit-errors-in-cli.md)
