Verifying implementation details against the spec and advocate analysis before issuing the verdict.
## Verdict: required refinements

1. **Align the mixed-diff pinning with the AC’s pre-/post-fix contract.** The subspec requires that a mixed diff still derives production mutation candidates and reports `surviving-mutation` when scoped tests stay green, and that this subcase **passes on both pre-fix and post-fix baselines**. Today the mixed-diff test also asserts `sourceSite.file === "src/safe.ts"`, but with the test hunk before the production hunk, pre-fix code still treats `src/helper.test.tsx` as production and `verifyCandidates` stops at the first surviving mutation on that path. Pre-fix therefore yields `surviving-mutation` on the test file, not `src/safe.ts` — so the `sourceSite.file` assertion is post-fix-only and the test does not truthfully satisfy the AC wording it is meant to pin. **Required outcome:** the mixed-diff subcase must pin production-line surviving-mutation in a way that holds on pre-fix and post-fix baselines (e.g. production-first diff ordering so production candidates are exercised first, and/or assertions scoped to what is actually stable across baselines). It must still prove that production changes remain mutation targets when test paths are present in the same diff.

**Rationale:** The spec’s mixed-diff AC was deliberately written (per verdict-plan) to separate all-test pass-through (pre-fix RED) from production survival in mixed diffs (pre- and post-fix green). A post-fix-only `sourceSite` pin weakens that contract and misstates what the test proves. The production-only exclusion fix, docs, all-test pass-through, inversion checkpoint, and `*.test.ts` regression coverage are otherwise consistent with the completed spec; no other actuator changes are required.