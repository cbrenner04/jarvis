- Enforce oversized-split integrity after the actuator: a split verdict may succeed only when every original task and acceptance outcome is preserved exactly once, no work is orphaned, and the routable index links every replacement. Invalid split trees must fail review rather than commit.

- Cover that guard with malformed split outcomes as well as the existing valid-tree path.

- Add revision-keyed rendered fixtures and snapshot coverage for the advocate and adjudicator role prompts. Plan-mode documentation states all review variants are snapshot-tested; current coverage omits these revised roles.
