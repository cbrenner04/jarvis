# Print PR link on spec completion

When `jarvis run` finishes a spec successfully, the run terminal should print
the draft PR's URL alongside the existing `spec complete` line so the operator
does not have to look it up via `gh` or scroll through the session log.

- [ ] [00 - Print PR URL on completion](./00-print-pr-url-on-completion.md)
