The advocate's technical justification holds up (verified: the test file does import both the in-process `startDaemon` and the detached-spawn `startDaemon`/`stopDaemon`, and only the in-process one allows state-store injection). One documentation gap remains and requires action.

**Required outcome:** Add a short comment in `v2/src/daemon/daemon.sandbox-unrunnable.test.ts` above the new list-runs test explaining why it uses the in-process `startDaemon` (aliased `startInProcessDaemon`) rather than the detached-spawn `startDaemon`/`stopDaemon` used by the file's other test — specifically, that seeding a run row into the daemon's state store requires holding a direct reference to that store, which the detached-process launcher has no seam to support. This prevents a future reader from assuming the test spawns a real OS process (as the file's existing header comment implies) or "fixing" it to match the other test in a way that would break it.