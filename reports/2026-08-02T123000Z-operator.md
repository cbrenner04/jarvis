# Operator session — 2026-08-02

Surface: v2 (`jarvis`). Agent order: `codex → cursor → claude` (codex never hit quota; every
implement invocation this session was `codex gpt-5.6-sol`).

## Outcome

**14 PRs merged, one left open by design, `main` green.** TUI slice 4 shipped end to end; slice 5 is
three-fifths shipped and blocked on a decision, not on work.

## Merged

| PR | What |
| --- | --- |
| [#2507](https://github.com/cbrenner04/jarvis/pull/2507) | seed: TUI slice 4 detail pane |
| [#2508](https://github.com/cbrenner04/jarvis/pull/2508) | intent split → wire fields + detail pane |
| [#2509](https://github.com/cbrenner04/jarvis/pull/2509) | plan: pipeline-list detail fields |
| [#2510](https://github.com/cbrenner04/jarvis/pull/2510) | plan: execution-loop human-only contracts |
| [#2511](https://github.com/cbrenner04/jarvis/pull/2511) | `pipeline_list` carries pipeline `terminalAction`/`seedPath`/publication outcome and stage `id`/`position`/`artifact`/`failureDetail` |
| [#2512](https://github.com/cbrenner04/jarvis/pull/2512) | seed: mutation verifier skips directive-only criteria |
| [#2513](https://github.com/cbrenner04/jarvis/pull/2513) | plan: write-step rules human-only markers |
| [#2517](https://github.com/cbrenner04/jarvis/pull/2517) | human-only marker contract in `DEFAULT_WRITE_STEP_RULES` + `spec-guidance.md`, isolated per-injection regressions |
| [#2518](https://github.com/cbrenner04/jarvis/pull/2518) | `spec.criteria-ticked` selects checkpoints by phrase **or** quoted `@mutate` directive |
| [#2519](https://github.com/cbrenner04/jarvis/pull/2519) | detail pane: pipeline identity block, stage roll-up, stage diagnostics |
| [#2520](https://github.com/cbrenner04/jarvis/pull/2520) | seed: mutation selector fires on prose mentions |
| [#2521](https://github.com/cbrenner04/jarvis/pull/2521) | detail pane: selected-run detail by id, lossless display-width wrapping |
| [#2522](https://github.com/cbrenner04/jarvis/pull/2522) | seed: gate autofix can turn a green tree red |
| [#2523](https://github.com/cbrenner04/jarvis/pull/2523) | seed: TUI polls `wait` for state nothing renders |
| [#2524](https://github.com/cbrenner04/jarvis/pull/2524) | seed: TUI slice 5 command dock |
| [#2525](https://github.com/cbrenner04/jarvis/pull/2525) | intent split → five slice-5 leaves |
| [#2526](https://github.com/cbrenner04/jarvis/pull/2526) | plan: TUI command language |
| [#2527](https://github.com/cbrenner04/jarvis/pull/2527) | plan: pipeline start admission API |
| [#2528](https://github.com/cbrenner04/jarvis/pull/2528) | plan: TUI dock projection |
| [#2529](https://github.com/cbrenner04/jarvis/pull/2529) | typed TUI command parser |
| [#2530](https://github.com/cbrenner04/jarvis/pull/2530) | reusable `pipeline_start` admission API, CLI rewired as adapter |
| [#2531](https://github.com/cbrenner04/jarvis/pull/2531) | dock rows as a pure function over monitor state |
| [#2532](https://github.com/cbrenner04/jarvis/pull/2532) | seed: mutation verification outlives its run |

## Open at close, deliberately

[#2533](https://github.com/cbrenner04/jarvis/pull/2533) — dock projection subspecs 01-03. All
criteria ticked, `typecheck` / `check` / `test:v2` green, held draft because the harness reports a
surviving `operator-flip` in `v2/src/tui/tui-entry.tsx`. Both flagged sites are unobservable
defensive code (proved by hand, see below), so this needs a decision — delete the dead
`clearSelection` block or exempt the sites — then
`jarvis run resume e734022c-d610-459c-ab21-7f7268f8f083`. Details in the PR comment and
`v2/spec/implement-queue.md`.

## What the reviews caught that the gate did not

Seven implement PRs went through subagent review with instructions to *apply* every mutation
directive rather than read it. Five carried a real finding:

- **#2511** — both checkpoints were never verified at all. Their criteria quoted `// @mutate …`
  without the literal phrase `Mutation checkpoint:`, and the verifier selected on the phrase only.
  The mutations did kill, but only because a reviewer applied them by hand. Fixed in #2518.
- **#2518** — its own directive quoted an expression the formatter had wrapped across two lines, so
  it resolved `target_absent` and never ran; and the mutation it described was a no-op, because
  block-based disjuncts subsumed the text-based ones it targeted. Hand-fixed before merge.
- **#2530** — a directive pointed at a guard my own refactor had deleted, and a duplicated
  `unregistered-project` guard made the surviving one unobservable, so that refusal had *no*
  effective mutation coverage. Both hand-fixed and re-verified.
- **#2519 / #2531** — clean on this axis; #2531's audit (36 directives, all applied) is what proved
  the salvage below was safe.

The pattern is stable across three sessions: a ticked mutation-checkpoint criterion is the least
trustworthy line in a spec, and CI cannot see it.

## Two hand salvages

**Slice 4, subspec 00.** The run finished 00 and then blocked on 01 — removing the `waitState`
panel invalidated two `tui-ink-monitor` assertions that 01's scope forbade touching. Re-running
resets the worktree, so 00 would have been discarded. Cherry-picked it out (#2519) and widened 01's
scope to name exactly those two assertions.

**Dock projection, subspec 00.** The run finished the work and settled `iteration_timeout` before
committing — and the timeout left **three** `@mutate` directives applied to production source. They
did not surface together: two were restored, the file was copied out, and the copy still carried a
third, because the scoped verification was still mutating that worktree minutes after the run row
was terminal. Salvage required reversing every directive mechanically. Landed as #2531 after an
audit confirmed all 36 directives resolve and kill, with no fourth stranded mutation.

## Friction not worth a seed

- The `--detach` entry run ID reports `completed` while the write row is still live; waiting on the
  printed ID is misleading. Added to the runbook.
- Left-pane and right-pane now derive a pipeline's project differently (first joined run vs
  unanimous), so they can disagree. Recorded in the #2521 review, not seeded.
- A spec subspec whose own acceptance criterion quotes the human-only markers gets classified
  human-only and is never gated (observed in #2517's subspec 00). Cute, and it did not cost
  anything here.
- `bun run fix` in a worktree without `node_modules` fails confusingly; `lint:md` needs the primary
  checkout's install.

## Cost

Operator `/cost` figures pending — ask the operator.

**Agent-side cost is unmeasurable this session.** 99 of 100 invocations were codex, and every codex
telemetry row carries `cost_source: "unavailable"` / `usage_source: "unavailable"` / `cost_usd:
null`. The single cursor row ($0.18, `computed`) is the entire measurable agent spend. This is the
same hole cursor had before #2431/#2433/#2446 — and worse, because codex is metered rather than
subscription-billed, so the missing number is real unbilled spend. Seeded as
`seeds/codex-usage-is-never-recorded`.
