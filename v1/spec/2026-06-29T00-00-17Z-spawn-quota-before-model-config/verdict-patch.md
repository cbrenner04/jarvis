No required outcomes.

The branch satisfies every acceptance criterion in the binding subspec: spawn classification is **transient → auth → quota → model_config**; the codex dual strict-quota + model-config fixture classifies as `quota`; genuine model-config-only and preserved precedence cases remain covered; durable docs are updated in the canonical homes without duplicating the full precedence list.

Remaining gaps (unpinned auth+quota+model_config triple-match, weak-quota+model_config boundary, `intent.md` drift, completed-spec supersession notes, adjacent `aider-model-warnings.md` typo) are documentation hygiene or optional hardening outside the subspec’s acceptance criteria and documentation-update scope. They do not block completion of this behavioral change.
