- Align the durable docs with the implemented split. `v1/docs/run-loop.md` still describes completion/review gates as committing `check:fix` or reusing `check:fix:unsafe` output. That must no longer be true anywhere for the built-in path: built-in `bun run ready` is verification-only, built-in autofix lives at `bun run fix`, and any dirty-tree commit after a green gate applies only to custom `readyCommand` behavior. This is required by the subspec’s acceptance criteria that built-in `ready` never invokes any `:fix` script and that non-authoritative docs align without reintroducing conflicting step semantics.

- Make the `readyCommand` boundary consistent across docs, including plan mode. The durable behavior record and aligned v1 docs must describe exactly where `readyCommand` overrides are honored and must not imply override behavior at unwired call sites. If plan-mode readiness does not thread `readyCommand`, docs must say that plainly instead of suggesting custom overrides still run there. This is required because the subspec explicitly left override semantics unchanged and out of scope except for the built-in script split.

- Remove stale `check:fix` attribution from harness-facing wording around post-ready dirty output. Commit subjects, error text, and related comments in the ready gate must describe generic dirty output after `ready`, not “pre-ready check:fix”, because after this change that label is false for the built-in path and misleading for custom overrides. This is necessary to keep operator-visible semantics accurate and consistent with the split documented in `v2/docs/v1-behaviors.md`.
