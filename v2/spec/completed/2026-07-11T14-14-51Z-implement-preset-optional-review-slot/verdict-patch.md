Both findings hold up.

**Required outcomes:**

1. **Fix the double-shrink regression.** The shrink pass that runs after a completed `implement` step currently triggers off `role === "implement"` on any completed step. Pinning both positions of the new two-step `implement` shape to `role: "implement"` means this pass now fires twice for a single workflow instead of once — an emergent execution-behavior change introduced by this spec's pinning decision, even though no new shrink logic was written. This directly violates the spec's decision to pin both positions without assigning new behavior, and the intent's explicit "do not add review behavior here" boundary. The shrink trigger must be adjusted so it fires exactly once per workflow regardless of whether `implement` resolves to one or two steps (e.g., trigger only on the last completed step with that role, not on every completed step carrying it).

2. **Add coverage for the two-step execution path.** No test currently exercises the resolved two-step `implement` preset through `executeWorkflow` itself — existing tests only check `resolveWorkflowPreset`'s output shape in isolation, or the shrink pass with a single hand-authored implement step. Add a test that runs the two-step preset through `executeWorkflow` and asserts the shrink pass executes exactly once. This is required to satisfy the acceptance criterion that preset tests cover the two-step shape "without review execution behavior" — the double-shrink interaction is precisely the kind of unintended execution-behavior change that criterion is meant to catch, and it currently has zero coverage.