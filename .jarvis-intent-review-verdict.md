Cross-checking staged intents against workflow docs and pipeline validation code.
## Verdict

**Intent 1 (`pipeline-intent-debate-posture-realizes`)** — Solid problem, decisions, and prerequisites. Gaps:

1. **Failing-test ACs** (spec guidance): AC1 says “a test that expects…” instead of anchoring the existing pin `pipeline-definition-validation.test.ts` (“intent under debate is unrealizable…”). AC2 should name `pipeline-stage-resolve.test.ts` (“no preset mapping” / `intent`+`debate`) as the baseline-failing resolution test, not only behavioral prose.

2. **Resolver vs doc**: Decision routes `intent`+`debate` through bare `intent` + `reviewBehavior: "debate"`, but today `light` still goes through preset key `intent-reviewed` in `WORKFLOW_POSTURE_PRESETS`. Doc AC restates the whole table on bare presets; intent does not say whether `light` stays on the `intent-reviewed` preset path in code (doc-only reframe) or moves to bare `intent` like debate. Worth one explicit decision so implementers do not refactor `light` out of scope.

**Intent 2 (`pipeline-posture-table-pins-cli-review-acceptance`)** — Right follow-on and serial prereq on intent 1. Gaps:

1. **Failing-test AC**: “A test fails on baseline…” should name the new test file or `describe` block, not anonymous “a test.”

2. **Load-bearing encoding**: Cross-check needs a fixed mapping from pipeline `review` (`none` / `light` / `debate`) to the CLI/workflow-args surface. **`implement`+`none`** is pipeline-unrealizable while bare `implement` may allow `--review-passes 0`; intent does not say whether that counts as the same posture for parity or is intentionally out of band. Without that, the nine-cell enumerator is ambiguous.

3. **CLI surface source**: Decision says compare to “CLI/workflow-args acceptance surface” but not which module is canonical (shared table/helper vs usage strings vs ad hoc per-workflow checks). Pin that before plan drafts duplicate, brittle sources.

**Packaging**: Two same-seam intents are fine if plan runs them **serially** (intent 2 prereq); call that out in the fan-out/plan queue so they are not parallel-planned off the same base.

**Docs**: `workflow-runner.md` on intent 1 is appropriate; no `v1-behaviors.md` churn required unless that catalog gains pipeline posture rows later.

**Overall**: Proceed after tightening failing-test anchors on intent 1, clarifying intent 1 `light` code vs doc scope, and adding intent 2 decisions for posture→CLI encoding and the canonical acceptance surface.