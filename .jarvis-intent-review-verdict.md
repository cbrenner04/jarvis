## Verdict

The cluster is **not ready** as staged. Required intent revisions before plan:

1. **`workflow-print-run-id-at-admission`** — Reconcile the failing-test AC with hardening reality (ID already pre-wait): either preservation-style anchor to an existing test plus a guard that fails pre-fix (flush, preset omission, reorder), or drop “fails against pre-fix” and state the observable regression. Spell out **first stdout line** (stderr such as `intent paths:` excluded). Tie log/TUI AC to a named harness test or narrow to daemon-admitted ID equality in `workflow.test.ts`.

2. **`workflow-attached-waits-for-terminal`** — Rewrite the problem for **what still fails on main** (early CLI exit vs test/doc gap). Remove stale “ID printed last” overlap with print-run-id. AC must name a **multi-row, real attach** test (not mock `start` + single `wait`) that proves the CLI stays up until workflow-entry terminal and final JSON/exit match entry rollup.

3. **`workflow-attached-run-boundary-progress`** — **Pin the client mechanism** for step boundaries during attach wait (e.g. `list` poll, log tail, split wait)—“existing IPC” alone is not implementable. State explicitly: no `workflow-step:` for the admitted entry run; lines are **attach-only** (detach emits none).

4. **`workflow-detach-after-admission`** — **Pin invocable CLI surface** (flag name or env); deferred default/opt-in blocks agent-verifiable detach ACs. State detach runs the **same pre-`start` gates** as attach.

5. **Cross-cutting** — Failed-admission ACs should anchor to **named existing tests** where possible. Plan may add `index.md` order and consolidate doc edits; intents need not duplicate a doc strategy, but boundary/detach doc bullets should not contradict attach-only boundary lines.

**Rationale:** Spec guidance requires agent-verifiable failing tests for runtime behavior, behavioral ACs that are pinable without plan invention, and prerequisites that reflect real gaps—not a correct DAG with stale problems or unspecified IPC/CLI surface. Advocate findings on IPC, detach surface, terminal-wait test grade, and print-run-id baseline are load-bearing; plan cannot safely draft implementable ACs without the above.