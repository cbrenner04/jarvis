# Per-step ready-gate budgets with a total ceiling

repo: cbrenner04/jarvis

**Prerequisite:** Implement against merged `gate-timeout-is-not-a-red-gate` — ready gate exit `124` is retryable infrastructure failure, not a repairable red gate (`scripts/ready.ts` timeout handling / `ready-finalize`).

- [ ] [00 - Per-step budgets, run ceiling, and flake-retry reset](./00-per-step-budgets-and-ceiling.md)
