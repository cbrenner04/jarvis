## Verdict

**Upheld issue:** `daemon-queue-promotion.test.ts`'s `flushBackgroundRuns()` helper was reduced from two `setImmediate` flushes to one during a later "shrink" pass unrelated to the direct-invocation conversion itself. The conversion commit faithfully preserved both flushes; the reduction was an unreviewed drive-by change introduced under a commit whose stated purpose was diff-size reduction, not behavior change. Nothing in the spec, commits, or code comments establishes that a single microtask tick is sufficient post-conversion — this is an undocumented, unjustified timing behavior change riding along with an in-scope test-harness conversion, which both subspecs explicitly scope to *only* the direct-invocation conversion ("Out of scope: ... Deleting or thinning behavior coverage").

**Required outcome:** Restore `flushBackgroundRuns()` in `daemon-queue-promotion.test.ts` to its pre-shrink behavior (two `setImmediate` flushes), matching what the actual conversion commit (`9d090a51`) produced. If the actuator has a concrete, verifiable reason the direct-invocation path only needs one tick (e.g., removal of an IPC round-trip that previously required a second microtask hop), that reasoning must be stated explicitly in the `00-queue-promotion-direct-invocation.md` Decisions section and the single-flush behavior kept — but absent that justification, the safe and spec-compliant default is to restore both flushes, since passing tests in one sandbox run do not prove robustness against scheduling variance and the subspec explicitly disclaims any coverage-thinning.

No other findings require action.