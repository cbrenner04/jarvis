# Operator session — 2026-06-27 (Tier 1+2 drain, guardrail spiral)

## What shipped

Triaged the ready-intent backlog (20 → pruned to keepers), then drained the
Tier 1+2 set. **6 of 7 intents merged**; the 7th is spec'd and deferred.

| Intent | PR | Agent | Notes |
| --- | --- | --- | --- |
| worktree-branches-off-fresh-base | #685 | codex | new branches cut from `origin/<base>` |
| ready-flip-confirms-base-current | #686 | codex | draft→ready guarded on base-current (both modes) |
| cleanup-retires-abandoned-runs | #687 | codex | `cleanup --abandon` (review-incomplete: operator-reviewed) |
| merge-on-green-gate | #688 | claude+cursor | `triage --merge` gates ready→CI-green→admin-merge |
| finalize-dirty-complete-run | #691 | cursor | `triage --mark-ready` auto-finalizes complete-but-dirty |
| merge-target-by-worktree-or-spec | #695 | cursor | `triage --merge` resolves worktree/spec/PR-ref |
| finalize-refuses-when-behind-base | #693 | — | **spec only (draft); impl deferred** |

Also: intent-autofix (#677, earlier spec) finalized + merged; backlog prune #674;
v2 docs dropped aider→opencode/ollama + queued NL-router as Phase 9 (#675);
scratchpad standard `.scratch/` (#673).

## The spiral (and the real lesson)

Mid-session **main went red on CI undetected** and I burned heavy manual effort
recovering. Root causes, all structural:

1. **`bun run ready` ≠ CI.** `ready` auto-fixes lint via `check:fix:unsafe` and
   passes; CI runs `bun install --frozen-lockfile` + strict `bun run check` (no
   autofix) on the committed tree and fails. A pre-existing unused binding +
   non-null assertion sat red on CI while every local gate was green.
2. **Hand integration-merges dropped the autofix.** I committed the merge, ran
   `ready` (which autofixed), then pushed *without committing the autofix* → red.
3. **`triage --merge` (the green gate we shipped this session) was unreachable**
   on the worktrees that needed it (`.active-spec-path marker not found`), so I
   fell back to unguarded `gh pr merge --admin` — which let the red through.
4. **My own errors:** admin-merging before CI concluded (racey `--watch`), and a
   `node_modules` symlink hack that clobbered the real dir and got committed,
   breaking `frozen-lockfile` install in CI.

**Owner principle: guardrails should be built, not verbalized.** So the lessons
became seeds, not runbook caveats:

- `seeds/ready-mirrors-ci.md` — `ready` = pure verification matching CI; move
  `check:fix:unsafe` to a committed pre-`ready` step; Jarvis bakes fix→commit→gate.
- `seeds/triage-merge-handles-marker-less-worktrees.md` — gated merge must work on
  marker-less worktrees so the green gate is always reachable.
- Built already: `.gitignore` `node_modules/`→`node_modules` (#696) so a symlink
  can't be tracked again.

## Other seeds / backlog

- `seeds/register-codex-gpt-5-4-mini.md` (owner added pricing) — register the
  cheaper codex tier.
- Ready-intents remaining (not driven): deflake-watchdog, drop-aider,
  remove-actuation-capability-floor, plan-generated-spec-markdown-passes-lint-md,
  opencode-ollama-local-model-run, finalize-refuses-when-behind-base.

## Config change

`~/.jarvis/config.json` agentOrder reordered **codex → cursor → claude → opencode**
(all 6 lists) to spare the weekly claude quota. Held: codex quota exhausted for
hours mid-session; **cursor subscription was out** entirely (not just throttled),
so the ladder repeatedly fell to claude until cursor recovered. Net: claude usage
stayed modest (codex/cursor did most impl work).

## Cost

Jarvis spend (plan + run per intent, from run summaries):

| Intent | plan | run | total |
| --- | --- | --- | --- |
| worktree-fresh-base | $2.18 | $2.52 | $4.70 |
| ready-flip | $2.43 | $4.78 | $7.21 |
| cleanup-retires | $2.58 | $3.00 | $5.58 |
| merge-on-green | $2.69 | $1.40 | $4.09 |
| finalize-dirty | $3.54 | $0.03 | $3.57 |
| merge-target | $0.03 | $0.01 | $0.04 |
| finalize-refuses | $0.03 | — | $0.03 |
| intent-autofix (#677) | — | $1.07 | $1.07 |

**Jarvis total ≈ $26.3** (codex did the costly runs; cursor runs ~$0.03 est;
claude largely spared). Operator session `/cost`: *pending — fill from `/cost`.*

## Deferred

- `finalize-refuses-when-behind-base` (#693): spec drafted, draft PR open off the
  old base. Rebase + run fresh next session (lowest-value edge guard).
