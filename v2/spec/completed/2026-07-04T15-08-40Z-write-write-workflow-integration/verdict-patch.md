1. Strengthen or narrow the fallback contract so the proof and durable docs match exactly. The current end-to-end test proves per-step resolution and quota fall-through within `claude`’s rung list, but it does not prove fall-through across the loaded project agent order to a later agent binding. Because the subspec and `v2/docs/workflow-runner.md` describe fallback from the loaded project agent/model config during workflow execution, the branch must either prove that full cross-agent fall-through in the workflow path or reduce the stated contract to the narrower fallback actually exercised. The supported behavior, the acceptance target, and the test evidence need to say the same thing.
