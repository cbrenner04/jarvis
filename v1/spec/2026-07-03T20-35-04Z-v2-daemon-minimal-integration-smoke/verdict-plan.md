Adversary finding 1 (status RPC coverage gap) is upheld. Findings 2, 4, and 5 are not valid concerns requiring refinement. Finding 3 is a minor tightening, upheld at low severity.

## Required refinements

1. **Status RPC coverage gap must be closed.** Removing `status RPC on live daemon reports running state` from the smoke file drops the only test that exercises the daemon's `status` wire-method dispatch — the Prerequisites' claim that `daemon-start-list.test.ts` "exercises real run-control handlers over real sockets" does not extend to the `status` method. The spec must state that status-method coverage is preserved, either by extending the kept smoke test's single real-process/real-socket session to also assert a `status` response before stopping the daemon, or by adding a DI-based dispatch test for the `status` handler. Extending the kept smoke test is more consistent with the spec's "keep exactly one test" minimalism decision and should be the stated default. This is required because the spec's own Behavior section promises "removed daemon integration assertions remain covered by agent-runnable or existing socket-handler tests" — as drafted, that promise is false for `status`.

2. **Socket-unbound poll bound should be concrete.** The conditional polling decision ("poll with a short bounded interval... if it races") should specify a concrete bound (e.g., a max attempt count and per-attempt interval) rather than leaving "short bounded interval" for the implementer to invent. This removes ambiguity and keeps the test's timing behavior deterministic and reviewable.

No other refinements are required. The DI "second startDaemon fails" replacement does not regress coverage (the original real-process test never asserted the surviving daemon's post-failure health either, so parity holds). The intent's file path (`v2/src/daemon.sandbox-unrunnable.test.ts` vs. the subspec's correct `v2/src/daemon/daemon.sandbox-unrunnable.test.ts`) is a typo in the intent, not a defect in the subspec. `v2/docs/v1-behaviors.md` is correctly out of scope since this is a test-only reorganization with no change to operator-visible or wire-protocol behavior.