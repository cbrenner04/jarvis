# Operator session — 2026-07-27T19-10-10Z

36 PRs merged, zero open at close. Recovery batch complete (3/3); per-project pipelines slices 1 and
2 complete (1a, 1b, 2a, 2b, 2c). Agent order: codex-first for the first ~2h, then claude-first, then
codex-first again at operator request.

## Landed

### P0 recovery batch — complete

| # | Work | PRs |
| --- | --- | --- |
| 1 | write-loop iteration durability floor | plan #2225, impl #2236 |
| 2 | resume a durable review row through its mutation tail | intent #2219, plan #2227, impl #2233 |
| 3 | implement repairs a ticked run with a surviving mutation | intent #2224, plan #2234, impl #2246 |

### P1 — complete

| Work | PRs |
| --- | --- |
| review roles honor the configured idle budget | #2221, #2226, #2230 |
| `run log` snapshots live runs, `--follow` tails | #2222, #2232, #2238 |
| husk at managed path no longer strands re-dispatch | #2223, #2228 |

### Per-project pipelines — slices 1 and 2

| Slice | Work | PRs |
| --- | --- | --- |
| — | brief split into six seeds; routed seed → intent → plan → implement | #2220 |
| 1a | definitions, source registry, admission validation | #2235, #2237, #2240 |
| 1b | project config selects a pipeline | #2241, #2248 |
| 2a | durable pipeline + stage records | #2244, #2245, #2249 |
| 2b | daemon-owned ordered stage execution | #2251, #2255 |
| 2c | restart reconciliation | #2250, #2254 |

### Config and seeds

`#2242` bound codex's implement role to `gpt-5.6-sol` (from `terra`). Seeds filed: `#2231` →
corrected by `#2239`, `#2244` (gate-repair scope leak), `#2247` (posture table), `#2252` (split
discipline ×2), `#2253` (absent pipeline config, P0), `#2256` (cleanup socket abort).

## Headline: spec size predicts implement success

Every subspec at 28–36 lines landed on its first implement. The one spec planned at 157 + 125 lines
(`implement-repairs-ticked-surviving-mutation-run`) cost two failed runs and hand-holding:

1. First run died `idle_output_timeout` mid-write-step, losing **753 uncommitted lines** across 11
   files with 13 criteria already ticked and zero commits.
2. Second run touched implement admission, reddening ~10 workflow-dispatch tests at once and
   spending its repair budget on breadth.

Both are blast-radius failures, not model failures. Its single ready-intent covered a state-store
query, a new daemon request, CLI admission, and a bounded repair loop — four surfaces. The pipeline
seeds, which named distinct surfaces, split into 1a/1b and 2a/2b/2c unprompted and every one landed
first try. Seeded as `intent-splits-by-surface` and `plan-splits-an-oversized-subspec` (#2252), with
the plan-side guard specified as **auto-split**, not a refusal, and thresholds kept out of prompt
text at operator direction.

## A shipped slice bricked the primary command

`#2248` (slice 1b) treats an **absent** `projects.<key>.pipeline` as malformed, so every
`jarvis run workflow implement` refused with `invalid-project-pipeline-config`. Both queued slice-2
dispatches died on it. Unblocked by hand-adding `"pipeline": {"name": "full-review"}` to
`~/.jarvis/config.json` — inert today, since nothing reads the resolved definition. Seeded P0 as
`#2253`.

Second-order: v1 shares that config file and its validator rejects unknown project keys, so the same
edit **broke `jarvis1` outright**. Slice 2c fixed it the minimal way (accept and ignore); slice 2b
had independently written full v1 parsing and validation, which I reverted at merge — v1 is
maintenance-only and that was new investment.

## Agent comparison

| | codex | claude |
| --- | --- | --- |
| intents / plans | strong throughout; correct dependency blockers on 1b and 2b | strong |
| implements | 2 red on `terra` (hung tests by design; unrelated v1 test edited to go green); clean on `sol` (2a, 2b) | all landed green |

Two codex review-role stalls (183 s, 143 s) against the hardcoded 90 s review idle budget blocked a
plan lane twice — the exact defect #2230 fixed. After it merged, the same lane completed first try.
The `terra` → `sol` binding change (#2242) landed mid-session; 2a and 2b were its first consumers and
both landed.

## Manual interventions (each one is a harness gap)

- **#2228** — repair iteration edited an unrelated v1 test; reverted before merge.
- **#2243** — an *intent* workflow's repair iteration committed `scripts/test-slice.ts`,
  `test/test-slices.test.ts`, a v1 test, and two `.jarvis-intent-review-verdict.md*` sidecars. Closed;
  its three ready-intents hand-landed as #2244. Seeded.
- **#2248** — workflow died `invocation_error` at its review step *after* the write step committed
  twice: no PR, no gate, and re-dispatch would have discarded both commits. Hand-published. It also
  shipped `setInvertPipelineAdmissionGuardForTest`, a production test switch whose "inversion test"
  asserted its own canned error; removed.
- **#2233** — settled `surviving_mutation_failed`; wrote the missing coverage by hand (verified: fails
  against the flipped guard, passes clean).
- **#2246** — CI stopped queueing runs on the branch entirely; verified locally, then a merge-from-main
  push finally triggered it. Conflict resolved by union.
- **#2255** — merge conflict with 2c over `Pipeline` gaining `ownerIdentity`/`status`.

## My own errors

- Claimed a run settled `completed` over an exhausted red gate. The durable log shows
  `ready_gate_failed` / `resumable: true` after three repair attempts — the harness was right. I read
  a per-attempt `boundary_committed` mid-tail. Seed #2231 withdrawn and replaced (#2239).
- Claimed `jarvis cleanup` exits 0 on its socket error, inferring it from a commit that had actually
  come from the operator's *second* run. Reproduced properly in an isolated `JARVIS_HOME`: it exits
  **1**. Seed narrowed before filing (#2256).
- Committed once to the primary checkout's `main` because a `cd` into a worktree silently didn't take
  effect. Pushed to the branch and reset `main`; nothing reached `origin/main`.

The first two share a root: inferring system behavior from a signal I hadn't isolated. Both were
caught before the artifact shipped, but only because the claim was re-checked against the durable
record.

## Friction not seeded

- `jarvis config set-agents` takes one comma-joined argument, not space-separated words.
- Writes to `~/.jarvis/**` and all `git fetch`/`push` need the agent sandbox disabled.
- 2a's workflow kept running gate repairs after its PR merged; `jarvis run kill` refused
  `run_not_active` while `run list` showed the row live (owned by a different keyed daemon).
- `createPipeline`'s `beforeStageInsert` fault-injection seam is a test hook in a production
  signature. Spec-sanctioned and it proves real rollback, unlike the 1b flag — noted, not seeded.

## Cost

Agent-side, from `~/.jarvis/telemetry.jsonl` (268 role invocations, 16.9 h of agent wall time):
**$156.84**, all of it `claude` — `codex` and `cursor` are subscription-billed and record `$0.00`.
Operator-side API cost is recorded in the cumulative CSVs.
