- Unavailable-command lookup must recognize only `approve`, `reject`, `resume`, `kill`, `pause`, and `log`. Inputs such as `constructor`, `toString`, and `__proto__` must return `unknown_verb`, never `recognized_unavailable` or a non-string command value. Add regressions covering inherited object-property names. This is required by the spec’s exhaustive unavailable-verb classification and exported typed result contract.
