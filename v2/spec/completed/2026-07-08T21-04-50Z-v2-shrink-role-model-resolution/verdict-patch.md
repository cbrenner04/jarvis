**Verdict:**

1. **Restore dedicated actuator-missing-role coverage in `v2/src/config/machine-profile-loader.test.ts`.** The only "missing required role" test in this file was repointed from `actuator` to `shrink` (diff: `actuator: undefined` → `shrink: undefined`, and the corresponding assertion), rather than adding a `shrink` case alongside the existing `actuator` one. AC1 for this subspec requires *extending* existing required-role missing-role coverage to include `shrink` — extending means both cases exist, not one replacing the other. Add back a test asserting `actuator` missing triggers the same hard-error shape, in addition to the new `shrink` case. (Note: `v2/src/config/agent-model-config.test.ts` already covers both `actuator` and `shrink` missing-role cases independently, so this fix is scoped only to `machine-profile-loader.test.ts`.)

No other outcomes are required. The claimed stale doc bullet in `v2/docs/role-resolution.md` is not an issue: the file already contains an explicit decision bullet ("Model-resolution `shrink` is separate from telemetry phase") disambiguating `shrink` (role) from `patch_phase: "shrink"`, satisfying the intent's disambiguation requirement. No further doc edit is needed.