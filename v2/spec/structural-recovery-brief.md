# Structural recovery brief

Standing document: what is open, in what order, and the operating rules that still earn their place. Rewritten 2026-09-18 from a full backlog audit (every seed, ready-intent, and open spec verified against `main` @ `368dfd9ca`). Session narratives live in `reports/`; per-item tracing in [`structural-recovery-seed-ledger.md`](./structural-recovery-seed-ledger.md). Neither document is a journal: when a session lands something, move the row, do not append a paragraph.

## Where it stands

The 2026-08-29 charter is done. All five structural retirements landed (front door, run-row-derived settlement #3745 + foreign-owner liveness #4023, atomic terminal writes, both module splits, dead-export/test-seam gates), and the three frontiers that followed them are closed: **publication** (#3794 and its three lanes), **daemon identity** (stable address, self-handoff, committed-successor watch #4022; digest artifacts retired #3903), and **recovery honesty** (linked-row resume through the workflow #3977/#3982, owner stamping #3981/#3987, conclusive base-ref probe #3990/#3995, committed-lane continuation #4014, `recover` on `landing_failed` #4025). Fix share of `v2/src` commits fell from 63% pre-reset to ~14% and has stayed there.

What is left splits into two kinds. **Fixed points that strand complete work** — a lane finishes, a gate or tail refuses for a reason the lane cannot change, and `run resume` replays the refusal. Five of those are open (P0 below) and each was hand-finished at least twice. **Legibility and recovery chains** — four open specs, each heading a strict ready-intent chain, that make a failure diagnosable and recoverable without reading `v2/src`. Everything else is dogfood quality or parked display work.

Counts after 2026-09-18 second session: 14 open specs (plus 2 landed dirs awaiting archival), 14 queued ready-intents (2 dispatchable now), 31 seeds. Landed: #4046, #4048 (ledger § Landed).

## Priority-ordered work

| P | Item | Why |
| --- | --- | --- |
| **P0** | Terminal-incident-once chain: spec `owning-daemon-writes-invocation-settled-marker` (#4034, implement parked on `slot_contention`) → [[run-ad-hoc-terminal-derives-from-settled-marker]] | Store landed #4011. Third session of double or early terminal notifications on the primary wake path |
| **P0** | [[daemon-changeover-rebind-test-flakes-under-load]] (#4049) | Stranded a lane via a false `ready_gate_out_of_scope` and recurred; intent pipeline c09393f9 failed on quota, re-dispatch |
| **P0** | Spec `ready-gate-repair-fence-revert-and-settle` (#4041) → [[ready-gate-repair-prompt-targets-failing-step]] | Three recurrences 2026-09-14/16; repair wandered 36 files while the one red step sat in-diff |
| **P0** | Spec `render-observer-verification-keeps-a-fixed-deadline` (#4039; pipeline 020135f4 implement parked on `slot_contention`) | A registered prompt mapped to a >30 s test file fails deterministically and resume replays it |
| **P0** | Spec `gate-allowset-derivation-handles-external-and-empty-spec-scope` (#4042; #3423, #4004) → [[fence-derivation-failure-settles-honestly]] | Two branches into one dead end at flip-to-ready on published work |
| **P0** | Spec `rebased-lane-publishes-with-lease` (#4043; intent PR #4040 pending merge) | #4014 rebases continued lanes; the completion push is non-force. Predicted, not yet observed |
| **P0** | Spec `publication-failures-settle-failed-writer` (#4045) → [[publication-failure-rows-migrate-failed]] → [[failed-publication-consumers-drop-completed-special-case]] | `completion_commit_failed` / `ready_flip_failed` rows read `completed`; every consumer special-cases the cause |
| **P0** | Spec `linked-implement-routing-settles-a-real-outcome` (#4044) → [[detach-admission-refuses-without-a-run-row]] | Routing failure mints a phantom run id and returns `complete`. Silent lane loss under `--detach` |
| **P0** | [[wal-lock-holder-child-exits-silently]]; [[coscheduled-test-pair-strands-runs-terminally]] (scheduling half) | Load flakes that reach the gate; the probe half of the second landed (#3990/#3995), the runner still co-schedules the pair |
| **P1** | Chain A: spec `stage-success-reopens-skipped-successors` landed #4046 → spec `branch-resume-admits-skipped-successor-lane` planned #4047 → [[branch-resume-refusal-names-blocking-row]] | A re-driven lane keeps a `skipped` successor no verb reopens until the branch-resume spec lands |
| **P1** | Chain B: spec `persist-gate-refusal-recovery-state` (#3822, implement in progress, paused on `five_hour` quota) → [[redrive-slot-refused-gates]] → [[report-gate-refusal-causes]] | Slot refusal is honest but nothing re-drives it; three implements slot-refused 2026-09-18 — land it first |
| **P1** | Chain C: spec `serve-canonical-failures-from-daemon` (#3823, 5 subspecs) → [[render-operator-failures-consistently]] | Failures still diagnosed by reading source; `failureDetailFromLogs` composes prose, TUI dumps raw detail. Note prereq 2 is only partly true: producers are the ready gate and publication, not intent/plan/implement |
| **P1** | Chain D: spec `surviving-mutation-settlement-records-killing-set` 01 (00 landed #3787) → [[mutation-repair-commits-pushed-before-settlement]] → [[repair-exhausted-error-names-site-and-killing-set]]; then [[non-terminating-mutation-settlement-names-its-site]] (one-line spread, same sites) | `mutation_repair_exhausted` is terminal and unpushed repair commits left green CI describing a tree that no longer existed |
| **P1** | Resume honesty residue: [[pipeline-resume-resumes-a-resumable-implement-row]]; [[resume-surfaces-admission-gate-refusal]]; [[interrupted-pipeline-stage-cannot-be-resumed]] (#2996, carved from the closed settlement seed); [[explicit-reset-flag-names-continue-path]] (dispatchable) | A stage never reflects a lane recovered by `run resume`; admission refusals reach only the daemon log; a killed stage is a dead end |
| **P1** | Retirement safety: [[stale-reset-destroys-commits-for-external-specs]] (re-scoped: gate 2 blind to external trees); [[implement-retirement-destroys-artifacts-before-materialization]]; [[abandon-refuses-unlanded-work-with-no-pr]] | #4014 removed the branch deletion in the #3433 shape; what remains is the blind gate, the `--base <own-branch>` collision, and `--abandon` on unpublished work |
| **P1** | Cleanup and home: spec `cleanup-archives-hand-landed-specs`; [[cleanup-reaps-orphan-session-logs]] (dispatchable) → [[retention-tiers-for-session-logs-and-telemetry]]; spec `bulk-terminal-run-dismissal-cli`; [[worktree-materialization-fails-on-committed-node-modules-symlink]] (#4003, half landed) | Hand-landed specs need an archive PR every session; orphan logs live forever |
| **P2** | Dogfood quality: [[review-roles-check-falsifiability-not-plausibility]]; [[implement-respects-target-repo-doc-layout]] (#3426; leak 3 closed by #4029, test pins leak 1); [[intent-split-covers-sibling-repo-surfaces]] (#3439, re-scoped); [[detached-pipeline-plan-stage-consumes-ready-intents]] (#3041); [[per-project-config-overrides-seam]] (#3026/#3150); [[implement-can-run-integration-slice-tests]] (re-scoped to measurement criteria); [[mutation-verifier-ignores-whitespace-only-line-changes]]; [[self-parsing-structural-tests-can-bind-to-their-own-fixtures]] (one file, not a corpus); [[completed-write-step-rows-stamp-finished-at]]; [[serial-rerun-includes-frozen-v1]] | Each recurs but none strands a lane |
| **P2** | Fan-out: [[superseded-pipeline-pr-hygiene]] (unblocked by #3745); [[pipeline-fan-out-per-lane-terminal-settlement]]; [[pipeline-fan-out-lanes-serial-chained-bases]]; spec `tui-consumes-retained-pipeline-list` | Fan-out pipelines still derive `failed` after every lane succeeds |
| **P3** | [[agent-confinement-is-per-vendor-and-unexpressed]] (#1453); [[cli-retire-run-start-pause-and-config]]; [[tui-dock-command-grammar-mirrors-cli]]; [[tui-typed-run-steering-clears-command-input]] | Parked; see the open decision on `run pause` below |

Dispatch order for the next session: the P0 rows top to bottom, one implement at a time; fan out the four chain-head specs (A–D) as plans and standalone implements in parallel with them, since each head is on `main` already. Read `## Prerequisites` before approving any fan-out gate.

## Contradictions and decisions surfaced by the audit

- **Retired 2026-09-18 as a contradiction:** `merge-pipeline-stage-pr-at-its-approval-gate` prescribed merging the intent PR at `approve-intent` and re-resolving the plan base, the opposite of [[detached-pipeline-plan-stage-consumes-ready-intents]] (consume from the source the plan actually read) and of `first-workflow-walkthrough.md`, which says inter-stage merging is not required. Consume-from-source is kept; the stacked-PR cleanup half moved to [[superseded-pipeline-pr-hygiene]]. Revert by restoring the seed if you prefer merge-at-gate.
- **Open decision, `run pause`:** [[cli-retire-run-start-pause-and-config]] deletes it first, while #3853 added a `run-paused` incident on every paused row and [[tui-dock-command-grammar-mirrors-cli]] aligns the dock to it. Decide whether pause stays before planning either seed.
- **Corrected in place:** [[implement-can-run-integration-slice-tests]] decision 3 contradicted `prompts/implement/rules.md:29` (#3867, tick harness-run suites on in-sandbox checks); re-scoped to measurement criteria. [[implement-admission-persists-its-run-row]] decision 3 contradicted #4014's continuation; rewritten around the phantom-id root cause. [[intent-split-covers-sibling-repo-surfaces]] relied on a `siblings` key that exists only in frozen v1. Spec `stage-success-reopens-skipped-successors` intent said same-transaction, its subspec says separate; the intent now matches the subspec. Spec `surviving-mutation-settlement-records-killing-set` 01 counted 16→18 `run list` columns; the row is already 17.
- **The brief had marked two open seeds as landed** by citing their seed PRs as fixes: #3833 (render-observer deadline) and #3832 (`daemon status` reports `stopped` on a busy daemon). Both are open; the status seed's cited classifier was also renamed (`classifyLegacyDaemonUnit` / `probeSocketLiveness`).
- **The ledger recorded `pipeline-settlement-derives-from-run-rows` as closed** (#3745) while the seed stayed on disk carrying #2996, which #3745 did not change. Carved into [[interrupted-pipeline-stage-cannot-be-resumed]]; seed reaped.
- **Issues closed 2026-09-18:** #3949, #3974, #3372.
- **Docs contradicting each other on the publication-failure row status** (`write-behavior.md:85` vs `:585/598` and `daemon-host.md:301`) are the subject of [[publication-failures-settle-failed]], not a separate fix.

## Operating rules

**Circuit-breaker (per-lane, per-gate).** A lane needing hand intervention twice in a row on the same gate leaves Jarvis until the gate fix merges; hand-land instead, re-open after one clean end-to-end run. Mirrored in the runbook.

**Parallelization.** Working number is three concurrent implements and one gate slot (`MAX_CONCURRENT_AGENT_GATE_INVOCATIONS = 1`, conservative, operator decision to leave it). Resume a slot-refused lane on zero live lanes, not on a settle notification. 2026-09-18: three concurrent implements slot-refused each other three times; until chain B lands, run implements one at a time. Intents and plans fan out freely; approve fan-out gates back to back after reading `## Prerequisites`. Do not merge `v2/src` while lanes are live: the decision verbs wait on a drain (`pipeline_no_live_owner` is honest, self-resolving, and operator latency).

**Never merge an autonomous implement without an independent diff review.** Measured: 2026-09-17 late, 3 of 5 implement PRs carried a defect the gates passed (#4012, #4014, #4017: ticked criteria on tests that exercised nothing, dropped guards, corruption cases); 2026-09-18, 1 of 5 (#4060: self-ticked a consecutive-pass criterion on a test still failing 3/5 under load). Review also catches weak-but-harmless tests the gates cannot see. A green mutation `pass` is not evidence either: read `acceptedSites`, and commit before running it. A ticked criterion whose only evidence is a `*.sandbox-unrunnable` file is unverified.

**Salvage beats re-dispatch.** A strand after the work is committed is a publishing problem: rebase the branch onto current `main`. Check `git diff origin/main...<branch>` before hand-implementing "remaining" subspecs, and `git log -1` in the worktree before trusting a local run against a lane under repair.

**A dirty guard in a wedged worktree may be the verifier's mutant.** Check parentage of any hot `bun test` (`ps -o ppid=`) before concluding anything: daemon-parented is a live gate child, `launchd`-parented is a leaked orphan.

**Guards that destroy treat inconclusive as "do not act".** `ENOENT` from a sandboxed caller, a failed `gh` probe, a timed-out socket probe: none is authoritative. A `stopped` from `daemon status` on a busy machine is exactly this shape and is still unfixed.

**Land-a-slice.** A multi-subspec spec converges only with immediate re-dispatch after each merged slice. Implement PRs carry the spec tree, so close the plan-stage PR as subsumed once the implement merges; intent-stage PRs must merge because they carry the seed deletion.

**Quota is a window.** `five_hour` rejected with `seven_day` headroom means pause until `resetsAt`; only a rejected `seven_day` ends a session.

**Plans invent precision, and `@mutate` pins that do not kill are worse than none.** Verify named tests exist at plan review; verify a pin by deleting the line it names. A mechanical post-hoc rewrite of agent output is a defect surface (the #3628 lesson), so prefer a reprompt to a normalizer.
