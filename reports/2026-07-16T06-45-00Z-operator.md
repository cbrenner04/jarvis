# Operator session — 2026-07-16 (v2 dogfood: ready-to-run + P0, terra actuator, review restored)

Drove v2 (`jarvis`) to clear the **Ready-to-run** and **P0** backlog buckets. Mid-session the
operator flagged a review-passes regression and prioritized the review-crash fix; both handled.
Actuator moved from codex/luna to codex/**gpt-5.6-terra** after luna produced hollow ACs.

## Implementation PRs merged (8)

| PR | Spec | Agent | Notes |
| --- | --- | --- | --- |
| [#1610](https://github.com/cbrenner04/jarvis/pull/1610) | shared-invocation idle-output watchdog | codex/luna | clean |
| [#1617](https://github.com/cbrenner04/jarvis/pull/1617) | daemon-stop-refuses-active-runs | codex/luna | operator-finalized (complexity extract + format) |
| [#1620](https://github.com/cbrenner04/jarvis/pull/1620) | distinguish-ready-gate/flip-failures | codex/terra | P0 publication core split |
| [#1622](https://github.com/cbrenner04/jarvis/pull/1622) | non-live-workflow-claim-reclaimable | codex/terra | |
| [#1630](https://github.com/cbrenner04/jarvis/pull/1630) | review-without-evidence-fails-the-run | codex/terra | clean re-run fixed luna's hollow AC2 |
| [#1629](https://github.com/cbrenner04/jarvis/pull/1629) | tui-row-navigation | codex/terra | real ink wiring verified |
| [#1633](https://github.com/cbrenner04/jarvis/pull/1633) | rehydrate-review-profiles-after-snapshot | codex/terra | **review crash fix — verified live** |
| [#1637](https://github.com/cbrenner04/jarvis/pull/1637) | ready-gate-fails-on-any-red-test-file | codex/terra | after spec-AC fix #1636 |

Every implement PR ran with in-harness review OFF (see review findings) and was gated by a
**subagent diff review** + a Biome format finalize (the red-gate-doesn't-feed-back gap ships
unformatted code every run).

## Config / seed / backlog PRs merged

- Config: [#1619](https://github.com/cbrenner04/jarvis/pull/1619) codex implement model luna → terra.
- Seeds: [#1611](https://github.com/cbrenner04/jarvis/pull/1611) restore-default-review-passes;
  [#1626](https://github.com/cbrenner04/jarvis/pull/1626) 4 session findings;
  [#1636](https://github.com/cbrenner04/jarvis/pull/1636) ready-gate spec AC fix.
- P0 intents: #1604, #1605, #1606 (3 seeds → 6 ready-intents).
- Plans: #1607, #1608, #1613, #1614, #1615, #1616, #1627, #1632.
- Review-crash pipeline: #1631 (intent) → #1632 (plan) → #1633 (impl).
- #1635 reviewed-intent (produced by the now-working review).

(#1612, #1621, #1623 were the owner's concurrent PRs — rungs, README, standalone-review seed.)

## The headline: review was crashing, now restored

Every daemon-run review step crashed on `args.profile.render.critic is not a function` — the
review prompt-profile's render callbacks don't survive the workflow JSON snapshot. #1633 persists
`profile.domain` and rehydrates renderers via a domain registry before dispatch. **Verified live**
after a (correct, post-pull) daemon bounce: a light-review intent ran `critic gpt-5.6-terra ok` +
`actuator gpt-5.6-terra ok` — no crash. Review is functional again.

Related review findings: the composable-collapse regressed bare-preset `reviewPasses` to 0 (#1611
seed); the owner's `standalone-review-mode` seed (#1612) supersedes #1611's direction (remove
inline review vs restore it) — **#1611 should be pruned as superseded.**

## Actuator: luna → terra

codex/gpt-5.6-luna produced hollow/red ACs on 3 of 4 implement runs (ticked-but-unimplemented AC2,
red gate, wrong wedge discriminant). Switched implement to **gpt-5.6-terra** (#1619); terra
produced correct logic on every subsequent spec reviewed (distinguish, non-live-claim,
review-evidence, tui, rehydrate, ready-gate) — residual issue is only unformatted output. Escalation
(rungs / agents array) fires on quota/stall, **not** on bad output, so it doesn't help quality — the
model is the lever.

## Harness findings (seeded)

- **Review crashes across snapshot** (owner seed, fixed #1633).
- **Default review passes regressed to 0** (#1611; superseded by standalone-review-mode).
- **daemon-restart auto-resume flips snapshot-less orphans to `failed`** (#1626; blocked #1625).
- **v2 re-run reuses stale origin branch** instead of re-implementing — needs origin-branch delete;
  no jarvis-native clean re-run (#1626).
- **Parallel-planned sibling specs conflict** once the core sibling lands — the publication cluster
  (#1626); consolidate the remainder against post-#1620 code.
- **shared/invocation abort doesn't clear the idle timer** (#1626, latent race from #1610 review).
- **red-gate doesn't feed back**: every implement ships unformatted (Biome format) and/or fails the
  sync-child-process guard (#1634) — AC "tests pass" is ticked without running `check`/guard.
- **Publication `gh pr ready` strands** repeatedly (masked "transient network error"), even on fresh
  branches — recovery is reopen/hand-create the PR. Reinforces existing seeds.
- **cleanup --abandon says "unknown worktree" for v2 worktrees**; v2 has no cleanup (existing seed).
- **codex cost unrecoverable**: all 53 v2 invocations record null cost, AND `data/prices.json` has
  no gpt-5.6-luna/sol/terra rates — reconstruction blocked (extends the codex-cost seed).

## Deferred / claude-phase queue

- **#1634 daemon-status** — logic correct; fails sync-child-process guard (`execFileSync git`), needs
  async git rework + a guard AC.
- **#1625 daemon-restart** — real regression (flips snapshot-less orphans to failed); needs spec
  decision (#1626 seed).
- **wedged-run-killable** — hard spec; both luna and terra botch the reapable discriminant; cleaned
  for a claude re-run.
- **Publication cluster** (#1624 + #1616 + repair-red-gate + failed-flip) — stale vs #1620 split;
  consolidate + re-plan on current main.
- **stale-daemon-refuses-new-work** — blocked on daemon-status landing.

## Verification discipline

Held every daemon-lifecycle merge for subagent review before bouncing — this caught the #1625
restart regression that a "verify by bouncing" approach would have shipped. Verified the review fix
by re-running an actual review workflow (not just its test). Corrected one self-inflicted error: an
initial bounce ran before the `git pull`, loading pre-fix code — re-bounced after pull.

## Cost

| Row | Value |
| --- | --- |
| v2 harness exec | **2:41:24** across **64** codex invocations (36 terra / 22 sol / 6 luna) — includes ~4.3 min of concurrent owner runs; ~157.1 min mine. Per-spec plan/run times recovered from `telemetry.jsonl` `duration_ms` by branch. |
| v2 harness spend | **unavailable** — `usage_source: unavailable` on all 64 rows (codex wrote no session rollout under `~/.codex/sessions`, so the documented tokens × `data/prices.json` reconstruction has no input). `prices.json` *does* carry gpt-5.6 rates; the missing input is usage. No warning is emitted on the unavailable path — see ready-intent `codex-unavailable-usage-is-diagnostic`. |
| Operator (Claude Code) `/cost` | **$83.33** — opus $71.87 + sonnet $11.45 (subagent PR reviews) + haiku $0.0022; API 2:06:29, wall 10:32:19 |

See the five cumulative CSVs for the reconciled rows.
