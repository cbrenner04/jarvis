**Required outcome:** Add explicit coverage in `AGENTS.md`'s scoped-test sentence (and the subspec's Decisions, if referenced there) for the case where both `v1/**` and `v2/**` are touched without `shared/**` — state that surfaces are additive/unioned (run `test:v1` + `test:v2` + `test:integration:v2`), matching `classifyChangedPaths` in `scripts/ci-test-scope.ts`. This is required because the subspec's own stated principle is to reuse CI's exact classification rather than a new or looser rule, and the current four-bullet enumeration doesn't make the union behavior unambiguous for an agent following the instruction.

The clause-scoping concern (whether "before ticking the acceptance criteria they cover" ties to the whole compound instruction) does not need action — the sentence's grammar already binds it correctly, and AC #2 is satisfied as written.