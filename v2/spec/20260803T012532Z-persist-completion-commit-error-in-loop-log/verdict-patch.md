- Enforce the event contract at the TypeScript boundary: `completionCommitError` must be accepted only when `loopOutcomeKind` is `completion_commit_failed`, while remaining optional and compatible with `publicationFailure`. Add compile-time regression coverage rejecting other outcome/field combinations. Raw legacy JSONL must remain readable without runtime rejection. This is required by the subspec’s explicit validity rule; a comment alone does not enforce it.
