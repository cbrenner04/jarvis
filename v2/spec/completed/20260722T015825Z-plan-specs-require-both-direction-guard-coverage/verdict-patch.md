1. Replace self-fulfilling output coverage. Drafted criteria must depend on the production prompt behavior, not a test-only branch that manufactures expected output from intent text. Coverage must fail against the prior prompt and prove code-touching classification, inverted-guard wording, and suppressed-effect absence.

2. Add explicit spec-only coverage proving no both-direction guard criterion is emitted. Documentation-only coverage does not satisfy the separate spec-only acceptance requirement.

3. Establish one normative durable home for the contract. The other required documents must cross-link or provide only context-specific references, avoiding duplicated normative wording per `v2/docs/documentation-standard.md`.

4. Keep affected acceptance criteria incomplete until the revised coverage and documentation structure genuinely satisfy them.
