Reviewing staged intents against spec guidance and repo conventions; checking how "module boundary" and the plan step are documented.
## Verdict

**`plan-emits-one-subspec-per-module-boundary`**
- **Module boundary** is the oversize signal but neither a decision nor the `spec-guidance.md` bullet defines it. Align with `intent-splits-by-surface` (surface = boundary; name exemplar boundaries) in the doc AC or an explicit “same definition as intent split” decision — otherwise plan and intent lanes can diverge.
- Split trigger is **acceptance-criteria–aggregate only**. No decision for a draft whose `## Decisions` span multiple boundaries while each AC stays single-boundary (still one implement blast). State that case is in or out of scope.
- No explicit **k > 2** rule (one draft → k subspecs). Strongly implied; one decision line would remove ambiguity.
- **“existing plan coverage stays green”** is preservation prose without a test anchor; fine at intent level if subspecs cite pinning tests.

**`plan-split-index-orders-by-dependency`**
- Missing **guard-inversion AC** (spec guidance: runtime behavior + guards). Intent 1 has one; this intent should mirror it for order enforcement.
- **Dependency source** undecided: inferred by plan agent, fixture-declared only, or required draft structure. AC says “declared dependency” in the fixture but the product decision should say what operators/drafts must supply.

**`plan-split-preserves-draft-scope`**
- Missing **guard-inversion AC** (same as above).
- **Distribution scope** is “decisions” + “acceptance criteria” only. Silent on `## Documentation updates`, problem/evidence duplication, and other subspec sections — load-bearing for “exactly one” / no-drop contract.

**Cross-cutting**
- Three same-seam plan-step intents are appropriately **chained via Prerequisites**; plan/implement **serially** per spec guidance (not an intent defect).
- `v1-behaviors.md` omission is consistent with other net-new v2 workflow specs.

**Ready if addressed:** boundary definition + AC-only vs decisions trigger + inversion ACs on intents 2–3 + preservation section scope (+ dependency source for intent 2). Mechanism (prompt vs post-draft pass) can stay for plan draft if tests stay behavioral.