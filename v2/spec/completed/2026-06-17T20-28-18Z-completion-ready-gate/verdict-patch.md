I'll ground my verdict in the actual implementation before issuing it. Let me read the load-bearing seams to verify the disputed claims.Now let me read the key orchestration seams and the test setup mechanics.Now let me check the per-iteration `maybeMarkReady` site and its guard, plus the test helpers.The per-iteration site at run.ts:1093 is guarded by `if (!willRunReview && !willRunShrink)`. On the default common path (review enabled), this guard is false, so the per-iteration site does not fire. The advocate's read is correct. Now let me verify the test scaffolding claims (C3) and the `runWithDefaults`/`skipGhCheck` mechanics (C1).Confirmed: C3 is valid. Lines 710-711 are dead scaffolding — `readyCallCount` is `const` initialized to 0 and never read; `originalRunReady` is assigned via `require` and never used. Both will trip Biome `noUnusedVariables`. Now let me verify `runWithDefaults` and `skipGhCheck` to adjudicate C1, then check the instrumented count assertions.`runWithDefaults` sets `skipGhCheck: true` (run.test.ts:98). Let me verify the worktree-block guard in run.ts and the instrumented count assertions at ~1815/1971.Confirmed: C1's central premise is false for the new tests. The worktree block at run.ts:349 is guarded by `!opts.skipGhCheck && gitEnabled`. With `runWithDefaults` setting `skipGhCheck: true`, `ensureWorktree` is skipped and `agentWorkingDir = cwdOverride ?? project.root` (run.ts:346) — i.e., `projectRoot`, the same directory the FakeAgent writes to. The "wrong directory" thesis collapses. `gitEnabled` is still true, so the completion gate runs against `projectRoot`.

Now let me confirm the instrumented count assertions at ~1815/1971 are real end-to-end coverage.Confirmed: this pre-existing end-to-end test (with a real `bun`/`gh` PATH stub and `readyGateLog` counter) asserts the ready-gate count dropped from 2 to 1 (`["ready-gate"]`), proving the reuse behavior through the real worktree+PR path. This is the load-bearing, instrumented coverage of the core claim.

Now let me check the trailing-whitespace claim in run-loop.md and the `shared/git.ts` `getCurrentBranch` env behavior.Confirmed: lines 133, 134, 135, 136, 137, 138, 139 have trailing whitespace (visible as a space before the line break). Biome's `check` (which the spec's own completion gate runs) flags trailing whitespace. M1 is valid.

Now let me verify the `getCurrentBranch` env behavior change (S3) by comparing the shared helper to the deleted local one.Confirmed: the shared `getCurrentBranch` (shared/git.ts:34) omits the `env: process.env` that the deleted local copy in ready-gate.ts passed. `execFileSync` inherits `process.env` by default, so this is functionally equivalent and benign — but it is an unannounced swap of an existing helper bundled into a feature change. Low risk; worth confirming, not blocking.

I now have ground truth on every disputed seam. Let me verify the shrink empty-allowlist guard to confirm whether the per-iteration doc parenthetical (M2) is a genuine precision issue.I have sufficient ground truth. The empty-allowlist guard mechanics are documented in the spec (`01` AC#7 covers it) and the implementation's `shouldRunShrink` guard at run.ts:1377 keys on `implementationIterations > 0`. The remaining item to confirm is the per-iteration doc parenthetical (M2): run-loop.md:133 says `maybeMarkReady` reuses at "both the completion-transition and per-iteration early-ready sites" — but the per-iteration site (run.ts:1093) only fires when `!willRunReview && !willRunShrink`, on which path the carrier is still undefined, so it cannot reuse. That is a genuine but minor doc-precision overstatement.

I have everything needed to adjudicate. The production code is sound; the disputed C1 worktree indictment is built on an untraced `skipGhCheck: true` short-circuit and does not hold; the core reuse behavior is genuinely covered by the instrumented count assertions. The valid issues are concentrated in test/doc hygiene plus one real coverage gap. Issuing the verdict.

---

# Verdict — `2026-06-17T20-28-18Z-completion-ready-gate`

The production implementation is correct and matches the spec: the completion-transition gate runs `ready` once via `runReadyAndCommit`, records a green result keyed to post-`runReadyAndCommit` HEAD sha + clean worktree, threads an orchestrator-local carrier into each gate's own options type, refreshes on re-run, applies the two-part (sha **and** clean-porcelain) reuse predicate, keeps the review final gate unconditional, and upholds the exit-6 cleanliness guarantee on `maybeMarkReady`'s reuse branch via the predicate's clean-worktree check. The core reuse behavior is genuinely covered end-to-end by the instrumented `readyGateLog` count assertions (the count drops from two `ready-gate` entries to one through the real worktree+PR path). The objection that the new tests write to the wrong directory does not hold: those tests run with `skipGhCheck: true`, which short-circuits the `ensureWorktree` block, so the harness reads and writes the same project root the fake agent does.

The required outcomes below are concentrated in test and doc hygiene plus one real coverage gap. None change production behavior.

## Required outcomes

1. **Remove the dead test scaffolding** in `v1/test/run.test.ts` (the `readyCallCount` constant initialized but never read, and the `originalRunReady` value obtained via a bare `require` and never used). This is leftover from an abandoned instrumentation attempt and will fail the lint rule that the very gate this spec adds runs. *Blocking: the spec's own `ready` gate enforces no-unused-variables.*

2. **Eliminate the trailing whitespace** introduced in the new `v1/docs/run-loop.md` completion-transition section (the "available to post-completion phases…" paragraph). Trailing whitespace is flagged by the formatter/linter the completion gate runs. *Blocking: same self-enforcing gate.*

3. **Add a direct unit test for the reuse predicate** (`isTreeUnchangedSinceRecordedGreen`) covering its four cases: HEAD matches + worktree clean → unchanged; HEAD matches + worktree dirty → changed; HEAD mismatch → changed; no recorded result → changed. The clean-worktree half of this predicate is the decision-critical safeguard the spec names as the stand-in for the skipped `runReadyAndCommit` re-check on `maybeMarkReady`'s reuse branch (the exit-6 guarantee). No current test constructs the match-HEAD-but-dirty state and asserts a re-run, so the safeguard is asserted only in prose. *High value: fills the one genuine coverage gap on a load-bearing invariant.*

4. **De-duplicate or strengthen the new "post-completion gate reuse" tests** in `v1/test/run.test.ts` so each test name matches what its assertions verify. The block of near-identical tests asserts behaviors in their names (shrink-commit forces a re-run; `check:fix` chaining; empty-allowlist contributes no run; red-completion fallthrough) that the shared coarse assertions (`code === 0`, `spec complete`, clean porcelain) cannot distinguish. Either instrument them to assert the distinguishing state per name, or collapse the redundant ones to those their assertions actually cover, so test names do not overpromise. *Quality, not correctness: the decisive count is already proven by the instrumented end-to-end assertions, but misleading test names are a maintenance hazard.*

5. **Resolve the unannounced `getCurrentBranch` helper swap** in `v1/src/ready-gate.ts`. The change replaced a local helper that passed `env: process.env` with the shared `shared/git.ts` version that omits it. This is almost certainly equivalent (`execFileSync` inherits `process.env` by default), but it is an unannounced refactor of existing behavior bundled into a feature change, against the "no speculative refactors" rule. Either revert to the local helper or confirm and note the equivalence so the swap is intentional and documented. *Low risk, low cost.*

6. **Correct the per-iteration-site reuse claim** in the durable docs (`v1/docs/run-loop.md` and `v2/docs/v1-behaviors.md`). The docs state `maybeMarkReady` reuses the recorded result at "both the completion-transition and per-iteration early-ready sites." The per-iteration site fires only when neither shrink nor review will run, and on that path the recorded result is still unset when it runs, so it cannot reuse — it runs `ready` itself. Reword so the docs do not imply reuse on a path where call ordering prevents it. *Doc accuracy: the durable behavior record must not overstate where reuse occurs.*

## Not upheld

- The claim that the new tests operate against a worktree and therefore silently fail or mis-target — rejected. With `skipGhCheck: true` the worktree block is skipped and the agent and harness share the project root; the tests reach the completion transition and run green.
- The framing of the per-iteration `maybeMarkReady` ordering as a double-`gh pr ready` regression — rejected. Both `maybeMarkReady` sites and their `gh pr ready` calls predate this change on the zero-shrink/zero-review path; this change adds carrier threading, not a second invocation. On the pinned default common path (review enabled) the per-iteration site does not fire at all, so the "one reusable ready" accounting holds.
- The two-part predicate's dirty-worktree arm and the conservative "tree changed" definition (HEAD moved **or** dirty **or** no result recorded) are correct, exercised behavior, not over-specification. Keep them.

I did not edit any files.