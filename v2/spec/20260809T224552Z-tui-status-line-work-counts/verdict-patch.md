1. Pipeline-linked workflows must count only through their pipeline when the entry run ID differs from the workflow invocation ID, as it does in production. Add production-shaped coverage proving matched groups are excluded from ad-hoc counts; otherwise the spec’s one-count-per-work-item guarantee is violated.

2. Reachable-gate classification must derive from immutable admitted pipeline data or an authoritative daemon-projected boundary, not the TUI’s current registry definition. Cover changed, removed, or unknown registry definitions so retained snapshots remain truthful across revisions. This is required by the fan-out gate and retained-observation criteria.

3. Align durable operator/architecture documentation with any resulting snapshot or linkage contract changes.
