# Structural recovery brief

Phase tracker for the v2 structural recovery, from the 2026-08-29 code review of `v2/src`. **Standing document**: current priorities and operating rules only. Session narratives live in `reports/` (one per session since the reset); per-seed tracing lives in [`structural-recovery-seed-ledger.md`](./structural-recovery-seed-ledger.md). Rewritten 2026-09-05 (compaction) from the journal it had become.

## Why this phase, and where it stands

237 of 376 v2 code commits over the six weeks before the reset were fixes, clustered on a handful of structural seams — terminal state written on the happy path only, config assembled twice with one copy stale, settlement copied then redriven. The recovery's charter: five structural retirements that close whole classes — one dispatch front door, settlement derived from run rows, atomic terminal writes, the module splits, dead-export/test-seam gates.

**As of 2026-09-05 it is mostly working**: since 08-31, 11 of 79 `v2/src` commits were fixes (~14%, was 63%), zero reverts. Closed: front-door chain (#3226 et al.), both module splits (workflow-runner #3380/#3388/#3393, daemon #3364–#3394), mutation-gate P0 chain (4/4), watchdog trio, external-spec chain (#3122), notification-sweep CPU fix (73–98% → 4.6–15%), daemon socket/status honesty (#3468–#3473), CLI retirements (`write`, aliases, `TuiDaemonClient.start`). The remaining bug frontier is concentrated where the one unfinished retirement lives: **chained/linked-row settlement and resume** (the 09-02→09-05 issue wave: #3462/#3463/#3395, #3464, #3400, #3433), plus the external/`plan.commit:false` family mid-chain. 2026-09-06 landed the matcher head lane (#3514), the disposable-lane gates (#3512), and closed the importer-cap fuse (#3513); it also showed the seam is now mostly **operator-visible**: stage rollups disagreeing with durable run rows, and recovery verbs refusing on branches with undone work. The late 2026-09-06 session closed both dependent lanes (#3535, #3538) and the external chain (#3534, #3543), and shifted the frontier again: the remaining defects are no longer mostly *settlement* but **publication** — five distinct ways a lane that finished its work still could not publish, every one hand-salvaged. The keystone is that `iteration_timeout` has never been resumable for an implement lane, so none of those five could recover themselves. 2026-09-06 (operator session) closed two of that family — inventory [#3555](https://github.com/cbrenner04/jarvis/pull/3555) and socket reclaim [#3554](https://github.com/cbrenner04/jarvis/pull/3554) — and **corrected the keystone claim**: the inventory fix alone does not restore resume, because the full-suite AC is ticked last and consumes the iteration budget. It also established that the pipeline shape itself is sound (a single-lane `full-review` ran seed→ready end-to-end, both gates approved after review) and that **every remaining failure is in the implement stage**. The newest frontier is the verifier itself: it can hang forever on a mutant it generated, and when it does, no `run` verb can stop it. **2026-09-09 (hand-landed, 40 PRs):** the deadline pair ([#3648](https://github.com/cbrenner04/jarvis/pull/3648), [#3650](https://github.com/cbrenner04/jarvis/pull/3650)), gate-slot ownership ([#3647](https://github.com/cbrenner04/jarvis/pull/3647)), the notification cursor ([#3652](https://github.com/cbrenner04/jarvis/pull/3652)), `daemon stop` liveness ([#3655](https://github.com/cbrenner04/jarvis/pull/3655)), the dead-directive sweep ([#3654](https://github.com/cbrenner04/jarvis/pull/3654)), and an issue burn-down (#3372 precedence, #3029 mechanism 1, #3030, #3381 closed) all landed by hand; every one skipped the harness. Also learned: `jarvis cleanup` cannot archive a spec no run ever touched, so hand-landed specs need an archive PR ([[cleanup-archives-hand-landed-specs]]). **2026-09-09 (pipeline dogfood, 12 PRs):** four `full-review` pipelines driven seed→ready located the implement-stage failure precisely — [[chained-implement-cannot-tick-its-own-criteria]], a split between the spec tree the agent writes (implement worktree) and the one the criteria-ticked contract reads (`specReadRoot`, the plan worktree). Three lanes failed three different ways from that one cause; a standalone implement of the *same spec* completed end-to-end unattended twice ([#3692](https://github.com/cbrenner04/jarvis/pull/3692), [#3693](https://github.com/cbrenner04/jarvis/pull/3693)), which isolates the defect to the chained stage rather than to `implement`, the agents, or concurrency. Plan lanes cleared the contract gate **4/4 unaided** — the first session with no hand-collapsed plan. The session's second block then drove all six sibling ready-intents to `main`: **six standalone implements, five completing end-to-end unattended** (criteria ticked, gate green, draft→ready flipped, PR published, zero operator steps) — the same preset that failed 3/3 as a chained pipeline stage that morning, on the same specs and agent order. Also closed the verifier-orphan mode empirically ([#3691](https://github.com/cbrenner04/jarvis/pull/3691) + [#3706](https://github.com/cbrenner04/jarvis/pull/3706): 113/113, zero orphans, after two 20GB orphans had driven the machine into OS low-memory reaping), and diff review caught two defects the gates passed — a dead tie-break whose acceptance criterion claimed coverage of an unreachable branch, and a verb path that resolves a pipeline-id prefix and then sends the raw argument ([[pipeline-verb-path-discards-its-own-resolution]]). 35 PRs merged.

| Defect class (pre-reset taxonomy) | Fixes | Status |
| --- | --- | --- |
| Runner/write-loop settlement + resume honesty | 30 | atomic-store + daemon/write-loop consumers landed; workflow-runner 01 landed, 02 deferred |
| Mutation/plan-contract verifier | 26 | gate chain complete; scanner-based; escape hatch live |
| Cleanup/worktree management | 27 | lossless git-status landed; [[cleanup-improvements]] carries the rest |
| Pipeline stage settlement/lifecycle | 24 | **the open front** — [[pipeline-settlement-derives-from-run-rows]] |
| Daemon restart/reconciliation | 23 | absorbed into the settlement seed (#2996/#3030 shapes) |
| Error attribution ("the row lied") | 18 | honesty invariant applied at daemon status (#3473), incidents (#3418); linked-row projection remains |
| Ready-gate repair scope | 14 | autofix scoped #3343; fence policy decided 2026-09-05 — chain queued |
| Dispatch parity CLI vs daemon | 11 | front door complete; the class's new instance is the linked-row matcher split |

## Priority-ordered work

| P | Item | Why now |
| --- | --- | --- |
| **P0 — settlement seam** | [[pipeline-settlement-derives-from-run-rows]]: canonical state landed 2026-09-06; **daemon-terminal-run-stage-settlement hand-implementation in flight 2026-09-10** — branch `hand/terminal-run-stage-settlement` (WIP commit pushed): shared settlement algorithm `settleLinkedStagesFromEntryRunWith` (`pipeline-stage-settlement.ts`, the SQLite store delegates), daemon owner `stage-settlement-owner.ts` (`settleStagesForEntryRun` / `settleOrphanedRunningStages`), wired into the workflow promise `finally`, dispatch/adopt post-wait, `pipeline_resume` precondition, and the start sweep (`recoverContinuablePipelines` settles then continues; startup order now reconcile → recover runs → continue); `settlement_deferred` marker, both redrive predicates, `resumeDrivesDeferredSettlement`, and the wedged-stage incident are deleted. **Not done:** typecheck of the last edit (`ctx.logReader` in the admission `finally`), adapting ~25 tests (`pipeline-stage-dispatch.test` predicate/marker describes, `pipeline-execution.test` restart-sweep + resume-deferred cases, `operator-incidents.test` wedged case, `state-store.test` transitions), the two AC tests in `daemon-pipeline-start.test.ts`, docs (`daemon-host.md` 23/57/647-649, `pipeline-execution.md` 52-60/149/164, runbook 311, v1-behaviors) | The last unfinished retirement, and every 09-06 stage wedge was an instance: a stage rollup reading `in-progress` behind a durably `failed` entry run, and `branch_not_resumable` on a branch with plainly undone work |
| **P0 — CLOSED 2026-09-08: planning corrupted its own drafts** | [[boundary-split-emits-near-duplicate-subspecs]] — **shipped** [#3628](https://github.com/cbrenner04/jarvis/pull/3628) | `normalizePlanDraftSpecDir` re-split every authored plan draft by a hardcoded jarvis surface taxonomy, copying the parent body into each child — identical `## Problem` text, bare surface titles, zero-criteria children, index/title drift, all by construction. It taxed **all three** plan lanes on 2026-09-08 (each hand-collapsed) and, because the taxonomy ran for every registered project on ordinary vocabulary (`flags?`, `database`, `storage`, `persist*`, `socket`), it was already half-firing on `chess-mvp-yolo`, a SwiftUI app. Retired along with the taxonomy; the one-artifact-per-bullet rule survives via the vocabulary-free `referencedArtifactPaths`. Review of the fix caught two regressions inside it — an over-matching path regex and a silently dropped missing-criteria check that would have let a zero-criteria subspec read as vacuously complete |
| **P0 — fan-out is single-lane** | [[fan-out-plan-resolution-is-all-or-nothing-across-lanes]]; [[skipped-successor-strands-a-recovered-lane]] | **Rescoped 2026-09-07: a consumption race, not a fan-out ceiling.** Verification fails only on a sibling ready-intent already consumed into a spec tree, so approving *every* gate before any sibling plan lands dispatches all lanes in parallel — verified on two pipelines, four concurrent plan stages. The serial "head lane first, land, then resume" practice is what manufactures the failure. Fan-out plan stages already parallelize; the fix restores that rather than building it, and its regression test must consume a sibling first or it passes vacuously |
| **P0 — a lane that finishes cannot publish** | [[coscheduled-test-pair-strands-runs-terminally]]. **Shipped 2026-09-08/09:** gate invocations account against the iteration budget [#3617](https://github.com/cbrenner04/jarvis/pull/3617) and the slot is owned [#3647](https://github.com/cbrenner04/jarvis/pull/3647). **Shipped 2026-09-06:** inventory [#3555](https://github.com/cbrenner04/jarvis/pull/3555), socket reclaim [#3554](https://github.com/cbrenner04/jarvis/pull/3554) | **Correction:** the inventory fix was *not* sufficient. Each subspec's last AC runs the full suite inside the write step's own iteration budget, nothing staggers that across lanes, and because that AC is ticked last *and* consumes the budget, a lane killed mid-suite has zero fully-complete subspecs — so `hasCompletedSubspec` is false and the timeout stays non-resumable regardless. Two lanes died at exactly 45m01s/45m02s with only `test:v2` outstanding. The gate-invocation seed is now the keystone |
| **P0 — CLOSED 2026-09-09: verifier orphans outlive their owner** | [[verifier-group-sigkill-escalation-dies-with-its-parent]] — shipped [#3691](https://github.com/cbrenner04/jarvis/pull/3691) | The SIGKILL escalation sat on an `unref`'d timer, so an owner exiting inside the 50 ms grace cancelled the only signal that would reach a SIGTERM-resistant child. [#3578](https://github.com/cbrenner04/jarvis/pull/3578) bounded the verifier's *waiting*, which is why orphans kept appearing after it shipped. Now the timer is referenced and settlement awaits an `ESRCH` probe. Residual risk on the PR: that probe is unbounded by spec decision, trading a leak for a hang if SIGKILL ever fails |
| **P0 — CLOSED 2026-09-10: `run kill` reports settlement** | [[make-run-kill-rpc-report-settlement]] + [[render-run-kill-settlement-outcomes]] shipped [#3725](https://github.com/cbrenner04/jarvis/pull/3725) on [[record-verifier-spawn-process-groups]] + [[reap-verifier-process-groups]] [#3724](https://github.com/cbrenner04/jarvis/pull/3724): every finalization spawn records a pgid, kill signals them before a bounded (30 s) durable-settlement wait and returns `RunKillOutcome`; the CLI fails closed on the old `{ ok: true }`. Previously: **The hang itself shipped 2026-09-07** — [#3578](https://github.com/cbrenner04/jarvis/pull/3578) landed `MAX_KILLING_TEST_MS = 30_000` with detached process-group termination, `non-terminating-mutation` classification, and restore-in-`finally`; verified on `main` at `diff-derived-mutation-verifier.ts:119,304`. Its spec went unarchived and its ready-intent unconsumed, so this row read open for a day | A `guard-flip: === → !==` on the exit condition of a `while (true)` loop does not terminate, and the verifier waits for it indefinitely: one core at 100% for 2h55m, the run stuck `live` with no agent past every watchdog, **both `run kill` and `run kill --force` printing `killed` while changing nothing**, and an inverted production guard left in the worktree because restore only runs after the test returns. The `*-anchors` scanners are all written in that loop shape, so every `=== -1` guard in a growing corpus is a non-terminating candidate |
| **P0 — the pipeline cannot finish what it starts** | [[chained-implement-cannot-tick-its-own-criteria]] ([#3688](https://github.com/cbrenner04/jarvis/pull/3688), amended [#3690](https://github.com/cbrenner04/jarvis/pull/3690)) | The chained implement stage reads its spec from the prior stage's worktree while the agent writes in the implement worktree, and the criteria-ticked completion contract reads the former. Three lanes on 2026-09-09 settled `missing_blocker`, `gate_invocation_refused` and `contract_miss` from this one cause; the third had the spec present and ticked **15/15** in the worktree the contract does not read. Merging the plan PR first is not a workaround. This is the concrete mechanism behind "every remaining failure is in the implement stage", and it is the last thing standing between a `full-review` pipeline and an unattended seed→ready run |
| **P0 — operator can't observe or recover** | [[pipeline-cli-discovers-daemons-like-run-list]]; [[pipeline-recover-resolves-past-approval-gates]] (**rewritten 2026-09-08**); [[skipped-successor-strands-a-recovered-lane]] | **Recover's premise changed.** The `stage_resolution_failed` refusal no longer reproduces — recover *admits* on a `full-review` plan stage behind an approval gate. The live defect is worse and was masked by it: recover **discards the operator's hand-correction**, restores the agent's draft, and validates that, reporting a violation naming a file deleted before invocation. So the documented correct-then-recover procedure is currently a hand-land. Otherwise unchanged: three verbs are dead in ordinary use: every `pipeline` verb and `cleanup --abandon` go blind after any merge rotates the digest (hit 4× in one session, and the CLI auto-starts a daemon that supersedes the one owning live lanes); `recover` can never run on `full-review`; a re-driven lane keeps a `skipped` successor no verb reopens |
| **P1** | External chain: spec **complete 2026-09-06** (#3534 + #3543, closes the resolution half of #3417/#3374) → [[all-spec-documents-external-capable]] flip → [[cleanup-external-spec-home-lifecycle]] | Re-repro was done and confirmed before driving. `gate-allowset-derivation-fails-on-external-spec-home` is the remaining external-path defect and shares #3417's root: `resolveSpecScopeRoot` succeeds on an external absolute `specPath`, so the graceful fallback is unreachable |
| **P1** | Structural-invariant chain: cli **complete** (#3541/#3547), execution-loop **complete 11/11 2026-09-06** ([#3556](https://github.com/cbrenner04/jarvis/pull/3556), archived), daemon **00-02 landed** ([#3557](https://github.com/cbrenner04/jarvis/pull/3557)) → 03-08 open → docs RI | Both remaining lanes needed hand-finishing, neither for the re-key itself: execution-loop stranded at its completion commit on lint autofix could not repair, daemon wedged 2h55m on the verifier hang. Subspec 03's work is stashed in that worktree, unverified. Re-dispatch 03-08 fresh. Also seeded [[self-parsing-structural-tests-can-bind-to-their-own-fixtures]] — a locator in this corpus silently bound to a fixture (4 anchors instead of 6) after autofix renamed its inventory constant |
| **P0 — CLOSED 2026-09-09: fixed deadlines strand correct work** | killing-test budget derives from the unmutated baseline [#3650](https://github.com/cbrenner04/jarvis/pull/3650); WAL handshake has no private deadline [#3648](https://github.com/cbrenner04/jarvis/pull/3648). Both hand-landed | Two independent fixed wall clocks terminated complete, correct runs. `MAX_KILLING_TEST_MS` is 30s while `workflow-runner-resume.test.ts` takes **32.06s** unmutated, so every candidate in that file — and in any file resolving it as a sibling, i.e. the whole `workflow-runner-` stem — reports as a non-terminating mutant; hit twice on 2026-09-08, both hand-finished, `resume` a fixed point each time. Separately `waitForLockHeld` gives a spawned subprocess **10s** to boot Bun and take a lock while `test:v2` runs 10-wide; it failed five times in one session and the fifth stranded a run as `ready_gate_out_of_scope` — `nextAction: stop`, no resume. Both convert machine load into terminal failure of correct work |
| **P1 — CLOSED 2026-09-09: wake path is unusable** | notification delivery cursor is exclusive [#3652](https://github.com/cbrenner04/jarvis/pull/3652) | `notifications wait --since <deliveryCursor>` re-delivered the same incident forever, so the documented primary wake path for backgrounded work is a fixed point; drives operators back onto the polling the runbook forbids |
| **P1 — CLOSED 2026-09-09: cheap, growing** | dead `@mutate` directives swept [#3654](https://github.com/cbrenner04/jarvis/pull/3654); the guard treats them as inert | Grew with every implement that copied directives forward |
| **P2** | Fence chain: [[ready-gate-repair-out-of-diff-edits]] → [[remove-ready-gate-repair-fence-bypass-from-production]] | Policy decided 2026-09-05. Note the fence is **working today**: it caught a stale-based lane trying to "repair" across #3499's files (2026-09-06) |
| **P1 — CLOSED: guards that destroy** | PR-ownership probe fails closed (spec `cleanup-pr-ownership-probe-fails-closed`, 2026-09-07); `daemon stop` refusal checks run liveness [#3655](https://github.com/cbrenner04/jarvis/pull/3655) (2026-09-09) | A failed `gh` probe reads as "no open PRs" and now gates worktree destruction — reachable *by default*, since `gh` false-negatives under the sandbox every agent session runs in. It already silently disables `--abandon`'s ready-PR guard. Same inconclusive-is-permissive shape as the socket outage |
| **P1 — the gate rejects what is well-formed** | [[plan-contract-classifies-the-rules-out-clause]]; [[index-link-check-rejects-annotated-subspec-lines]] (the directive sweep half closed [#3654](https://github.com/cbrenner04/jarvis/pull/3654)) | **One class, not three regexes (2026-09-07).** Plan-contract checks are strict lexical patterns over authored prose that fail *closed* on well-formed variation, and each fires late — at publication or contract-miss — so every false positive costs a complete, reviewed draft. Three instances: the mandated `rules out` clause read as a second surface (two hand-landings); #3383's bare-word over-match in the same classifier; and `INDEX_LINK_PATTERN` anchoring `$` at the link paren, so a `(after 00 and 03)` annotation makes a linked subspec read as absent (three failures, hand-landed [#3584](https://github.com/cbrenner04/jarvis/pull/3584)). Each was fixed as a point fix, which is why there is a third. The shared fix is tolerant extraction plus a message distinguishing *absent* from *present but unparseable*. Separately, two false `@mutate` pins shipped inside ticked ACs — an annotation that does not kill is worse than none |
| **P2** | [[implement-admission-persists-its-run-row]]; [[per-project-config-overrides-seam]]; [[superseded-daemon-releases-run-ownership]]; [[intent-split-covers-sibling-repo-surfaces]]; [[implement-respects-target-repo-doc-layout]]; [[detached-pipeline-plan-stage-consumes-ready-intents]] (plan-stage half; the `provenIntentPrune` slug half shipped [#3657](https://github.com/cbrenner04/jarvis/pull/3657)); [[quota-classification-covers-every-step-role]] (stranded-`paused` incident half; precedence shipped [#3669](https://github.com/cbrenner04/jarvis/pull/3669)). **Shipped 2026-09-09:** `--base` fails closed on a stale local main [#3673](https://github.com/cbrenner04/jarvis/pull/3673), intent resume consumes its seed [#3665](https://github.com/cbrenner04/jarvis/pull/3665) | The dogfood frontier |
| **P2** | [[cleanup-improvements]] (7 defects); [[cli-retire-run-start-pause-and-config]]. **Shipped 2026-09-10 by hand:** dead-export gate + sweep [#3718](https://github.com/cbrenner04/jarvis/pull/3718) (221 demotions; the gate now catches new orphans in `bun run check`), test-support glob [#3717](https://github.com/cbrenner04/jarvis/pull/3717) (also found `scripts/*.test.ts` ran in no slice — now in `test:shared`), dedupe trio [#3726](https://github.com/cbrenner04/jarvis/pull/3726), prompt-id chain [#3719](https://github.com/cbrenner04/jarvis/pull/3719) + fragment policy / single assembler [#3727](https://github.com/cbrenner04/jarvis/pull/3727), verifier registry surface [#3721](https://github.com/cbrenner04/jarvis/pull/3721), daemon resume config path [#3722](https://github.com/cbrenner04/jarvis/pull/3722), never-landed fails closed [#3720](https://github.com/cbrenner04/jarvis/pull/3720), cleanup external queues [#3728](https://github.com/cbrenner04/jarvis/pull/3728), archive publication on an isolated cleanup branch [#3729](https://github.com/cbrenner04/jarvis/pull/3729) (open at handoff, CI re-run after a test fix) | Slot between structural landings |
| **P3** | [[mechanical-cruft-pass]]; [[superseded-pipeline-pr-hygiene]]; display/TUI seeds. Resume twins merged [#3723](https://github.com/cbrenner04/jarvis/pull/3723) | Parked until the settlement seam settles |

## Operating notes

**2026-09-10 hand burn-down.** 15 ready-intents landed by hand in one session (PRs #3715–#3729); the four left are the settlement seam (in flight, above) and the operator-failure chain ([[settle-workflows-with-falsifiable-failures]] → [[serve-canonical-failures-from-daemon]] → [[render-operator-failures-consistently]]; the shared record exists in `shared/operator-failure-record.ts`, only the store consumes it). Lessons: (1) Bun mis-transpiles a local identifier named `declare` — calls silently no-op; (2) `state-store-wal-concurrency.test.ts` is a load flake (passes idle) and was the only red in three otherwise-green slices; (3) the dead-export gate immediately caught my own new modules' in-file-only exports, so demote as you go; (4) the reap intent's `daemon.ts:633-635` shrink literals and "gate-only sweep" claims were already stale — verify an intent's Problem paragraph against `main` before implementing it; (5) cleanup archive publication changed a contract ten e2e tests pinned in-checkout, and `gh` fixtures had to treat `cleanup/archive-*` as unmerged, as production `gh` would.

**Circuit-breaker (per-lane, per-gate).** If a lane (plan / implement / review) needs hand-intervention twice in a row on the same gate, stop routing that lane through Jarvis until the gate fix merges — hand-land the work instead. Re-open the lane only after one clean end-to-end run on the fixed gate. Mirrored in [operator-runbook.md § Circuit-breaker](../docs/operator-runbook.md#circuit-breaker-stop-routing-a-lane-that-keeps-failing-the-same-gate).

**Parallelization (measured 2026-09-06, refined again by the operator session).** The ceiling is **concurrent full-suite gate invocations**, not lane count and not load. Four concurrent implements ran at load 6-11 with zero watchdog false-kills and a saturation alarm at 18-22 that never fired in two hours — yet two lanes died at exactly **45m01s and 45m02s** with real work committed and only `bun run test:v2` unticked. Each subspec's final AC runs the whole suite inside the write step's iteration budget and nothing staggers that across lanes. Load average does not predict it; attribute by what each lane is doing. Hold at two concurrent implements until [[implement-gate-invocation-outlives-the-iteration-ceiling]] ships. Earlier measurement, still valid: Lane count is not the ceiling — seven concurrent lanes ran at load 6-21 with zero watchdog false-kills, and plans fan out freely (3 concurrent, 2 of 3 clean end-to-end unattended). The ceiling is **verification-phase overlap**: three implements reaching diff-derived verification together drove load to 48.8 and timed out all three at exactly 45.1 min, because each verifier spawns up to `MAX_CONCURRENT_VERIFIER_TEST_RUNS` (4) `bun test` subprocesses. Nothing staggers them; hold at two concurrent implements. A `shared/**` implement's gate still runs all six slices and cannot share the machine. And `ready_gate_out_of_scope` is worse than "manufactured by load" — `main` itself fails `bun run test:v2` on an idle machine with a *rotating* set of 30 s timeouts, all green in isolation ([[coscheduled-test-pair-strands-runs-terminally]]), so the base-ref probe can be truthfully right and still strand healthy work. Attribute by process family before believing any ceiling; never run local suites beside a live gate. Approve fan-out gates **simultaneously**, before any sibling plan consumes its ready-intent — the serial "head lane first, land, then resume" practice is what triggers [[fan-out-plan-resolution-is-all-or-nothing-across-lanes]] (corrected 2026-09-07; [[plan-bases-off-a-declared-prerequisite-branch]] + [[pipeline-fan-out-lanes-serial-chained-bases]] remain the structural answer for coupled lanes on independent bases). **Do not merge while lanes are live** — every lane branched pre-merge is stale-based and its review/repair steps rewrite files the merge landed.

**A green mutation `pass` is not evidence.** Four distinct vacuous passes in one session: 0 accepted/13 skipped (fixes uncommitted, so candidate positions were stale), 0/0 against a stale local `main`, 0/0 on a file with 348 changed lines that had yielded candidates minutes earlier, and one genuine 1/0. `kind` alone cannot distinguish a verifier that killed everything from one that evaluated nothing — read `acceptedSites`. The gate is also structurally blind to **argument-swap** defects, which is exactly the bug class [#3555](https://github.com/cbrenner04/jarvis/pull/3555) fixed. Commit before running it: verification is diff-derived against the base ref.

**A dirty guard in a wedged worktree may be the verifier's mutant, not agent work.** Restore only runs after the killing test returns, so a hung candidate leaves an inverted production guard on disk that reads exactly like agent-authored corruption. Check parentage of any hot `bun test` (`ps -o ppid=`) before concluding anything: daemon-parented is a live gate/verifier child, `launchd`-parented is a leaked orphan, and the recoveries differ.

**Land-a-slice.** A multi-subspec spec converges only with immediate re-dispatch after each merged slice, `cleanup --yes --abandon` between. Treating a merged slice as the finish line strands doc subspecs permanently.

**Guards that destroy.** A guard deciding whether to destroy or land something treats every inconclusive answer as "do not act" — `ENOENT` from a sandboxed caller is inconclusive, not authoritative (the socket outage), and a test that asserts a recorded field is not a test that the behavior happens (#3483's consumption bug).

**A `@mutate` annotation that does not kill is worse than none.** Two shipped inside ticked acceptance criteria on 2026-09-06 and both passed every mechanical gate: one where the mutant collapsed a result to `[]` while every assertion over it was `.not.toEqual` or emptiness-tolerant, and one where the annotated line was reachable only through a branch no fixture exercised. Verify a pin by deleting the line it names, not by reading it.

**Planning was corrupting its own drafts, and it reached other projects (2026-09-08).** Three consecutive plan lanes needed hand-collapsing before the cause was found: `normalizePlanDraftSpecDir` re-split every authored draft by a hardcoded jarvis surface taxonomy, copying the parent body into each child. The agents' content was sound every time. Retired in [#3628](https://github.com/cbrenner04/jarvis/pull/3628). Two lessons survive it. First, **a mechanical post-hoc rewrite of agent output is a defect surface** — the split was redundant with the draft prompt's one-surface-per-bullet rule and with the review roles, which already split oversized subspecs correctly, and it was the only one of the three that damaged anything. Second, **jarvis-shaped machinery applied to every registered project leaks**: the taxonomy's patterns are ordinary software vocabulary and were already half-firing on a SwiftUI app. Check any shared-path feature for that shape before assuming it is jarvis-only. Same class as #3426.

**Plans invent precision.** A criterion naming a test that does not exist passes review and blocks ticking later; verify named tests exist at plan review, and prefer discovery scripts over hand manifests (the #3442 narrowing).
