- `resume` terminal-status coverage must be real. The test named for terminal-status rejection currently exercises a settled in-memory run whose durable status is still resumable, so it does not verify `createRunControlHandlers`’s `terminal_run` branch. The outcome must be that terminal durable statuses are explicitly covered and the IPC assertion matches the real handler response. This is required by the subspec’s “real handler path” decision and the acceptance criterion that migrated assertions match shipped handler semantics.

- The single in-flight guard must be covered through `resume`, not rechecked through `start`. The current test name claims `resume` rejects while another run is active, but the RPC sent is `start`, so the `resume` contract is untested. The outcome must be a test that actually invokes `resume` in the guarded state and asserts the real rejection. This is necessary because the subspec is about preserving IPC coverage on the real handlers, not keeping stale test names over different behavior.

- Teardown must prove the abort path, not only explicit settlement. The completed subspec requires fixture-controlled abort/settle behavior and teardown that aborts in-flight fake executor work without unhandled rejections before server/store shutdown. The current fixture exposes settlement and signal observation, but teardown never aborts pending work, so the accepted teardown contract is not met. The outcome must be that in-flight background work can be aborted during teardown and drained cleanly, matching the written decisions, tasks, and acceptance criteria.
