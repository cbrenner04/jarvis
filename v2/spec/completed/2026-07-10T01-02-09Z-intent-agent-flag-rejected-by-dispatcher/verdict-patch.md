No required outcomes. Both adversary findings resolve in the advocate's favor: the `agentFlag` duplication in `cli.ts` is explicitly mandated by the subspec's third bullet (the CLI-layer seam), is confirmed inert (never consumed — only `parsed.rest` reaches `intentCommand`), and is spec-compliant, not an implementation defect. The `cli.test.ts:766` replacement test's absence-check assertion matches the subspec's explicit instruction (assert `cap.err()` no longer contains the old error string, explicitly not asserting success) — this already supersedes the plan-review's more prescriptive suggestion. All three acceptance criteria are met as specified. No changes needed.