# Operator session — Phase 8 (PR lifecycle + attribution) + F3 telemetry

**Date:** 2026-07-10/11 UTC · **Operator:** Claude Code (Opus 4.8, 1M) · **Scope:** land v2 Phase 8 and telemetry slice F3, per `v2/spec/v2-meta-index.md` + `.scratch/v2-ready-intents-prioritization.md`.

## What shipped

Phase 8 delivers the v2 runner's output side (previously the runner stopped at worktree creation — no commit/push/PR). Driven seed → intent → plan → run, one PR per behavior.

| PR | Spec | What landed |
| --- | --- | --- |
| #1277 | commit-completed-run-worktree | Harness commit at completion boundary: isolated-index snapshot (`git add -A` → `write-tree`), `Jarvis-Agent` trailer + `Spec:` selector, clean-tree no-op, crash-recoverable pending record + CAS `update-ref`, exposes `commitSha`. New `completion_commit_failed` retryable outcome. |
| #1282 | publish-completed-run-draft-pr | Branch push (first `-u`, later plain) + idempotent draft PR (reuse-by-baseRef); new v2 transient-retry seam (3 attempts, 1000ms flat, non-ff = permanent) + `gh auth status` readiness probe. |
| #1292 | finalize-pr-attribution-and-ready | Attribution footer (v1 renderer ported to `v2/src`, reconciled to v2's single meta-commit collapse) + PR-body refresh w/ narrative-marker preservation + ready gate + guarded draft→ready flip (`already ready`/`not a draft` short-circuit before retry). New `ready_finalize_failed` outcome. |
| #1287 | record-work-boundary-telemetry (F3) | `work_boundary_recorded` telemetry at the completion boundary: extended `CompletionCommitResult` with harness-computed `filesChanged` (name-only tree diff, renames off) + emit `{run_id, attempt_id, outcome_kind, run_status, commit_sha, files_changed}` via a v2-local JSONL sink (no `shared/**` leak), attempt-grain (no `invocation_id`), gated on attached telemetry block, best-effort/append-failure isolated. |

**Meta-index:** Phase 8 ticked (#1293). **F3** prioritization slice complete.

Supporting merges: seed PR #1273; ready-intent PRs #1274/#1275; F3 intent revision #1280 (unblock, see below); docs/gotchas #1293. New v2 modules: `completion-commit.ts`, `completion-publisher.ts`, `pr-attribution.ts`, `pr-body-refresh.ts`, `ready-finalize.ts`, `work-boundary-telemetry.ts`.

## Cost breakdown (Jarvis spend)

Source: `~/.jarvis/runs.jsonl`. Token columns are mode-level (plan phase vs patch phase).

| Spec | plan $ | run $ | total $ | agents |
| --- | --- | --- | --- | --- |
| commit-completed-run-worktree (b1) | 1.86 | 2.07 | 3.93 | codex |
| publish-completed-run-draft-pr (b2) | 5.49 | 5.20 | 10.69 | codex→claude |
| record-work-boundary-telemetry (F3) | 5.02 | 4.20 | 9.22 | claude, cursor (+failed cursor auth attempt) |
| finalize-pr-attribution-and-ready (b3) | 5.89 | 0.05 | 5.94 | claude (plan), cursor (run) |
| **Total** | **18.26** | **11.53** | **29.79** | |

`tokens_in=842,459  tokens_out=301,150` across plan+run. Cursor runs (b3, F3 re-run) were ~free — the mid-session switch to cursor-first (below) drove run cost down (b3 run $0.05 vs codex/claude runs $2–5).

**Operator (own) session cost:** **$27.46** (Claude Code `/cost`; claude-opus-4-8[1m] $27.46 + haiku sliver $0.0016). API 38m56s, wall 3h47m; 44.4k in / 157.2k out, 33.0M cache-read / 791.7k cache-write; 252 lines added / 5 removed (operator-authored: seeds, F3 intent revision, docs, report).

**Session total: ~$57.25** (jarvis $29.79 + operator $27.46).

## Workflow / tooling observations

1. **F3 prerequisite blocked at plan; fixed by revising the intent, not the code.** F3's seed decision "source facts from the harness commit result only" was too strict — b1 exposes `commitSha` but not a changed-file count, so plan raised a `## Blocker`. Resolved by revising the ready-intent (#1280) to bring count-exposure into F3's scope (harness computes `filesChanged` from the commit's trees), then re-planning fresh. Clean example of the prerequisite gate doing its job. *Not a harness gap — an authoring precision miss on my seed.*
2. **`agentOrder` config gotchas** (recorded in the runbook, #1293): (a) duplicate agent in one order is rejected by config validation (`claude:opus → claude:haiku` illegal); (b) an unauthenticated primary that errors `model_config` is **terminal — no cascade** (cursor without login hard-failed a run in 4s); (c) codex also lives in `subRoleAgentOrder.reviewPanel/reviewActuator`, so dropping it from the main orders isn't enough.
3. **Concurrent operator session detected at cleanup.** Worktrees I didn't create (`v2-install-and-config`, `v2-first-workflow-walkthrough`, `onboarding-overview-and-coexistence`, `the-last-bits-of-v2`). Avoided the blanket `jarvis1 cleanup` (would remove their worktrees); scoped cleanup to my four specs + manual teardown of only my own worktrees/branches.
4. **Cursor is a strong free actuator once authenticated** — solo-completed the two most complex Phase 8 specs (b3 attribution+ready, F3 telemetry) at ~$0. Plans still led with claude-opus for spec quality.

## Config note

Per operator request, `~/.jarvis/config.json` agent orders were changed this session: **codex removed** everywhere; **cursor-first** for patch runs, **claude-opus-first** for plan/review. Backup at `~/.jarvis/config.json.bak-*`. Left in place ("for now"); restore codex when its quota returns.

## Open / not mine

- PR #1272 (`resume-continues-from-checkpointed-partial-progress`) — completed-spec impl from a prior session, left open for its owner.
- Concurrent session's in-flight v2-onboarding worktrees — left untouched.
