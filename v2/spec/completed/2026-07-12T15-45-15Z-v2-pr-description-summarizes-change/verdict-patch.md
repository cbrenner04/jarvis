**Verdict — one required fix:**

`listLandedIntentFiles` (`v2/src/execution/intent-output.ts:94`) must not fall back to listing every `.md` file in the durable `ready-intents/` directory when the invocation's ownership entry is missing or empty. That fallback silently substitutes "every historical intent ever landed in this project" for "what this run authored," which directly violates spec `01`'s decision that the summary is one line per file this run authored, and undermines the retry/resume guarantee the derivation is supposed to provide (ownership should be the durable, re-derivable source of truth per `01`'s decisions). When ownership can't be resolved for the invocation, the function must fail closed — e.g., return an empty file list (subject-line-only summary) or otherwise avoid emitting unrelated filenames — rather than fail open onto an unscoped directory listing. This is new code introduced by this spec, not a pre-existing gap, so it must be fixed before merge.

The second flagged concern (the `plan.prompt.draft` promptId trigger not covering a hypothetical `write-write` preset) is not upheld: `write-write` is absent from `WORKFLOW_PRESET_BUILDERS` and unreachable from any CLI entry point, so it isn't a live gap against this spec's acceptance criteria.