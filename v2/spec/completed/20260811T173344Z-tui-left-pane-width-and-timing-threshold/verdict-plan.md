1. Split the oversized subspec into independently testable geometry and timing-format subspecs, ordered and linked from `index.md`. Assign every existing task and acceptance outcome exactly once; retain an integrated ≤200-column regression proving the coupled result.

2. Prevent an unintended stage-row behavior change. The 80-column boundary should govern pipeline/branch timing only unless stage wording is explicitly added to scope with dedicated boundary tests and documentation. The intent excludes unrelated row-content changes.

3. Prove the labeled timing survives real row composition at the representative terminal geometry. Use realistic pipeline and branch content and verify the painted row shows the labeled atom when it fits; arbitrary-content visibility or degradation-priority changes are not required.

4. Justify the selected 80-column floor and 45–50% clamp against both panes’ readability. Pin resulting left and right widths at ordinary and wide terminals so the right pane is not reduced without an explicit product constraint.

5. Tighten preservation coverage. Cite existing tests that remain green for stacked layout, divider nudging, right-pane derivation, the four-row dock, section framing, stage-row behavior, and row-composition/degradation semantics. Limit integration expectation changes to values directly caused by the geometry and timing-boundary retune.
