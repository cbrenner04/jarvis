**Verdict:**

- Fix the unhandled-rejection bug in `withHumanDecisionAdmission`'s cleanup chain (`daemon.ts`, ~lines 706–716): the `.finally()` attached via `void next.finally(...)` produces its own derived promise that re-throws whenever `next` rejects, and that derived promise is never observed. If any step inside `work()` after the caught `checkWorktreeDirty` probe throws (e.g., a store call), this surfaces as a genuine unhandled rejection capable of crashing the daemon process — directly undermining this spec's goal of a responsive, resilient daemon. Bring this cleanup in line with the established pattern elsewhere in the file (e.g., ~lines 451–460) where `.catch(...)` fully resolves the chain before `.finally()` runs, so no rejection escapes unobserved.
- Add a regression test exercising the previously-uncovered path: a failure inside `work()` *after* the dirty-worktree probe succeeds (e.g., a store call throwing during admission), asserting no unhandled rejection/process crash occurs and that the admission map entry is still cleaned up afterward. This closes the gap where only the `checkWorktreeDirty`-throws path is currently tested.