No valid blocking issues remain. Both raised concerns are resolved or non-blocking:

- The walkthrough-link deferral is explicitly recorded in the spec's Decisions section (with a named trigger: "pin when that doc lands on `main`") and AC 5 is consistent with it — this is a tracked, deliberate scope decision, not a gap.
- The README paragraph-wrapping style point is cosmetic, not gated by any acceptance criterion (`lint:md` passing is AC 7's actual bar), and doesn't warrant a required fix.

No changes required to land this.