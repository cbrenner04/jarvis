## Verdict

**Not ready.** Resolve the following before plan (intent edits and/or first plan pass on a single ordered `index.md`):

1. **Prerequisite chain** — Make serial order explicit across all four: link `workflow-attached-waits-for-terminal` to completed `20260721T115738Z-workflow-command-reports-terminal-workflow-failure` (additive: attach duration + CLI outlives multi-row workflows, not rollup re-litigation). Add `workflow-print-run-id-at-admission` as prerequisite for `workflow-attached-run-boundary-progress` (stdout order: run ID → boundaries → JSON). Pin that detach (`workflow-detach-after-admission`) follows terminal-wait + admission stdout contract (attach vs detach semantics).

2. **`workflow-print-run-id-at-admission`** — Reframe problem/decisions for **contract hardening** against current `workflow.ts` (already prints run ID before wait). Scope ACs to ordering, presets, flush/pre-`start` silence if still in scope, and tests—not “first time we print ID.” Add `v1-behaviors.md` to doc updates or justify no change.

3. **`workflow-attached-run-boundary-progress`** — Pin **how** boundaries are observed (CLI-only, no new IPC) and what counts as a constituent run (vs shrink/hidden/debate rows). Define progress line format (ID vs step label). Add `v1-behaviors.md` if stdout contract changes. Add a **named failing-test AC** per spec guidance (not only `typecheck` / `test:v2` / `test:integration:v2`).

4. **`workflow-attached-waits-for-terminal`** and **`workflow-detach-after-admission`** — Add **named failing-test ACs** each; terminal-wait AC must prove the **CLI process** stays up until workflow terminal (multi-row fixture), not mocked single `wait`. Detach: replace or qualify “within prompt bounds” with a testable bound or `(Manual)`.

5. **All four runtime intents** — Each subspec needs at least one AC naming a regression that fails on pre-fix code and passes after implementation; keep generic test-suite ACs as supplementary only.

6. **Plan ownership** — One spec, four ordered subspecs (same seam); plan must pin detach default vs flag and unified `write-behavior` / runbook / `v1-behaviors` edits in one index.

**Rationale:** Same-seam serial split is sound and matches the seed, but prerequisites, print-intent alignment with main, boundary observation contract, failing-test ACs, and `v1-behaviors` coverage are load-bearing per spec guidance; leaving them implicit risks no-op subspecs, wrong ordering, or plan drafts that fail the failing-test and behavioral-AC rules.