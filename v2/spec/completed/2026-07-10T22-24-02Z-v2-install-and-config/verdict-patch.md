**Verdict:** Fix the recovery table's PID-file-directory row so the documented stderr symptom is `Error: PID file directory does not exist: <dir>` (with the `Error: ` prefix), matching what `formatLifecycleError` in `v2/src/cli.ts` actually emits for that plain `Error`. This is a factual-accuracy fix required by the spec's own "document only shipped behavior" decision and the AC that every stated symptom resolve to committed v2 behavior. No other changes needed — all other recovery rows, output-shape claims, and structural separation (config-load vs model-resolution vs daemon-start) are correct as implemented.