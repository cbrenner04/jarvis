Validating the advocate's claims against the codebase and spec guidance before issuing the verdict.
## Verdict: required refinements

1. **Narrow scope to the finishless guard; stop re-owning invert-hook work.** Declare an implement-order dependency on the execution-loop TUI invert-hook subspec (`05-tui-monitor-terminal-window-drop-production-invert-hooks`) merged first. Remove from this spec the duplicate acceptance criteria, tasks, and decisions that only restate hook removal, empty stub deletion, window-filter manual inversion, row-cap manual inversion, and the structural “no `setInvert*ForTest`” criterion — those outcomes already belong to sibling 05 (and static `guard-production-test-flags`). Retain stub cleanup only as implement-time fallout if empty invert stub tests are still present after 05, without duplicating 05’s AC surface.

2. **Refresh stale problem framing in `intent.md` and the subspec Problem.** Lead with the live defect: `terminalRunInLiveWindow` returns `false` when `finishedAtMs` is `undefined`, which drops finishless terminal rows from the live window. Treat invert hooks as historical context or a brief negative contract (“must not reintroduce”), not as the primary failure mode — hooks are already absent in production code.

3. **Mirror prerequisite gating in the subspec.** Add a `## Prerequisites` section to the subspec matching `index.md` (store terminal reconciliation and list `finishedAtMs` honesty merged before implement), so an implement run driven from the subspec alone sees the same merge gate.

4. **Name concrete pinning tests in behavioral ACs.** The failing-test AC must cite specific new test title(s) (not “coverage for…”) that fail on pre-fix `return false` for `undefined` and pass after the guard change. The finishless guard-inversion AC must cite the same pinning test by name and require a `Mutation checkpoint:` comment on that test naming the source mutation (`return true` → `return false` on the `finishedAtMs === undefined` branch), per `v2/docs/test-writing.md` — not a checkpoint on the source guard. Reconcile tasks/decisions that currently contradict this (they ask for a source-guard checkpoint and for window/cap checkpoints on pinning tests owned by 05).

5. **Require a sparse `runTuiEntry` fixture so render proof is not vacuous.** The acceptance outcome for TUI rendering must state that the fixture keeps in-window terminal count below the twenty-row cap (or otherwise guarantees a finishless row would be visible once eligible). Without this, a test built like `buildFixtureRuns()` (twenty-two timestamped terminals) can stay green even when finishless rows remain effectively invisible under the unchanged cap/sort policy.

6. **Record cap/sort policy for finishless rows in Decisions.** Clarify that finishless rows are in-window and subject to existing finish-order sort (`?? 0`) and the twenty-row cap — no cap exemption or priority unless a separate product decision is made. This bounds the operator goal (“not dropped outright”) without expanding scope.

7. **Keep preservation ACs as test citations only.** The two “stays green” criteria citing `renders in-window terminal rows…` and `retains non-terminal rows…` are correct refactor-style ACs; leave them, do not paraphrase their behavior.

**Rationale:** The intent’s unique deliverable is finishless rows surviving the live-window filter; sibling 05 already owns invert-hook removal and window/cap guard pinning. Duplicating that work creates same-seam sequencing risk (spec guidance: plan same-seam siblings serially), vacuous or already-satisfied ACs, and checkpoint drift from `test-writing.md`. Sparse-fixture and named-test ACs are required so the behavioral change is actually proven and guard-inversion evidence is agent-verifiable.