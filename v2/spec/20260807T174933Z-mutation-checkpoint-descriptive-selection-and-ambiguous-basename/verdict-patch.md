Validating branch scope and subspec 01 alignment against the spec before issuing the verdict.
## Verdict: required refinements

1. **Remove out-of-scope review staged-Markdown lint work from this branch.** The diff includes `reviewed-staged-markdown-lint.ts`, `workflow-runner.ts` landing/reprompt/`landing_failed` behavior, `workflow-runner.test.ts` lint regressions, and `staged-markdown-lint.ts` verdict-file exclusions. None of that is tasked or AC-covered in subspec 01 (`01-ambiguous-basename-disambiguation.md`). It belongs to `v2/spec/20260807T160744Z-plan-review-actuator-staged-markdown-lint/`, which is still open. This branch must land only ambiguous-basename disambiguation and its documentation updates; unrelated review-landing lint work must be reverted here or moved to its own branch/PR.

2. **Bring the integration regression in line with its ticked acceptance criterion.** Subspec 01 AC requires `write.test.ts` — `bare basename disambiguates via completion changed paths` to prove execution-vs-commands `write.test.ts` collision is resolved through the real implement completion path and **reaches mutation checkpoint `caught`**. The current test asserts only `result.result.kind === "complete"`. Completion alone does not demonstrate that changed-path disambiguation picked the execution-side pinning file and exercised the guard checkpoint caught path (as opposed to failing open, resolving ambiguously, or succeeding on an unrelated pin). The test must assert the caught outcome the AC describes.

**Rationale:** Scope contamination blocks clean review and merge of subspec 01 — it couples an unrelated feature with its own spec, tests, and operator semantics. The integration-test gap leaves a ticked AC without the regression proof it claims; subspec 01’s core verifier/write-loop behavior otherwise matches its tasks (changed-path threading, `ambiguous_pinning_basename` contract, write-loop parity via shared `blockingUnparseableEntries`).

**Not required for this pass:** exact parent-directory equality vs aspirational “nested edits” decision wording (implementation matches tasked/AC semantics); git-diff failure degrading to ambiguous refusal; full `candidates` enumeration; extension-tolerance collision coverage; subspec 00 index/blocker housekeeping (shape selection already present on merge-base; separate harness issue).