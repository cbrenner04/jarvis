# One work tree for pipelines and ad-hoc runs

Authoritative for implement runs: acceptance criteria, documentation updates, and material decisions in this file supersede `intent.md` where they differ.

## Problem

Ad-hoc `run workflow …` launches render in a second-class "Unattributed" segment below the pipeline tree (`v2/src/tui/tui-monitor-lines.ts`). The segment reintroduces the bug class the tree already fixed with full-flatten + scroll viewport: `monitorSelectableNodeIds` appends the *post-eviction* `unattributedRows`, so expanding a pipeline shrinks the segment budget and silently deletes ad-hoc runs from navigation; and `retainUnattributedSegmentFifo` treats finishless terminals as unevictable must-keeps that crowd out everything else at small budgets. `leftPaneUnattributedBodyRowBudget` computes that budget from painted tree rows, coupling two independent surfaces.

## Decision ledger

- Ad-hoc work items are top-level nodes in the same flatten as pipelines; `buildMonitorPipelineTree` returns `displayNodes` only. Rules out keeping a second row list that the pane concatenates.
- An ad-hoc work item is one workflow-invocation group (its `WorkflowTableRow`, grouping unchanged); a run with no invocation degenerates to a `standalone` row. Rules out one node per constituent run.
- New display-node kind `adhoc` — `{ kind: "adhoc"; id: <representative run id>; depth: 0; tableRow: WorkflowTableRow }`. Rules out reusing `kind: "run"` at depth 0: consumers must tell a stage's run leaf from a top-level ad-hoc row (right-pane pipeline context, `run_leaf` vs `unattributed`), and depth arithmetic breaks once `tui-intent-branch-subtree` adds a tree level.
- The adhoc node's `id` is `workflowGroupRepresentative`'s run id (active member while any member is active, else the entry run) — the same identity today's unattributed segment already exposes via `monitorTreeRun`. Promoting it to the top-level node id and the scroll-follow key means a selection can drop when a group's last active member finishes; this is an existing risk, not a new one, and stays accepted rather than fixed here.
- Ad-hoc `TopLevelOrderKey` — this is the caller the shipped ordering spec deferred to (`20260807T212915Z-tui-work-tree-top-level-ordering/00-three-bucket-top-level-ordering.md`): `rank` is `running` when any member is non-terminal (`workflowGroupHasActiveMember`) else `terminal`; `createdAt` is the earliest member `createdAt`; `finishedAtMs` is `workflowRollupFinishedAtMs(members) ?? null`. Rules out the representative run's own timestamps — a collapsed group's finish is its last member's finish.
- `workflowGroupHasActiveMember` (group rollup over `isTerminalRunStatus`) is the deliberate rank predicate — not `isActiveRunStatus`, which the intent cites but which names the adjacent per-run representative-selection predicate, not the group-level rank predicate.
- Fixture invariant for the rank mutation checkpoint: the terminal ad-hoc item's `createdAt` must be earlier than every running top-level item's `createdAt`. Non-terminal items sort by `createdAt` ascending, so promoting the terminal item to `running` only changes the emitted order under this invariant; without it the mutation is hollow.
- Ad-hoc items are never `gated`. Rules out mapping `blocked` / `paused` runs onto the gate rank; "needs me" surfacing belongs to `tui-attention-segment`.
- No pane-side retention: every non-queued run matching no stage `workflowInvocationId` becomes a node, and overflow rides the existing scroll viewport. Rules out any replacement budget or FIFO. The daemon's fifty-newest-terminal `list` retention becomes the only cap. `filterMonitorRunsForLiveWindow` / `TUI_TERMINAL_ROW_CAP` (`tui-monitor-terminal-window.ts`) has no production caller today and stays that way — vestigial dead code, not a cap this spec relies on or removes.
- Ad-hoc rows stay non-expandable this subspec; `e`, `expand`, and `collapse` keep reporting `unattributed`. Deferred to first consumer: ad-hoc constituent-run expansion — pin when a caller needs it. Rules out adding invocation-level expansion here.
- The `unattributed` selection-error code keeps its name and meaning (a run not attributed to a pipeline); only its detection source moves from segment membership to node kind. Rules out folding it into `run_leaf`, which churns the dock's documented codes for no operator gain. Typed steering (`pause`/`kill`/`resume`/`approve`/`reject`) keeps refusing an ad-hoc row with this code unchanged — only navigation, selection, and inspection become first-class; steerability is deferred to `tui-attention-segment`.
- Detection reads `fullTreeRows`, not painted rows, so an off-viewport ad-hoc selection now reports `unattributed` instead of today's `stale_non_expandable`. A behavior change the deletion causes, not a preserved one.
- Initial selection changes: a running ad-hoc item can now sort ahead of a running pipeline (both `running`-ranked, ties break on `createdAt` ascending), so `firstSelectableNodeId` can land on an ad-hoc row where today it always lands on a pipeline or stage. A behavior change the unification causes, not a preserved one.
- Right pane: an ad-hoc selection renders run detail with no pipeline context, suppressed by node kind. Rules out the positional `findLast` ancestor scan, which would attach whichever pipeline happens to sort above it.
- Fixture invariant for the right-pane pin: the ad-hoc row must sort *below* a pipeline row. With no pipeline above it the ancestor scan finds nothing anyway and the guard mutation is hollow.
- Queue-block clipping is pre-existing and stays out of scope: `leftPaneTreeMaxVisibleRows` reserves one row for the queue heading only, not one per queued run, so a pane with more than one queued run already clips inside a fixed-height terminal today. Unification makes tree saturation the routine case rather than the edge, making the clipping more visible, but reserving full queue height is a separate change this subspec does not make.
- File and exported builder names stay (`tui-monitor-pipeline-tree.ts`, `buildMonitorPipelineTree`, `buildMonitorPipelineTreeJoin`). Rules out a work-tree rename, which would stale the `## Prerequisites` of every queued sibling ready-intent.
- `flattenMonitorPipelineTree`/`buildMonitorPipelineTree` drop their unused `maxVisibleRows` parameter. Rules out threading a pane dimension through a builder that never reads it.
- `workflowTableRowMembers` moves from `tui-monitor-lines.ts` to `tui-monitor-workflow-collapse.ts` and is exported. Rules out duplicating it in the tree module; the FIFO being deleted is its only current caller.
- Assertions run through the pure builders (`flattenMonitorPipelineTree`, `monitorLeftPaneTreeRows`, `monitorSelectableNodeIds`, `monitorRightPaneSegmentRows`) and the injected input hook; no real-terminal frame assertions (`v2/docs/test-writing.md` § TUI test strategy). Element-tree assertions against a stubbed `Text`/`RowBox` are permitted — `createMonitorDisplay retains unattributed orphans regardless of display clock` (`tui-ink-monitor.test.tsx`) already passes that way today.
- Out of scope: row appearance (`tui-work-row-anatomy`), pipeline subtree shape (`tui-intent-branch-subtree`), the queue segment's own row budget, and the legacy flat `monitorSegmentRows` / `monitorTextLines` path.

## Prerequisites

- `buildMonitorPipelineTreeJoin` already partitions runs into stage-matched and unattributed candidates via `isUnattributedCandidate` (`v2/src/tui/tui-monitor-pipeline-tree.ts`).
- `compareTopLevelOrderKeys` ranks a generic `{ rank, createdAt, finishedAtMs }` key, so a non-pipeline top-level item reuses it by supplying that shape (`v2/src/tui/tui-monitor-pipeline-tree.ts`).
- `workflowGroupHasActiveMember` and `workflowRollupFinishedAtMs` roll a collapsed group's members up to a status and a finish (`v2/src/tui/tui-monitor-workflow-collapse.ts`).
- `monitorLeftPaneTreeRows` already returns `fullTreeRows` alongside the viewport slice, and `withLeftPaneTreeScrollFollow` scrolls the viewport to the selected row (`v2/src/tui/tui-monitor-lines.ts`).

## Tasks

- `v2/src/tui/tui-monitor-workflow-collapse.ts`: export `workflowTableRowMembers(row: WorkflowTableRow): DaemonListRunRow[]` (moved verbatim from `tui-monitor-lines.ts`).
- `v2/src/tui/tui-monitor-pipeline-tree.ts`:
  - Add `export type MonitorPipelineTreeAdHocNode = { kind: "adhoc"; id: string; depth: 0; tableRow: WorkflowTableRow }` and include it in `MonitorPipelineTreeDisplayNode`.
  - Rename `isUnattributedCandidate` → `isAdHocCandidate`; `buildMonitorPipelineTreeJoin` returns `{ pipelineNodes, adHocNodes, builderRuns }`, mapping `buildWorkflowTableRows(adHocCandidates, builderRuns, new Set())` to `adhoc` nodes with `id: monitorTreeRun(tableRow).runId`.
  - Add `adHocNodeOrderKey(node)`: `const members = workflowTableRowMembers(node.tableRow);` then `const rank = workflowGroupHasActiveMember(members) ? "running" : "terminal";` (this call is the guard-mutation anchor), `createdAt: Math.min(...members.map((run) => run.createdAt))`, `finishedAtMs: workflowRollupFinishedAtMs(members) ?? null`.
  - Replace `orderPipelineNodes` with `orderTopLevelNodes(pipelineNodes, adHocNodes)`: map each list to `{ node, key }` with its own key deriver, sort with the existing `compareTopLevelOrderKeys`, unwrap.
  - `flattenMonitorPipelineTree(pipelineNodes, adHocNodes, expandedNodeIds, selectedNodeId, builderRuns)` (no `maxVisibleRows`) ends with `return orderTopLevelNodes(pipelineNodes, adHocNodes).flatMap((node) =>` / `node.kind === "adhoc" ? [node] : flattenPipelineNode(node, effectiveExpansion, builderRuns),` / `);` — that ternary line is the keystone-mutation anchor and must stay on one physical line.
  - `buildMonitorPipelineTree(snapshots, runs, expandedNodeIds, selectedNodeId)` returns `MonitorPipelineTreeDisplayNode[]` directly.
- `v2/src/tui/tui-monitor-lines.ts`:
  - Delete `retainUnattributedSegmentFifo`, `leftPaneUnattributedBodyRowBudget`, `unattributedLeftPaneHeading`, `monitorLeftPaneUnattributedSegmentRows`, and the local `workflowTableRowMembers`.
  - `monitorLeftPaneTreeRows` returns `{ treeRows, fullTreeRows }` only; `monitorSelectableNodeIds` returns `fullTreeRows.map((row) => row.id)`.
  - Add `selectedAncestorPipeline(fullTreeRows, treeRow)` opening with `if (treeRow === undefined || treeRow.kind === "adhoc") return undefined;` (guard-mutation anchor) and otherwise reproducing today's `findLast` scan over the rows preceding `treeRow`; `unwrappedRightPaneSegmentRows` uses it and resolves the selected run id from `treeRow.id` for both `run` and `adhoc` rows.
- `v2/src/tui/tui-ink-monitor.tsx`: `renderTreeRow` gains an `adhoc` case rendering `renderRunGridRow(treeRow.tableRow, …, treeRow.depth, …)`; `renderLeftPaneContent` paints `treeRows` then the queue block, with no heading and no unattributed loop, and shows `No runs.` when `treeRows` is empty.
- `v2/src/tui/tui-entry.tsx`: add `runRowSelectionError(fullTreeRows, selectedNodeId): "unattributed" | "run_leaf" | null` returning `unattributed` for a matching `adhoc` row (`row.kind === "adhoc"` is the guard-mutation anchor and must occur exactly once in this file) and `run_leaf` for a matching `run` row; call it from both `pipelineSteeringSharedSelectionError` and `expansionCommandSelectionError` in place of the `unattributedRows` membership checks.
- Tests — add:
  - `v2/src/tui/tui-monitor-pipeline-tree.test.ts` `ad-hoc work items order among pipelines by rank then finish`: one running pipeline, one terminal pipeline, one active ad-hoc group, one terminal ad-hoc run with a later finish than the terminal pipeline and a `createdAt` earlier than every running top-level item (the rank-checkpoint fixture invariant — see decision ledger); assert the flattened top-level order interleaves them running-then-terminal with terminals newest-finish-first. Carries the rank guard `// @mutate`.
  - `v2/src/tui/tui-monitor-lines.test.ts` `every ad-hoc row stays selectable when the work tree overflows the pane`: more ad-hoc rows than `layout.paneHeight` (mixed active, finish-stamped terminal, finishless terminal), no queued runs in the fixture so `maxVisibleRows` equals `layout.paneHeight` itself — the same derivation `overflowPaneMonitorFixture` already uses, not a new export; assert `monitorSelectableNodeIds` contains every run id and `treeRows.length <= maxVisibleRows`. Carries the keystone `// @mutate`.
  - `v2/src/tui/tui-monitor-lines.test.ts` `ad-hoc run detail omits pipeline context when a pipeline row precedes it`: a running pipeline plus a terminal ad-hoc run (so the ad-hoc row sorts below it), selection on the ad-hoc row; assert the right-pane rows are run detail only — no `pipelineId:` or `Stages` line. Carries the right-pane guard `// @mutate`.
- Tests — remove and rewrite:
  - Delete `unattributed segment FIFO retains active runs and drops oldest terminals first` (`tui-monitor-pipeline-tree.test.ts`), `left pane labels unattributed segment with retained run count` and `left pane labels unattributed segment with zero retained body rows` (`tui-monitor-lines.test.ts`), `right pane omits detail for runs outside the selectable window` (`tui-monitor-lines.test.ts` — imports the deleted `leftPaneUnattributedBodyRowBudget`, asserts a run is absent from `monitorSelectableNodeIds`, and carries a `// @mutate` on the windowed-selection fallback; its premise is exactly what this subspec abolishes), and `a selected unattributed run evicted by segment FIFO clears the selection` (`tui-entry.test.tsx`) — the last one's surviving contract (selection clears when the selected id leaves the selectable list) is already pinned by `when a refresh drops the selected id from the selectable list, selectedNodeId clears`.
  - Replace the `runTuiEntry unattributed segment FIFO` describe in `v2/src/tui/tui-monitor-terminal-window.test.ts` with one test that drives `runTuiEntry` at an 8-row terminal and asserts every ad-hoc run id — active, stale-finished, and all 22 terminals — is selectable.
  - Rewrite `emits pipeline, stage, and run rows with increasing depth and places orphans after the tree` (`tui-monitor-lines.test.ts`): the orphan run now appears as a top-level `adhoc` row inside the same flattened `treeRows` list, not a separately-returned `unattributedRows` row; retitle and reassert the shape.
  - Rewrite `after refresh, selectedNodeId is the first selectable tree or unattributed row in pane order` (`tui-entry.test.tsx`): its `pipelineTreeWithOutsideRunFixture` fixture's `RUN_ALPHA` is running with `createdAt: 0`, earlier than `pipe-alpha`'s `createdAt`, so unified ordering now selects `run-alpha` first, not `pipe-alpha`; flip the assertion to match this on-disk behavior change.
  - Update `places runs with no matching stage only in unattributed` and `excludes stage-matched and queued runs from unattributed candidates` (`tui-monitor-pipeline-tree.test.ts`) to the ad-hoc vocabulary and the new `buildMonitorPipelineTreeJoin` return shape; update the `unattributedRows` helpers in `tui-monitor-lines.test.ts`, `tui-entry.test.tsx`, and `tui-monitor-terminal-window.test.ts` to read ad-hoc rows off `fullTreeRows`; update `createMonitorDisplay retains unattributed orphans regardless of display clock` (`tui-ink-monitor.test.tsx`) to assert the orphan row paints in the left pane with no `─ Unattributed` heading.
  - Audit every `// @mutate` directive under `v2/src/tui/` after the deletions and renames above: re-anchor or delete (with its host test) any directive whose quoted original text no longer occurs verbatim in its target file.
- Update `v2/docs/operator-runbook.md` (§ Observe and the known-gotchas bullet on watching a workflow by branch), `v2/docs/v1-behaviors.md`, and `v2/spec/tui-command-center-brief.md` per Documentation updates.
- Run `bun run typecheck`, `bun run check`, `bun run test:v2`, `bun run test:integration:v2`.

## Acceptance criteria

- [x] `v2/src/tui/tui-monitor-pipeline-tree.test.ts` — `ad-hoc work items order among pipelines by rank then finish` asserts one flattened top-level list interleaving pipeline and ad-hoc items by rank (running before terminal) and, within terminals, newest finish first, with the terminal ad-hoc item's `createdAt` fixtured earlier than every running item's so the rank checkpoint below is non-hollow; it fails against the pre-fix code, which emits no ad-hoc nodes from the flatten at all.
- [x] `v2/src/tui/tui-monitor-lines.test.ts` — `every ad-hoc row stays selectable when the work tree overflows the pane` pins more ad-hoc rows (active, finish-stamped terminal, finishless terminal) than the pane height and asserts `monitorSelectableNodeIds` contains every one of them while the painted `treeRows` stay within `maxVisibleRows`; it fails against the pre-fix code, where `retainUnattributedSegmentFifo` drops all but the body budget before selection is derived.
- [x] `v2/src/tui/tui-monitor-lines.test.ts` — `ad-hoc run detail omits pipeline context when a pipeline row precedes it` asserts the right pane for a selected ad-hoc row contains only run detail — no `pipelineId:` and no `Stages` row — with a pipeline row sorted above it in the same tree.
- [x] `v2/src/tui/tui-monitor-lines.test.ts` — `every ad-hoc row stays selectable when the work tree overflows the pane`; Keystone checkpoint: its pinning test carries `// @mutate v2/src/tui/tui-monitor-pipeline-tree.ts "node.kind === \"adhoc\" ? [node] : flattenPipelineNode(node, effectiveExpansion, builderRuns)" -> "node.kind === \"adhoc\" ? [] : flattenPipelineNode(node, effectiveExpansion, builderRuns)"` inside the test body — dropping ad-hoc items back out of the work tree (baseline semantics) — and the mutation turns that regression RED.
- [x] `v2/src/tui/tui-monitor-pipeline-tree.test.ts` — `ad-hoc work items order among pipelines by rank then finish`; Mutation checkpoint: its pinning test carries `// @mutate v2/src/tui/tui-monitor-pipeline-tree.ts "workflowGroupHasActiveMember(members)" -> "true"` inside the test body — ranking every ad-hoc item as running, including terminals — and the mutation turns that regression RED.
- [x] `v2/src/tui/tui-monitor-lines.test.ts` — `ad-hoc run detail omits pipeline context when a pipeline row precedes it`; Mutation checkpoint: its pinning test carries `// @mutate v2/src/tui/tui-monitor-lines.ts "if (treeRow === undefined || treeRow.kind === \"adhoc\") return undefined;" -> "if (treeRow === undefined) return undefined;"` inside the test body — restoring the positional ancestor scan for ad-hoc rows, which then inherit the preceding pipeline's context — and the mutation turns that regression RED.
- [x] `v2/src/tui/tui-entry.test.tsx` — `reports expansion selection feedback without mutating expansion state`; Mutation checkpoint: its pinning test carries `// @mutate v2/src/tui/tui-entry.tsx "row.kind === \"adhoc\"" -> "false"` inside the test body — losing the ad-hoc classification so an ad-hoc selection no longer reports `unattributed` — and the mutation turns that regression RED.
- [x] `retainUnattributedSegmentFifo`, `leftPaneUnattributedBodyRowBudget`, `unattributedLeftPaneHeading`, and `monitorLeftPaneUnattributedSegmentRows` no longer exist in `v2/src/tui/tui-monitor-lines.ts` or anywhere else under `v2/src/`, and their tests (`unattributed segment FIFO retains active runs and drops oldest terminals first`, `left pane labels unattributed segment with retained run count`, `left pane labels unattributed segment with zero retained body rows`, `right pane omits detail for runs outside the selectable window`, `a selected unattributed run evicted by segment FIFO clears the selection`, and the `runTuiEntry unattributed segment FIFO` describe) are gone with them.
- [x] No `// @mutate` directive under `v2/src/tui/` references text removed or reshaped by this subspec — each pre-existing directive still resolves verbatim in its target file, or was re-anchored or deleted with its host test.
- [x] A run whose `workflowInvocationId` matches a pipeline stage appears only under that stage: `v2/src/tui/tui-monitor-pipeline-tree.test.ts` `excludes stage-matched and queued runs from unattributed candidates` and `nests a run whose workflow invocation matches a stage under that stage` stay green (retitled to ad-hoc vocabulary where they name the segment; assertions unchanged).
- [x] `v2/src/tui/tui-monitor-lines.test.ts` — `emits pipeline, stage, and run rows with increasing depth and places orphans after the tree` is rewritten so the orphan run appears as a top-level `adhoc` row in the unified flattened list rather than a separately-returned `unattributedRows` row; it fails against the pre-fix code, which still returns two separate lists.
- [x] `v2/src/tui/tui-entry.test.tsx` — `after refresh, selectedNodeId is the first selectable tree or unattributed row in pane order` is rewritten to assert the running ad-hoc run (`run-alpha`, earlier `createdAt`) is the initial selection ahead of the running pipeline; it fails against the pre-fix code, which always selects the pipeline first.
- [x] `v2/src/tui/tui-entry.test.tsx` `reports expansion selection feedback without mutating expansion state`, `typed run steering on ineligible selection reports feedback and issues no RPC`, and the `approve` / `reject` / `resume` ineligible-selection tests stay green with `unattributed` unchanged as the reported code for an ad-hoc row selection.
- [x] `v2/src/tui/tui-ink-monitor.test.tsx` `createMonitorDisplay retains unattributed orphans regardless of display clock` asserts the ad-hoc row paints in the left pane and that no `─ Unattributed` heading is painted; this is evidence the segment is gone, not a preservation claim.
- [x] `v2/docs/operator-runbook.md` § Observe describes one left-pane work tree whose top-level rows are pipelines and ad-hoc work items ordered together, states that ad-hoc rows are navigable, selectable, and inspectable with no pane-side retention (daemon `list` retention is the only cap), that navigation and viewport scrolling span every row, that typed steering (`pause`/`kill`/`resume`/`approve`/`reject`) still refuses an ad-hoc row with the `unattributed` code (deferred to `tui-attention-segment`), and no longer mentions an unattributed segment or its FIFO.
- [x] `v2/docs/operator-runbook.md`'s known-gotchas bullet on watching a workflow by branch drops its "Unattributed workflows" segment framing while keeping the still-true facts (ad-hoc top-level rows collapse one row per invocation, no flat-row `e` expansion).
- [x] `v2/docs/v1-behaviors.md` § TUI / observability records the unified work tree: ad-hoc top-level nodes, their rank derivation (running when any member is non-terminal, else terminal; never gated; earliest member `createdAt`, latest member finish), the deleted segment/FIFO/heading and its retention rules, the changed initial-selection ordering (a running ad-hoc item can now be selected first, ahead of a running pipeline), right-pane run detail without pipeline context for an ad-hoc selection, `e` still a no-op on ad-hoc rows, that an off-viewport ad-hoc selection now reports `unattributed` rather than `stale_non_expandable`, and that `unattributed` keeps its meaning and typed-steering refusal unaffected by the segment's deletion; the temporary retention-order inconsistency note left by `20260807T212915Z-tui-work-tree-top-level-ordering` is removed as resolved.
- [x] `v2/spec/tui-command-center-brief.md` row 2 records `tui-unified-work-tree` as shipped by this spec directory rather than `seeded`.
- [x] `bun run typecheck`, `bun run check`, `bun run test:v2`, and `bun run test:integration:v2` pass.

## Documentation updates

- `v2/docs/operator-runbook.md` § Observe (the `jarvis tui` table row and the paragraph after it) — one work tree; ad-hoc runs become navigable, selectable, and inspectable top-level rows ordered with pipelines; "Unattributed" and its FIFO are gone; no pane retention; typed steering (`pause`/`kill`/`resume`/`approve`/`reject`) still refuses an ad-hoc row (`unattributed`, deferred to `tui-attention-segment`); `e` remains a no-op on ad-hoc rows.
- `v2/docs/operator-runbook.md` known-gotchas bullet on watching a workflow by branch — drop the "Unattributed workflows" segment framing; ad-hoc top-level rows still collapse one row per invocation with no flat-row `e` expansion.
- `v2/docs/v1-behaviors.md` § TUI / observability — multi-daemon aggregation bullet: drop the segment FIFO retention and heading sentences plus the ordering-inconsistency note. Pipeline-tree bullet: ad-hoc top-level nodes, their order key, and right-pane detail without pipeline context. Navigation bullet: the changed initial-selection ordering (a running ad-hoc item can now be selected first, ahead of a running pipeline). Dock-code bullet: `unattributed` keeps its name and meaning for an ad-hoc row, is now reported for off-viewport selections too, and stays unaffected in meaning by the segment's deletion — typed steering still refuses ad-hoc rows with it.
- `v2/spec/tui-command-center-brief.md` — seed table row 2 state.
