# Session 2026-09-21: twelve lanes landed and two of them only did a third of the work

Operator session 2026-09-21 (Opus 5), Jarvis-on-Jarvis, agent order `codex, claude`. Started from a clean state (80 runs and 2 pipelines dismissed, no stray worktrees or branches). **12 implement PRs, one direct harness fix, and 2 spec PRs merged**; every implement had an independent diff review, and every daemon/publication PR had two green CI runs pinned to one head. The P0 row closed, chain C and the publication tail closed, chain D advanced one link, and issue #2996 closed.

## What landed

| PR | What |
| --- | --- |
| [#4132](https://github.com/cbrenner04/jarvis/pull/4132) | P0: stage `run_timeout` incident cause derives from durable invocation rows |
| [#4136](https://github.com/cbrenner04/jarvis/pull/4136) | IPC connect budget: 30 s default, separate from frame reads, `IPC connect timeout <N>ms` |
| [#4152](https://github.com/cbrenner04/jarvis/pull/4152) | Spawned-CLI connect-budget regressions and docs |
| [#4151](https://github.com/cbrenner04/jarvis/pull/4151) | One failure block rendered identically by run wait/list, pipeline list, and the TUI (chain C tail) |
| [#4145](https://github.com/cbrenner04/jarvis/pull/4145) | Stale publication causes on `completed` rows stop reading as failures (publication tail) |
| [#4138](https://github.com/cbrenner04/jarvis/pull/4138) | Every mutation-repair commit is pushed before verification or settlement (chain D) |
| [#4143](https://github.com/cbrenner04/jarvis/pull/4143) | `daemon status` reports `inconclusive`, not `stopped`, for a live but slow daemon |
| [#4140](https://github.com/cbrenner04/jarvis/pull/4140) | Stale-lane retirement validates `--base` before destroying; destroyed-artifacts report carries tip SHAs |
| [#4147](https://github.com/cbrenner04/jarvis/pull/4147) | `cleanup --abandon` refuses unlanded work with no PR unless `--discard-unlanded` |
| [#4149](https://github.com/cbrenner04/jarvis/pull/4149) | A resumed run reopens its failed stage, settles it from the real outcome, and dispatches the successor once |
| [#4153](https://github.com/cbrenner04/jarvis/pull/4153) | Decision verbs claim a pipeline from any draining generation whose owner witness matches |
| [#4154](https://github.com/cbrenner04/jarvis/pull/4154) | `pipeline resume` reopens interrupted stages; dismissed refuses; running-stage refusal names `run kill --force` (closes #2996) |
| [#4142](https://github.com/cbrenner04/jarvis/pull/4142) | Direct fix: intent stage discards the agent's `.claude/` bookkeeping dir before validation |

Spec PRs: #4119 (retired a false-premise ready-intent, refreshed brief/ledger), #4146 (seed `decision-verbs-claim-from-any-draining-generation`, implemented the same session). Intents #4120 #4121 #4122 #4129 #4130 #4131 #4137 #4139 #4148; plans #4123 #4126 #4127 #4135 #4141 #4144; closed as subsumed #4124 #4125 #4128 #4133 #4134 #4150. Archive PR carries this report.

## Review record

- **Two lanes reported complete after one of their three subspecs** (#4149, #4154): ready gate green, PR flipped ready, criteria for the untouched subspecs unticked. Only diff review caught them; #4154's partial state would have let `pipeline resume` reopen a dismissed pipeline. Both were rebased and re-dispatched to finish. This recurred, so it is a seed candidate (not written: the operator stopped new work).
- **Mutation gate verdicts under load were wrong in both directions.** `workflow-runner-resume.ts:2430` was reported surviving, then non-terminating; six existing tests kill it and nothing hangs. `state-store.ts:2573` reported non-terminating, passed on resume. Real survivors: `cleanup.ts:3102` (tip SHA spread, test added) and `pipeline-execution.ts:243` (branch unreachable through `resumePipeline`; helper pinned both ways — the branch is a dead-code cleanup candidate). A `launchd`-parented `bun -e ... CURRENT_OWNER_IDENTITY ... setInterval` orphan held one lane's worktree for hours; killed.
- **Non-terminating mutant, real:** `daemon-run-lifecycle-handlers.ts:162` made two finalization-kill tests hang on a hook that never fired; fixed by racing the hook against the resume settling.
- Every other review returned MERGE with each key regression shown failing against pre-fix code.

## Friction

- **Gate slot is the bottleneck.** Up to six lanes queued on `slot_contention`; each refusal emitted a `failed` terminal notification and marked its pipeline `failed` even though the automatic re-drive later completed the lane. #4149 fixes the stage half.
- **Three daemon generations after back-to-back source merges.** A pipeline owned two generations back refused `approve` with `pipeline_no_live_owner` for ~30 min, and the degraded listing refused the id prefix; forced a workflow fallback (plan #4144 merged, implement dispatched standalone). Seeded and fixed (#4146 → #4153). Prefix resolution for older generations is deferred; use the full id.
- **Operator error:** merged pipeline plan PRs (#4141, #4135) while their implement lanes held ticked specs, so re-dispatch hit a rebase conflict on the spec file; hand-rebased keeping the lane's ticks. Close pipeline plan PRs; do not merge them.
- A claude actuator left `.claude/.cc-writes` in the intent stage and stranded landing (fixed #4142); the same intent's prerequisites were prose, not bullets (hand-edited, then resumed).
- Terminal ready publication on pipeline `4bf787ec` failed `exit unknown` after the PR was hand-merged (seen once).
- A new daemon reconciled entry row `48d8f8c7` as killed at handoff while the draining generation still held the worktree; the lane continued on another row.
- The TUI does not show a live resumed row whose invocation entry is terminal (`797f29a9`).
- Follow-up from review: `reopenFailedStagesForResume` full-scans `listPipelines()` per resume admission; `probeSocket`/`supersedePeerDaemon` could pass their own connect budget.

## Seed candidates (not written)

Implement completes after the first subspec of a multi-subspec tree (recurred twice); TUI hides live resumed rows; slot-contention refusals notify as `failed`; test-spawned owner-identity probe leaks to `launchd`.

## Cost

| | Cost | Notes |
| --- | --- | --- |
| Operator (`/cost`) | $65.90 | claude-opus-5; API 36m 4s; wall 12h 0m; 40.1k in, 214.6k out, 92.6M cache read, 1.8M cache write |
| Agents (telemetry) | $90.21 | 346 invocations; codex gpt-5.6-sol/terra, claude-opus-5/sonnet-5 |
| **Total** | **$156.11** | 12 implements → $13.01 per landed implement |

Most expensive lanes: settles-from-recovered-row $15.70 (two dispatches), resume-interrupted-stage $10.33 (two dispatches), failed-publication $9.43, render $8.28.
