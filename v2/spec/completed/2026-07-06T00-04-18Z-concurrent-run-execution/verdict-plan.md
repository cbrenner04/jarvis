Verdict: refine per below.

1. **Acceptance criteria must assert the causal mechanism, not just the symptom.** Rewrite the two AC bullets so they describe the pending-promise construction directly (both write loops held open/unreleased simultaneously via the fake executor) rather than only citing `list`'s `isLive: true` output. `isLive: true` alone is satisfiable by a status-reporting bug without real concurrent execution; the AC is the contract a reviewer/implementer checks, so it must carry the real assertion instead of relying on the task checklist to supply it.

2. **Anchor the "nothing serializes admitted runs" claim to a file:line pointer.** This is a preservation/investigation claim about existing behavior in `spawnWriteLoop`/`start` in `v2/src/daemon/daemon.ts`, not new behavior — per this repo's "cite the test, don't paraphrase" convention for behavior-preserving claims, it must point at the specific code location instead of asking the reader to re-derive it from prose.

3. **Explicitly flag the intent-vs-investigation scope pivot.** The intent frames this as needing daemon changes ("executes multiple admitted runs in parallel rather than serializing them"); the investigation found the capability already ships and the work is regression coverage only. This reinterpretation is currently buried in a subordinate clause of the opening paragraph. State it up front and unambiguously — e.g., as a labeled note near the top of the subspec — so a reviewer skimming index/intent alongside the subspec sees the scope correction immediately, not only by close-reading body prose. This matches the spirit of the Prerequisites-gate rationale in spec guidance: assumed-necessary work turning out to be already-done is exactly the kind of premise mismatch that must be surfaced, not silently absorbed.

No refinement required for the "two vs two-or-more" scope narrowing — it's a defensible, intentional choice; a brief one-line acknowledgment is welcome but not mandatory.