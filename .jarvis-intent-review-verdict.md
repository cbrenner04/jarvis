Enumeration scope and merged-detection partition (advocate #1, #2) are load-bearing and must be resolved at drafting:

- State the branch-enumeration source and the exclusion set explicitly (current branch, base/`main`, branches with no PR, refs from co-located non-project checkouts). "Independent of worktrees" removes the project-scoping the worktree dir provided — replace it with an explicit scope.
- Close the partition. The three Behavior bullets all key off `gh` PR state, but the Gap motivates on hand-merged branches. A git-merged branch with no PR falls through all three. Decide explicitly: either narrow the Gap to "merged PR without worktree" (no-PR-but-git-merged out of scope), or add a git-ancestry merged check distinct from `gh` and cover that case in Behavior.

Add a `--dry-run` Behavior bullet: the new prune must honor it (advocate #4).

Clarify "local remote-tracking ref" (prunes `refs/remotes/origin/<branch>`, not the remote branch) to prevent reviewer misread (advocate #5).

Drop reliance on `cleanup.ts:701/:968` line anchors as contract; keep as evidence only (advocate #6). Sequencing already correctly mitigates the sibling-shape dependency.

Not intent defects: acceptance-criteria/failing-test framing (#3) is deferred to plan/draft.