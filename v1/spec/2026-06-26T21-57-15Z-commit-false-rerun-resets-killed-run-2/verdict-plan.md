I verified the advocate's decisive code claim directly. Confirmed: all four interrupt paths (`iteration.ts:846/872/904/918`) call `captureInterruptedDelta`, which records the delta **only into the in-memory `state.noCommitDelta`** (`recordNewlyCheckedAc`/`recordBlocker`) and then each path immediately `return`s. **None call `saveDelta`** — the only function that writes to disk. So the interrupt delta is recorded then discarded; on re-run `loadDelta` finds nothing. Bug #520 is live on all four paths. This flips the spec's central framing.

## Verdict — required refinements

1. **Correct the diagnosis prose (load-bearing).** The Problem/Decisions text claiming capture is "already wired … each persisting via `saveDelta`" and that this is "verify-and-guard, not green-field" is factually wrong. The interrupt paths record the delta in memory but never persist it. The spec must state the real defect: *delta is recorded in memory on interrupt but not written to disk; the fix is to persist it* (add `saveDelta` at/after the four capture sites or inside `captureInterruptedDelta`). The acceptance criteria already encode the correct red-first contract and need no change — only the diagnosis must stop telling the implementer no `src` change is needed.

2. **Scope the spec to include the fix, not just tests.** Because the bug is live, the task checklist's "if any path fails, fix the capture path" is the primary deliverable, not a contingency. Reframe so the persist fix is expected work, with the new integration tests proving it red-first.

3. **Name the relocation asymmetry and decide it explicitly.** The graceful path resolves a moved subspec via `findRelocatedSpecFile`; `captureInterruptedDelta` reads the original `activeSubspecPath` with no relocation handling, so a no-commit run that relocates its subspec then is interrupted captures nothing. This sits squarely inside "a run killed mid-progress is reset." The spec must either fold in coverage or add an explicit out-of-scope Decision deferring relocation-during-interrupt to its own subspec (noting #520's path doesn't relocate). Silent omission is not acceptable.

4. **Rule out the fix-up-iteration case in one line.** On fix-up iterations `activeSubspecPath` is `undefined` so capture no-ops; since fix-up runs the ready-gate fix (no subspec AC/blocker delta in that window), this is correctly out of scope — but a one-sentence Decision should say so rather than leave it unaddressed. No test warranted.

5. **Specify the reset-before-invoke assertion mechanism.** ACs require the re-run to un-tick "before the agent is invoked." The spec pins run 1's capture seam but not how run 2 proves reset precedes spawn. Name a race-free, content-based mechanism (e.g., assert the injected prompt/spec snapshot at the second run's prompt-build point already shows the AC un-ticked) so the implementer doesn't reach for timing.

6. **Acknowledge the seam-fidelity limitation.** Driving SIGINT/timeouts through the structured `{kind:"error", stderr:"aborted:<reason>"}` result is the right flake-avoidance choice, but it cannot prove a real signal surfaces as that result before teardown. Add a one-line known-limitation note; the end-to-end re-run-reads-disk assertion is the load-bearing, seam-independent check.

7. **Replace rotting line-number anchors with symbol anchors.** The spec leans on `iteration.ts:846/872/904/918` as load-bearing references that will drift. Cite the `captureInterruptedDelta` call sites and the `aborted: <reason>` branches by symbol/name instead. The git:true preservation AC is already in correct citation form — leave it.

8. **Clarify the four interrupt tests pin wiring, not four behaviors.** All four branches reach the identical capture+persist line, differing only in the upstream stderr match. A half-sentence saying these pin that *each interrupt branch persists* prevents over-reading them as four distinct behaviors. Coverage is justified since this wiring is exactly what regressed.

9. **Keep both doc ACs.** Because persistence-on-interrupt is a genuine behavior change (delta now survives where it didn't), updating `v1/docs/run-loop.md` and `v2/docs/v1-behaviors.md` is warranted, not speculative churn. The conditional wording is fine and will now trigger.

**Rationale:** The intent is correct that an interrupted commit:false run is not currently reset; the spec's prose contradicts the intent and the code. Per the repo's accuracy principle for refactor/preservation framing, a diagnosis that misstates current behavior would misdirect the implementer (here, into shipping tests with no fix). Refinements 1–2 fix that; 3–5 close the real structural gaps (relocation, fix-up scope, ordering assertion) so the single subspec stays atomic and its fix scope can't balloon; 6–9 are low-cost precision and correct doc/anchor handling.