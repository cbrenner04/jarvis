**Verdict: 3 refinements required.**

1. **Add the two missing stale-doc locations.** `v1/docs/specless-prompt.md` contains a "default 30 minutes" reference to `iterationTimeoutMs` and must be added to Documentation updates and covered by the stale-reference acceptance criterion, alongside the already-listed docs. The `README.md` example (`"iterationTimeoutMs": 1800000`) is the same knob and must update too; the spec should note this explicitly rather than leaving README out of scope. (The `JARVIS_READY_TIMEOUT_MS` comment is a distinct env var and stays out of scope — no action needed there.) Rationale: the intent's own AC says "docs listing the `iterationTimeoutMs` default no longer say 30 minutes" — an incomplete doc sweep leaves that AC only partially satisfied.

2. **Add `v2/docs/v1-behaviors.md` to Documentation updates.** Spec guidance is explicit: any change to existing functionality must update this baseline catalog. Its absence is a direct guidance violation, not a judgment call.

3. **Add a runtime behavioral acceptance criterion (or explicit test citations) proving the timeout actually applies at 10 minutes across each consumer** (patch/review/shrink/prompt iterations, and explicitly note whether plan-mode iterations share the same knob). Currently the ACs only check the constant's value and doc text — that lets a spec pass while a consumer reads the value through a separate path or hardcoded fallback and remains unaffected. The Decisions section should also state explicitly whether plan-mode's shared use of `iterationTimeoutMs` is in scope (intended) or should be exempted, since a reviewer can't otherwise tell whether that consequence was considered.

Additionally (lower priority but should be folded in with #3): the acceptance criteria referencing test files should distinguish between tests that assert the *default value* (must update) versus tests that merely supply 30-minute values as unrelated fixtures (need not change), rather than leaving that triage implicit for the implementer.