**Verdict: Refinements required**

1. **Add a missing-failure AC.** The Decisions section states commit failure is best-effort (log to stderr, still exit 8), but no acceptance criterion exercises this. Add an AC verifying: a checkpoint commit that fails to stage/commit is logged to stderr and the run still exits 8, not a harder failure. Without this, the failure posture can silently regress.

2. **Resolve the "tracked" vs. `git add -A` scope conflict.** The intent's Decisions say "uncommitted agent edits present (git-tracked...)" but the subspec instructs detecting changes via `git add -A` staged status, which also stages new untracked files. This is a real scope mismatch, not cosmetic — it changes what gets checkpointed. Pin one behavior explicitly: either (a) state the subspec intentionally widens scope to include new files the agent created (the more defensible choice, since the goal is not losing agent work), or (b) restrict staging to already-tracked paths only. Update both the Decisions and the acceptance criteria to match whichever is chosen, so intent and subspec no longer diverge.

3. **Pin ordering/interaction with the existing lockfile-commit path.** The subspec references `commitLockfileChanges`'s failure-handling posture by analogy but never states whether checkpoint-commit runs before, after, or instead of the existing lockfile-commit step, or whether its `git add -A` would re-sweep changes that step already committed. This must be specified as a decision: the sequencing relative to `commitLockfileChanges`, and whether checkpoint-commit now absorbs that responsibility or runs strictly after it against a clean-of-lockfile diff.

4. **Prevent duplicate/no-op commits on lockfile-only iterations.** Directly follows from #3: if lockfile changes are already committed separately, an iteration with only lockfile changes must not also produce a spurious/redundant checkpoint commit. Add a decision and acceptance criterion covering this case (e.g., a lockfile-only iteration exits 8 with only the existing lockfile commit, no additional checkpoint commit).

5. **Fill in the Prerequisites section.** The subspec depends by name on four existing behaviors/symbols (`hasUntrackedMutations`, `commitWipProgress`, `ops.hasStagedChanges`, `commitLockfileChanges`). Per spec guidance, Prerequisites exist precisely to gate confirmation of such dependencies before drafting proceeds. Leaving it empty skips that validation gate. Populate it with these dependencies so plan mode confirms they're observable in committed code before the subspec is trusted.

6. **Soften the line-number citation.** `iteration.ts:915` should be marked as an approximate/best-effort locator rather than presented as a precise, load-bearing reference, since it will drift with unrelated edits.