## Verdict — refinements required

1. **No stale `@mutate` directive may remain in the touched test file.** `v2/src/execution/mutation-checkpoint-verifier.test.ts:544` carries a directive whose original text (`DIRECTIVE_PATTERN.test(markerSource)`) no longer exists anywhere in `mutation-checkpoint-verifier.ts` — the checkpoint is dead and would only surface as `target_absent` if that test ever became a linked pin. The branch already repaired the same rot at line 211; leaving its twin is inconsistent. Outcome: every `@mutate` directive in this file must name source text that exists exactly once in its target file, and the checkpoint it guards must actually redden when applied. Note in the subspec that repairing line 211 moves that checkpoint's scoped surface from `v2/**` to `shared/**` (all three suites run for it).

2. **The token-boundary lookahead must be guarded on both arms.** The new gate requires `@mutate` to be followed by whitespace *or* end-of-line, but only the whitespace arm is exercised. A bare `// @mutate` with nothing after it must still be recognized as directive-position and reported `malformed`; today dropping `|$` leaves the suite green. Outcome: parser coverage that fails if the end-of-line arm is removed.

3. **The anchoring test must assert enough to identify the regression it guards.** The malformed-first/later-marker test asserts only `reason`. The neighbouring pre-existing malformed test asserts `sourceLine`; match that convention and also assert the reported `raw` text, since `raw` is what the operator blocker prints and "rescued by the later fragment" is precisely the shape being guarded.

4. **The positional requirement must be stated where agents are told how to author directives.** Nothing in `prompts/` says `@mutate` must be the first token after `//`; `prompts/write/mutation-directive-reprompt.md` only says "retarget each `// @mutate` directive". This matters because the change is behavior-visible: a line like `// pin: @mutate <path> "a" -> "b"` previously parsed and verified, and now yields no directive and no `malformed` — it settles as an unlinked/hollow hard-block with a message ("no `@mutate` directive linked to this criterion") that is misleading when a well-formed body sits two words later. That outcome is what the decision ledger chose, so do not change the parser; instead make the rule discoverable in the authoring/retarget prompt so agents can act on the blocker.

5. **Align the canonical write-step contract doc.** The branch updated the operator runbook and `v1-behaviors.md` but not `v2/docs/write-behavior.md`, which is the durable home for the implement-write mutation-checkpoint contract and is named in the `Sources:` line this branch edited. Its current wording ("comment-leading unparseable directives", "each linked `// @mutate` directive") is not contradicted but is coarser than the new rule. Outcome: one clause there stating directive-position recognition with token boundary, ignored later prose mentions, and malformed reporting anchored to the directive-position token.

Not upheld: the regex-source splicing used to build the anchored line pattern (the repo already composes `DIRECTIVE_PATTERN.source` elsewhere; the pattern carries no flags or anchors that would break it, and the split keeps "directive-position but malformed" distinguishable from "not a directive"), and the claim that the boundary checkpoint fails to isolate (dropping only the lookahead reddens the lookalike test alone; the keystone being coarser is expected).