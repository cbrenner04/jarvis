# Operator session — 2026-08-04

Jarvis-on-Jarvis v2. Agent order cursor-first, claude fallback (temporarily claude-first for two
specs, restored). **15 PRs merged (#2584–#2599), zero open at close, main green, Ready queue empty.**

## What landed

| # | Work |
| --- | --- |
| [#2584](https://github.com/cbrenner04/jarvis/pull/2584) | Fan-out subspec 00 — branch-scoped stage artifacts (`stageArtifactKey(stageId, branchKey)`) |
| [#2585](https://github.com/cbrenner04/jarvis/pull/2585) | Tick fan-out subspec 00 in index (router fix so re-run advances to 01) |
| [#2586](https://github.com/cbrenner04/jarvis/pull/2586) | Fan-out subspec 01 — concurrent sibling dispatch (single `runConcurrently` seam, bounded peer-claim waits) |
| [#2587](https://github.com/cbrenner04/jarvis/pull/2587) | Seeds: stalling implement successor + index-checkbox router no-op |
| [#2588](https://github.com/cbrenner04/jarvis/pull/2588) | Plan: pipeline-stage-dispatch-claim |
| [#2589](https://github.com/cbrenner04/jarvis/pull/2589) | Durable stage dispatch claim — SQL `pipeline_stage_admission` store + partition-time claim (both subspecs) |
| [#2590](https://github.com/cbrenner04/jarvis/pull/2590) | Plan: pipeline-terminal-state-waits-for-stage-settlement |
| [#2591](https://github.com/cbrenner04/jarvis/pull/2591) | Settlement-first fan-out terminality (failed branch no longer reads pipeline terminal while a sibling runs) |
| [#2592](https://github.com/cbrenner04/jarvis/pull/2592) | Plan: split-v2-review-prompt-ids-from-v1 |
| [#2593](https://github.com/cbrenner04/jarvis/pull/2593) | v2 implement review renders its own `implement.prompt.review.*` artifacts; v1 frozen byte-identical |
| [#2594](https://github.com/cbrenner04/jarvis/pull/2594) | Plan: emit-completion-commit-errors |
| [#2595](https://github.com/cbrenner04/jarvis/pull/2595) | Emit `completionCommitError` onto terminal `loop_finished` across three completion paths |
| [#2596](https://github.com/cbrenner04/jarvis/pull/2596) | Plan: project-completion-commit-errors |
| [#2597](https://github.com/cbrenner04/jarvis/pull/2597) | Project the message onto `run list`/`wait` as `error.completionCommitError` |
| [#2598](https://github.com/cbrenner04/jarvis/pull/2598) | Spec (hand-authored): render-completion-commit-errors-in-run-cli |
| [#2599](https://github.com/cbrenner04/jarvis/pull/2599) | Render the message in `run list` (trailing JSON column) / `run wait` |

Threads completed: **pipeline trustworthiness** (branch-scoped artifacts, concurrent dispatch, durable
claim, settlement-first terminality) and the **completion-commit-error chain** (emit → project → render).

## Notable events

- **Fan-out subspec 01 took three attempts.** cursor twice built the *forbidden* leader/peer
  coordination layer (the design #2577 was closed for); a subagent analysis disproved salvage and
  said re-run. claude, led explicitly, produced the spec's preferred single-concurrent-walk design;
  verified by hand — full gate, **8/8 consecutive** e2e (the AC requirement; the prior attempt hung
  1-in-3), all three `@mutate` pins kill in <100ms (fail, not hang), subagent SAFE. Its
  shrink/publication successor stalled 36 min mid-refactor; killed and hand-published from the
  verified committed state.
- **Two `completion_commit_failed` red gates from agent-written cognitive-complexity** (#2591
  `aggregateFanOutBranchSuffix` = 32; #2595 `runIntentResumeCommitAndPublish` = 25). Not autofixable
  and repair-budget can't fix it. Hand-extracted helpers preserving guard-condition text so `@mutate`
  directives still resolved and killed.
- **Dud / inert `@mutate` pins on otherwise-correct code** (#2591 five inert; #2597 two
  `target_absent`). The gate ignores `unparseable`/`target_absent` directives — the open
  `unparseable-mutation-directives-pass-the-gate` seed. Production was correct and genuinely tested by
  plain assertions in both cases; retargeted #2597's pins to honest single-line targets before merge.
- **A false `completed`/`no-work` run** (`328c3cc6`): after hand-finishing fan-out subspec 00, its
  index checkbox stayed unticked, so the router re-selected 00 (criteria already satisfied), settled
  `completed` having implemented nothing, and never reached 01. Fixed by ticking the index (#2585);
  seeded (`implement-router-reselects-fully-ticked-subspec-by-index-checkbox`).
- **Implement/plan successor steps stalled indefinitely twice** (`c6bf9b42`, `503f2683`) — no
  events, no watchdog, holding the branch claim; recovery only via `jarvis run kill` (classifier-gated
  — operator ran one). Seeded (`implement-review-publication-successor-stalls-indefinitely`).
- **cursor idle-timed-out twice** on the prompt-split spec (real edits then 90s silence, non-retryable);
  switched to claude-first and re-ran.
- **The render plan blocked twice on a multi-surface acceptance-criteria bullet** its draft kept
  producing; hand-authored the spec (#2598) with the AC split into atomic criteria.
- **Preflight deleted the retained fan-out salvage branch** (spec-dir basename == branch name) on the
  first implement launch; the named salvage commit `d402961c0` survived locally and was tagged
  `salvage/resume-binding-latch`.

## Seeds

Two new: `implement-review-publication-successor-stalls-indefinitely`,
`implement-router-reselects-fully-ticked-subspec-by-index-checkbox` (both merged #2587). Held the line
on new seeds otherwise per operator steer — the recurring dud-pin and complexity-gate frictions map to
existing seeds / are agent-output quality, not new harness gaps.

## Reviews

Every subagent diff review found something real: inert/dud `@mutate` pins (#2591, #2597), a mislocated
criterion pinned on an unexercised path (#2586). No DO-NOT-MERGE; production was correct in each case,
the pins were the defect. Six full reviews across the session.

## Cost

**$105.00** (operator, claude-opus-4-8). API time 2h 2m 5s; wall 20h 9m 46s. 126.1k input / 462.2k
output active tokens, 158.3m cache read, 1.6m cache write. 78 files touched on `main`; 7 specs
completed → **$15.00/spec**, $0.18/1k active tokens. Agents: cursor (free, led most invocations) +
claude actuator (quota); not separately dollar-billed.
