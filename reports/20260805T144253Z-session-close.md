# Session close — 2026-08-05

Jarvis-on-Jarvis, operator-driven, dogfooding the configured **full-review pipeline**.
Two keystones landed; bundle 2 started; seven harness-defect seeds filed.

## Landed (merged to main)

### Implementation PRs

- **[#2604](https://github.com/cbrenner04/jarvis/pull/2604) — mutation-checkpoint-verifier-trust (bundle 1, keystone).**
  Narrowed selection, path-qualified pinning resolution, **unparseable/unresolved now block the
  gate** (dud pins can no longer tick green), scoped-verification abort/timeout, stranded-mutation
  completion refusal. Hand-finished after `surviving_mutation_failed` (subprocess abort-path
  coverage) + two dud pins. Preceded by intent [#2602] + plan [#2603].
- **[#2609](https://github.com/cbrenner04/jarvis/pull/2609) — claude blindness fix.**
  Added `--include-partial-messages` to the claude adapter argv, so claude streams partial output
  and long no-tool turns (the review critic) stop tripping the 90s idle watchdog. **claude-first
  review now works end-to-end** (validated: bundle 2's intent critic reached its gate on
  claude-first without stalling). Preceded by intent [#2606] + plan [#2607].
- **[#2613](https://github.com/cbrenner04/jarvis/pull/2613) — bundle 2 branch 1/4:
  criteria-based-subspec-routing.** Routes implement to the first subspec with unticked
  non-human-only *criteria* (not index checkbox); shared `hasUncheckedNonHumanOnlyCriteria`
  predicate; stops `runLinkedImplementStep` re-resolving past the just-ticked subspec.

### Seed PRs

- [#2605] claude blindness (root-caused + implemented as #2609).
- [#2608] intent-workflow-lacks-stale-workspace-reset; pipeline-plan-stage-orphans-ready-intent.
- [#2611] implement-completion-commit-runs-formatter; mutation-checkpoint-criterion-must-name-enclosing-test.
- [#2614] mutation-verifier-resolves-from-full-bullet-block (the high-leverage one; do first next session).

## Not landed / handed off

- **Bundle 2 branches 2–4** (stale-workspace gates, iteration-timeout resume, no-work-settlement):
  not started. The pipeline intent **over-split** the deliberately-bundled seed into 4 coupled
  branches; closed the fan-out intent (#2610) + routing plan (#2612). Next session: run the
  remainder as ONE spec after the verifier fix. Seed remains on main.

## Dogfooding findings (all seeded or noted)

1. **Claude blindness** — root-caused to a missing `--include-partial-messages` flag; fixed (#2609).
2. **Mutation-checkpoint verifier reads only the criterion's first line** for resolution (selection
   reads the full block) → wrapped pinning-test refs go hollow and block completion. #2614. Highest
   day-to-day friction.
3. **Intent has no stale-workspace reset** → a killed intent strands a verdict marker that fails the
   next intent run non-retryably (blocked bundle 1's first pipeline attempt). #2608.
4. **Pipeline plan stage orphans its ready-intent** (branches from main, never deletes it). #2608.
5. **Implement commits unformatted code** → CI `check` (biome) fails; hand-fixed on #2604/#2609/#2613. #2611.
6. **Post-bundle-1 strict linker** needs the criterion to name its enclosing test. #2611.
7. **Pipeline marks the implement stage `failed` while the run is still live** (premature terminality,
   twice). Fresh evidence for existing `pipeline-stage-settlement-honesty`.
8. **Pipeline fan-out over-splits a bundled seed** into coupled parallel branches. Noted in queue.

## Process notes

- **Mutation-capture trap**: running the standalone verifier in the background then `git add -A`
  committed the applied mutations (routing shipped inverted guards, caught by subagent review).
  Never do that; commit first or use the daemon's implement run.
- **Blocking questions are expensive**: one AskUserQuestion sat idle ~3h48m. Default and proceed
  unless a decision is genuinely irreversible.
- Agent order ended **claude-first** (`claude,cursor`) — now safe post-#2609.

## Cost

`/cost` figures: *pending operator input.*
