- Define the commit owner and timing for every applicable surface: standalone writes commit after their durable completion; workflows commit only after all completion work, including hidden shrink — rules out committing an intermediate completed write loop.
- Define attribution when multiple bindings contribute and require the selected binding to provide `metadata.agent`, with explicit failure semantics when absent — rules out ambiguous attribution or silently malformed trailers.
- Define git-failure recovery after SQLite already records completion, including an actionable retry path — rules out permanent short-circuiting on the durable `completed` state.
- Define idempotency across commit success, process failure, and retry, including how Jarvis distinguishes its pending completion changes from unrelated later dirtiness — rules out duplicate commits or absorbing subsequent operator changes.
- Define the caller-visible result of post-completion git failure across foreground execution and daemon `list`/`wait`, while preserving the durable completion state — rules out contradictory success/failure reporting or SQLite rollback.
- Require the created commit SHA to be returned or otherwise exposed to the owning runner — rules out creating an addressable boundary that later publication and telemetry consumers cannot observe. Persistence and telemetry emission may remain deferred.
- Clarify recovery guarantees for staging, commit, and hook failures: tracked and untracked content must remain recoverable, with hook policy pinned if needed to uphold that guarantee — rules out interpreting “preserves changes” as only preserving one index topology.
- Add kill/abort explicitly to outcomes that never create a completion commit — rules out an uncovered terminal path.
- Scope parity documentation to the mechanics actually ported: whole-worktree staging, completion commit, and trailer convention — rules out implying parity for subject text, label selection, worktree lifecycle, push, or PR behavior.
