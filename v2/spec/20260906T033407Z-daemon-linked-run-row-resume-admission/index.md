# Daemon linked run-row resume admission and projection honesty

Daemon `reconstructWriteResume` still refuses paused `implement~link-N` rows, owning `implement~shrink` `completion_commit_failed` resume can return `{ ok: true }` without an attempt or log record, and `list`/`wait` `resumable`/`nextAction` can advertise `resume` on rows every resume verb refuses.

- [ ] [00 - Daemon linked paused row resume intake](./00-daemon-linked-paused-row-resume-intake.md)
- [ ] [01 - Owning write row completion_commit_failed admission](./01-owning-write-row-completion-commit-failed-admission.md)
- [ ] [02 - Resume projection honesty](./02-resume-projection-honesty.md)
