No required changes. The out-of-scope finding is valid as a systemic gap but is explicitly excluded by `intent.md`'s "Out of scope: The write-loop terminal-token contract (separate seed)" and does not bear on this spec's acceptance criteria, all of which concern placeholder resolution and reaching agent invocation — both of which the diff satisfies. The gap should be filed as a follow-up seed but is not this spec's responsibility to fix.