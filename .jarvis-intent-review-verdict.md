Reviewing the intent against spec guidance and repo conventions; checking documentation standards and related code context.


## Verdict

**Admission-only vs “resolves”.** Decisions say absent `pipeline` “resolves” to no selection but also “admission-only.” Today `readProjectPipelineConfig` returns `pipeline: undefined` when the key is absent, and `resolveProjectPipeline` treats that as `invalid-project-pipeline-config` (`project-pipeline-resolution.test.ts` “missing pipeline” row). The intent should state explicitly whether the fix is (a) `admitProjectPipeline` skips resolution when the key is absent—possibly via `'pipeline' in project` in the reader—and `resolveProjectPipeline` keeps rejecting bare `undefined`, or (b) the resolver’s contract changes and that unit test row moves. Without that, implementers can pick incompatible shapes.

**Failing-test AC naming.** AC1 and AC4 require a red/green test but never anchor a path (e.g. a named case in `workflow.test.ts` or a new `implement-workflow-steps` test). Spec guidance expects the subspec to name the test; the intent should at least point to the intended suite/file so plan mode doesn’t draft a vague “build-level test.”

**AC2 (preservation).** For unchanged valid-`pipeline` resolution, prefer citing the existing pin (e.g. `project-pipeline-resolution.test.ts` “resolves the configured source-owned definition…”) instead of paraphrasing, per refactor AC guidance.

**Docs.** `install-and-config.md` update is right (current “Each registered project must select a source-owned pipeline” contradicts optional absence). No `v1-behaviors.md` item needed for this v2-only config gate.

**Otherwise:** Problem, prerequisites, malformed-vs-absent decision (`{}` vs missing key), doc target, and invert AC for the absence guard are clear and appropriately scoped. No blocker if the resolver/admission seam and test anchor are tightened in plan draft.