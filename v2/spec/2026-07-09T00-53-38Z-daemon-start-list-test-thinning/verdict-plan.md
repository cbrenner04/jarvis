**Upheld findings requiring refinement:**

1. **Undecided fate of a second full-stack mapping test.** The spec names only one composed test to retain (`"list returns workflow step snapshots for live, stopped, and completed workflow-backed runs"`). If another test in the file exercises the same run/step-status → `terminalOutcome` mapping through the full list-handler stack, the spec must explicitly state its disposition — drop it as a subset of the retained composed test, or keep it and justify why it covers something the retained test doesn't (e.g., a distinct completed-run status branch). Leaving this implicit forces the implementer to guess, which conflicts with the intent's explicit requirement that "every dropped test [be] named with its surviving owner." Resolve by identifying the test by name and stating drop/keep/merge.

2. **No-logReader test's disposition must be explicit, not inferred from rationale prose.** The decision to consolidate the operator-error tests explains *why* no distinct behavior is lost (the file never wires a real `LogReader`, so both tests already hit the same store-only surface), but this reasoning is not the same as naming a surviving owner for `"list without logReader composes store-only error"` in the drop-list format the intent requires. Add an explicit line stating this test is dropped as redundant with the consolidated wiring test (which now covers the same store-only path) — no separate surviving owner needed beyond that consolidated test — so the PR-body verification criterion (test-count diff + named owner for every drop) is mechanically satisfiable without re-deriving the rationale.

**Rejected finding:**

3. The lack of a specified shared fixture builder for the new `stoppedOutcomeForRun` unit tests is not a spec gap. Per spec guidance, harness test specs should state observable behavior/coverage, not mandate internal test structure unless the structure is itself the contract — it isn't here, since the function's input type is already fixed by its `daemon.ts` signature. No refinement needed; leave fixture construction to the implementer.