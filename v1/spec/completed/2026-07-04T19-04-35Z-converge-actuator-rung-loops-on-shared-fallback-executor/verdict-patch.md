1. `v1/src/modes/patch/review.ts` / `v1/src/modes/review/review-invocation-binding.ts`: review-actuator fallback must not expand beyond the subspec’s contract. Only explicit `kind === "quota"` results and idle-timeout `error` results may advance to the next rung. Weak-quota upgrading for ordinary `error` results must not become part of actuator behavior. This is required because the subspec explicitly preserved terminal handling for `model_config` and all other errors, and the refactor was scoped as behavior-preserving.

2. `v1/src/modes/patch/shrink.ts`: shrink quota telemetry must preserve the pre-refactor/output contract the subspec names. Final-rung quota exhaustion cannot collapse the old per-rung telemetry shape into a single terminal row; the existing `quota-fallback` / `quota-exhausted` behavior must remain intact. This is required because the subspec explicitly called out unchanged `writeRung` rows as part of the preserved behavior, so telemetry shape is part of the observable contract and needs coverage that would fail on this regression.
