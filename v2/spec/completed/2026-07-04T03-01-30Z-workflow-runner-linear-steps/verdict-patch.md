1. **Empty-`runId` sentinel on full-workflow completion (confirmed at `v2/src/execution/workflow-runner.ts:101`).** When all steps complete, `executeWorkflow` returns `runId: ""` instead of the last step's actual run ID, even though `executeWriteLoop`'s result (in scope at that point) already carries the real ID. This breaks the one-step equivalence AC in subspec 01 ("A one-step `steps` array runs identically to today's single-step `executeWriteLoop` invocation, same terminal outcomes, same resume behavior") — a single-step workflow currently returns a different (empty) `runId` than calling `executeWriteLoop` directly would, which is a regression in the return contract, not a documented multi-step limitation.

   **Required outcome:** `executeWorkflow`'s completion path must return the actual `runId` from the last step's `executeWriteLoop` result (not a hardcoded `""`). Add/extend a test asserting the one-step workflow's `result.runId` matches what `executeWriteLoop` would return directly, satisfying the one-step equivalence AC.

No other adversary concerns require action — the migration test's use of the same-schema reopen pattern is consistent with this file's existing `004` migration test convention and doesn't need a synthetic legacy-schema fixture.