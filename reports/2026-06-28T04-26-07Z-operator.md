# Operator session — v2 Phase 3 (Daemon host + IPC + structured logging)

UTC 2026-06-27→28. Mandate: depart v1 feature work; drive **v2 Phase 3** end to end via the harness. Outcome: **Phase 3 complete** — all four intents merged, meta-index ticked.

## Shipped / merged

| PR | What | Agent(s) |
| --- | --- | --- |
| #721 | seed: phase-3-daemon-host | — |
| #722 | 4 ready-intents (intent split) | codex→cursor |
| #726 / #730 | structured-log-stream (plan / impl) | cursor / claude-haiku |
| #727 / #729 | daemon-host-ipc (plan / impl) | cursor+operator-verdict / claude-haiku |
| #732 / #733 | daemon-run-control-api (plan / impl) | claude-opus / claude-opus+operator-finalize |
| #740 / #741 | daemon-cli-control (plan / impl) | codex / codex |
| #728 | seed: verdict-survives-actuator-overreach | — |
| #742 | seeds: watchdog idle-timeout cascade (v1) + daemon-tests-use-real-daemon (v2) | — |
| #743 | chore: tick Phase 3, prune consumed seed | — |

Phase 3 delivers: structured run-keyed log stream (sink + tail/follow), hermetic Unix-socket IPC + daemon lifecycle + ownership registry, run-control API (start/list/tail/pause/resume/kill with graceful-pause core primitive + immediate-kill), and a thin `jarvis daemon|run` CLI. Core (`executeWriteLoop`) stayed daemon-unaware throughout.

## Cost

**Jarvis spend (runs.jsonl, this session): ≈ $23.93** across plan+run phases. Per intent (plan+impl, incl. retries): log-stream ≈ $3.01, host-ipc ≈ $9.70 (codex gpt-5.5 plan burned ~1.3M input tokens before falling to cursor), run-control-api ≈ $6.96 (haiku attempt + opus reimpl + finalize gates), cli-control ≈ $4.27. Cheap-tier first (codex/cursor) where available; opus only on the concurrency-heavy run-control spec.

**Operator (this Claude Code session): $45.41** (claude-opus-4-8; 23.8k in / 200.4k out / 70.8M cache read / 488.3k cache write; API 57m58s, wall 6h38m50s; 453 lines added / 68 removed). The orchestration loop — flake diagnosis and hand-finalize cycles — dominated total spend (~$45 operator vs ~$24 jarvis), as the runbook predicts. Full rows in operator-costs.csv / operator-outcomes.csv; per-spec in session-costs.csv / session-outcomes.csv.

## Workflow / harness observations

- **codex + cursor were rate-limited mid-session.** Per owner direction, flipped
  all agentOrders to claude-first (patch→haiku, plan→opus, review→sonnet),
  restored to codex-first when they returned. Backup at `.scratch/config.json.bak`.
- **cursor stalls under load → watchdog idle-timeout kills the run** (16–27 min
  lost twice) and does **not** advance the agent cascade. Seeded
  `watchdog-idle-timeout-advances-cascade` (#742).
- **Plan-mode review actuator edited the immutable `intent.md`**, failing
  validation and discarding the whole verdict. Hand-recovered #727; seeded
  `verdict-survives-actuator-overreach` (#728).
- **Plans can't fan out across a dependency chain** — an intent whose spec
  references a sibling's not-yet-merged interfaces blocks at plan time. Planned
  the leaves first, then each downstream tier. (Operator knowledge, captured in
  memory; correct behavior, not a bug.)
- **Gate contention**: 2 concurrent impl gates + a parallel v1 run produced
  phantom timeout failures in unrelated tests (triage, review). Re-running gates
  in isolation cleared them; the only *real* run-control failure was a guard-order
  bug. Reinforces the runbook's ~1–2-impl throttle.
- **v2 test-double drift**: `daemon-start-list.test.ts` reimplements the daemon
  inline; the copy diverged from `daemon.ts` and hid the guard-order bug. Seeded
  `daemon-tests-use-real-daemon` (#742).

## Would an existing v1 ready-intent have solved a session gotcha?

Owner-requested analysis. Mapping session gotchas against the ready-intents open at session start:

- **`jarvis-fix-commit-ready-order` — YES, directly.** Both host-ipc and
  run-control reached "spec complete" but failed the completion gate on
  **Biome format-only** errors, costing extra agent fix-up iterations and
  hand-run `bun run fix` in my finalizes. This ready-intent makes the gate run
  `bun run fix` *before* verification, so format-only reds self-heal. It landed
  mid-session (owner ran it on opus); runs after it merged no longer hit the
  format-fixup tax. Clearest "existing intent would've saved pain" hit.
- **`plan-cascade-advances-on-model-config` / `spawn-quota-before-model-config`
  — partial/adjacent.** They harden quota/model-config ordering in the cascade,
  but the cursor *stall* gotcha is a **watchdog idle-timeout**, which is terminal
  and never reaches cascade advancement — not covered. Hence the new
  `watchdog-idle-timeout-advances-cascade` seed.
- **`cursor-*` ready-intents (cost-measurement-spike, measured-usage-from-cli,
  no-unpriced-runs) — no.** All about cursor cost/usage accounting, not its
  stall/watchdog behavior, which is what actually cost me time.
- **All other open ready-intents** (closeout cost/outcome rows, opencode-ollama,
  register-codex-mini, remove-actuation-capability-floor,
  patch-run-writes-active-spec-path-marker, triage-finalize-*, plan-generated-
  spec-markdown-passes-lint-md, drop-aider) — **no** overlap with anything I hit.

Net: one existing ready-intent (`jarvis-fix-commit-ready-order`) addressed a recurring gotcha and was landed in-session; the remaining gotchas had no covering intent and produced two new seeds (#742) plus one earlier (#728).

## Deferred

- `run-completion-notify` (Phase 3 follow-up: `wait <runId>` push + cross-process
  follow wake) — owner deferred to its own session. Seed kept.
- End-of-session `jarvis1 cleanup` needs an interactive tty confirm (the `yes`
  pipe is ignored); merged worktrees from this session will be reaped at next
  session start, or run `! jarvis1 cleanup` manually.
