## Verdict — required outcomes

1. **`v2/docs/v1-behaviors.md:656` is now factually wrong and must be corrected.** That bullet still documents `revealSelectedAttentionTarget` as "a no-op when the target is absent from the selectable set (**the collapsed non-representative run-member case**)". With member-aware run matching in place, a collapsed pipeline-stage member *is* selectable under the provisional-selection guard and now reveals — this branch's own `tui-entry.test.tsx` change flips that exact assertion from no-op to real reveal. The general rule in the sentence still holds; only the cited example inverted. Outcome: the bullet must name a case that is still rejected (ad-hoc non-representative members, which have no expansion path) and state that a collapsed pipeline-stage member now reveals. This is a behavior change with a durable home the branch left misaligned; the repo standard requires alignment in the same subspec, and the subspec's own Documentation-updates intent ("the caller's expansion set is not written; ad-hoc unaffected") is only half-delivered without it.

2. **The stale in-code comment at `v2/src/tui/tui-entry.tsx:1016` must match.** It calls the ids the guard rejects "the deferred collapsed-member case" — precisely the case now admitted. Outcome: the comment names what the guard actually still rejects (ad-hoc non-representative members), so the next author working the Enter-reveal follow-on isn't misled by the first sentence they read.

3. **Bullet 599's unqualified "↑ retraces the nodes ↓ visited" must acknowledge revealed-member transience.** Because reveal deliberately writes nothing to `expandedPipelineNodeIds` (a Decision of this spec, not a defect), navigating off a revealed member re-collapses its stage and ↑ returns to the group representative rather than the member. The branch already edits this bullet; the qualifier belongs in that same hunk. No new regression test is owed — the behavior is the stated rule's consequence, only the absolute doc claim is wrong.

## Explicitly not required

- **Leave `runNodeMatchesSelection`'s identity arm in place.** It is subsumed by the member arm, but preserving pre-fix semantics as the leading disjunct is what makes the keystone directive a clean revert to identity-only comparison — the form the spec mandates. Do not restructure the predicate; three `@mutate` directives anchor on its current text.
- No changes to the tests, node depth conventions, or `selectNode`'s selectability asymmetry. The last Decision stops scope at the tree projection; input-binding behavior belongs with the follow-on Enter-reveal work.