Reviewing the implementation and docs against the completed subspec to issue a verdict.
## Verdict — required outcomes

1. **`v2/docs/telemetry-capture.md` — `cost_source` enum must match emitted values.** The new cursor provenance line documents `no-price` and `no-usage`, but the `cost_source` enum on the same page still lists only `"computed" | "estimated" | "unavailable" | null`. That contradicts `InvocationCompletedRecord` in `execute.ts` and the behavior this patch pins. The enum must include every provenance value the harness actually writes (at minimum `no-price` and `no-usage`).

2. **`v2/docs/v1-behaviors.md` ~403 — v1 vs v2 cursor usage/cost must be unambiguous.** The bullet now describes v2 shared finalize (`agent`-reported usage from terminal frames) inside a v1-sourced “Model and pricing visibility” section without a `[v2 divergence]` marker, while line 393 still correctly describes v1 text-mode estimation. Readers cannot tell which runtime the bullet applies to. The entry must clearly separate v1 estimation from v2 shared invocation and state both finalize branches operators see: `usage_source: "agent"` with `cost_source: "no-price"` when terminal `usage` is present; `usage_source: "unavailable"` with `cost_source: "no-usage"` when absent — consistent with `shared-invocation.md`.

3. **`intent.md` — align with the landed subspec.** Behavior, Decisions, and Acceptance criteria still describe the pre-fix finalize (no `cost_source: "no-price"` on the with-usage path; unchecked ACs). That contradicts the completed subspec and implementation. The intent contract must match the subspec’s finalized decisions so the spec package is internally consistent when the index marks the work done.

**No code or test changes required.** `finalizeCursorInvocationResult`, binding tests, guard-inversion pinning, and `shared-invocation.md` satisfy the subspec ACs. Gaps called out below are not blockers: present-but-empty `usage` handling (explicit subspec decision), no-usage telemetry via `executeWithQuotaFallback` (AC scoped to with-usage only), uneven no-warning assertions across no-usage fixtures, empty-usage binding hardening, and `v1-behaviors.md` line 405 v1 patch-mode notices vs v2 no-warning (documented in `shared-invocation.md`; line unchanged by this patch).