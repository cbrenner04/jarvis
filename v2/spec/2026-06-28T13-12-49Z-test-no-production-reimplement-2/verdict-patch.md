- Tighten the rule statement in [v2/docs/test-writing.md](/Users/christopherbrenner/Work/jarvis/.worktree/2026-06-28T13-12-49Z-test-no-production-reimplement-2/v2/docs/test-writing.md) so it explicitly applies only when an exported production seam can be exercised with injected fakes. The current section correctly defines the unit-under-test boundary and allows ordinary fakes/spies for external dependencies, but the lead rule should state that qualifier directly so the convention cannot be read as a broad anti-mocking rule. This is required by the subspec tasks and acceptance criteria.

- Rework the daemon run-control example so it documents the class of drift the expected `createRunControlHandlers` plus `daemon-start-list.test.ts` pattern avoids, without reconstructing specific prior mechanics or implying a required migration. The subspec requires an anti-pattern example, not a historical before-state; the durable doc should explain the outcome-focused mistake being avoided and keep the example scoped to documentation only.
