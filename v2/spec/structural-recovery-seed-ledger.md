# Structural-recovery traceability ledger

Supplemental to `structural-recovery-brief.md`. Tracks each `seeds/*.md` seed through the pipeline: seed → ready-intent(s) → plan → implement. **Point-in-time snapshot as of 2026-09-05 (compaction pass).** PR cells link to the GitHub PR that landed that stage; unverifiable cells are `—`. Regenerate by re-tracing seeds/ready-intents against the brief and `git log --grep`; session narratives live in `reports/`, not here.

A seed with multiple ready-intents spans one row per ready-intent; the `Seed` and `Status` cells are filled on the seed's first row only. A seed file is typically reaped once its `intent` run lands, so an already-split seed's ready-intents appear in the orphan table below rather than beside a seed row. Rows for reaped/landed seeds are kept for traceability.

## Seed ledger

| Seed | Status | Ready-intent | Plan | Implement |
| --- | --- | --- | --- | --- |
| pipeline-dispatch-shares-cli-front-door | **COMPLETE (4/4); seed reaped 2026-09-05** (audit: `FIXED_REVIEW_PASSES` in the resolver's forbidden list) | admit-pipeline-recovery-through-workflow-start | [20260831T031314Z](https://github.com/cbrenner04/jarvis/pull/3225) | [#3226](https://github.com/cbrenner04/jarvis/pull/3226) |
| | | dispatch-pipeline-stages-through-shared-preparation | [#3165](https://github.com/cbrenner04/jarvis/pull/3165) (hand-landed) | [#3170](https://github.com/cbrenner04/jarvis/pull/3170) |
| | | require-complete-pipeline-context | [#3148](https://github.com/cbrenner04/jarvis/pull/3148) | [#3155](https://github.com/cbrenner04/jarvis/pull/3155) |
| | | share-workflow-start-preparation | [#3141](https://github.com/cbrenner04/jarvis/pull/3141) | [#3143](https://github.com/cbrenner04/jarvis/pull/3143) |
| pipeline-settlement-derives-from-run-rows | **the active structural item** — slice 1 landed; absorbed the operator-killed (#2996) seed 2026-09-05; the restart-reconciliation half (#3030) shipped separately 2026-09-09 as [#3672](https://github.com/cbrenner04/jarvis/pull/3672); #3478 corrected the entry-row-only premise | durable-run-backed-stage-settlement | [#3165](https://github.com/cbrenner04/jarvis/pull/3165) (hand-landed) | [#3173](https://github.com/cbrenner04/jarvis/pull/3173) (archived) |
| | | canonical-pipeline-execution-state-and-stage-claims | **rewritten 2026-09-05** against landed claim machinery; next slice | — |
| | | daemon-terminal-run-stage-settlement | canonical landed 2026-09-06 | **in flight by hand 2026-09-10** — branch `hand/terminal-run-stage-settlement` (WIP pushed; production rewired, tests/docs pending; see brief P0 row) |
| implement-admits-externally-landed-specs (#3122) | **CLOSED 2026-09-02** — admit #3272 → route #3297 → chain #3350 → archive #3360/#3363 | — | — | — |
| all-spec-documents-external-capable | **COMPLETE 2026-09-06** — subspec 00 [#3534](https://github.com/cbrenner04/jarvis/pull/3534), subspecs 01-04 (fan-out lane failure incidents #3374 + docs) [#3543](https://github.com/cbrenner04/jarvis/pull/3543); both salvaged by hand. Subspec 00 originally landed (salvaged by hand after the daemon died mid-run; closes the resolution half of #3417/#3374). Subspecs 01-04 open. Previously in-flight — foundation [#3483](https://github.com/cbrenner04/jarvis/pull/3483); [[pipeline-external-chained-resolution]] spec active (plan [#3484](https://github.com/cbrenner04/jarvis/pull/3484), covers #3374) | — | — | — |
| split-workflow-runner-resume-machines | **COMPLETE 2026-09-03** (#3351, #3380, #3388, #3393) | — | — | — |
| split-daemon-run-control-handlers | **COMPLETE 2026-09-03** (#3311, #3312, #3364, #3379, #3386, #3392, #3394) | — | — | — |
| dead-export-and-test-seam-gates | intent-split → the hygiene RIs below | — | — | — |
| typed-step-stubs-and-bounded-spins | **DONE #3352** | — | — | — |
| cli-retire-write-and-legacy-aliases | **COMPLETE** — aliases #3356, TuiDaemonClient.start #3349, `jarvis write` #3445 (hand-landed; spec archived 2026-09-05) | — | — | — |
| cli-retire-run-start-pause-and-config | not-started (P2, sequenced: pause plumbing before `run start`) | — | — | — |
| prompt-corpus-dead-weight-sweep | landed via #3259/#3260/#3265 | — | — | — |
| plan-draft-rules-single-source | landed via #3317 (dedupe draft rules) | — | — | — |
| terse-review-role-prompts | landed via #3342/#3382 | — | — | — |
| declarative-prompt-fragment-policy | intent-split → `declarative-fragment-policy-single-assembler` RI | — | — | — |
| mechanical-cruft-pass | not-started (P3) | — | — | — |
| implement-owns-its-prompt-ids | intent-split → implement-owned-prompt-artifacts / v1-migration / v2-wiring RIs | — | — | — |
| per-project-agent-fallback-order | folded into [[per-project-config-overrides-seam]] 2026-09-05 (#3026); **seed file deleted 2026-09-09** (the umbrella carries the `projects.<key>.agents` override and #3026) | — | — | — |
| blocker-contract-credits-existing-section | **hand-landed 2026-09-09** — spec [#3668](https://github.com/cbrenner04/jarvis/pull/3668), impl [#3670](https://github.com/cbrenner04/jarvis/pull/3670); #3029 mechanism 1 fixed, 2 and 4 remain | — | — | [#3670](https://github.com/cbrenner04/jarvis/pull/3670) |
| pipeline-list-display-retention / tui-dock-command-grammar / tui-typed-run-steering | parked (display/TUI) | — | — | — |
| full-light-review-pipeline | **hand-landed 2026-09-07** (registry entry + tests + docs; seed reaped) | — | — | — |
| cleanup-improvements | not-started — **absorbed session-log retention, dead-daemon files, spec-scoped archival 2026-09-05** | — | — | — |
| ready-gate-repair-out-of-diff-edits | **fence policy decided 2026-09-05** (absolute fence + honest settlement, closes the #3040 dead-end); chain head before [[remove-ready-gate-repair-fence-bypass-from-production]] | — | — | — |
| implement-retirement-destroys-artifacts-before-materialization | not-started; fresh evidence #3433 | — | — | — |
| implement-resumes-stalled-unmerged-subspec-chain | not-started | — | — | — |
| notification-sweep-derives-bounded-incident-set | landed 3/5, CPU 73–98% → 4.6–15%; 03/04 residual | (see orphan table) | — | #3384, #3391, #3396 |
| structural-invariants-key-on-behavior-not-incidental-structure | in-flight — audit doc [#3485](https://github.com/cbrenner04/jarvis/pull/3485); **locator spec COMPLETE 8/8 [#3498](https://github.com/cbrenner04/jarvis/pull/3498) 2026-09-06**; the three `*-anchors` RIs are now unblocked | — | [#3442](https://github.com/cbrenner04/jarvis/pull/3442) (hand-landed), [#3447](https://github.com/cbrenner04/jarvis/pull/3447) | [#3498](https://github.com/cbrenner04/jarvis/pull/3498) |
| watchdog trio (idle-activity / stall-stdout / committed-progress) | **COMPLETE** #3218, #3227, #3189+#3194; sidecar filter #3278 | — | — | — |
| watchdog-timers-never-hold-the-event-loop | **COMPLETE 2026-09-06** ([#3511](https://github.com/cbrenner04/jarvis/pull/3511)); the lane's pins were vacuous (Bun `clearTimeout` alone drives `hasRef()` false) and were rewritten to record the `unref()` call, then mutation-verified at all three sites | — | [#3508](https://github.com/cbrenner04/jarvis/pull/3508) (hand-landed) | [#3511](https://github.com/cbrenner04/jarvis/pull/3511) |
| implement-publication-tail | **merged carrier 2026-09-05** (former implement-completes-without-publishing [verify-or-reap] + reuses-closed-same-branch-pr [live again 2026-09-03 via #3396]; includes the `defaultGhReadyFlip` state-filter gap) | — | — | — |
| plan-draft-contract-miss-reprompts-before-blocking | largely fixed [#3348](https://github.com/cbrenner04/jarvis/pull/3348) (detection bug); bare-filename residue + #3383 evidence | — | — | — |
| mutation-gate P0 chain | **COMPLETE 4/4** — #3188, #3197, #3202, #3195; containment #3211 | — | — | — |
| guard-flip-derivation-crash-is-contained | containment landed #3211; root-cause slice open | — | — | — |
| mutation-verifier-ignores-whitespace-only-line-changes | not-started | — | — | — |
| importer-cap-counts-realized-not-surface-total | **P0 dated fuse — CLOSED 2026-09-06** ([#3513](https://github.com/cbrenner04/jarvis/pull/3513)); `resolveKillingTests` returns early when co-located coverage exists, so the 200-cap can no longer brick a surface (v2/src was 165/200) | skip-importer-discovery-for-covered-guards [#3494](https://github.com/cbrenner04/jarvis/pull/3494) | [#3495](https://github.com/cbrenner04/jarvis/pull/3495) | [#3513](https://github.com/cbrenner04/jarvis/pull/3513) |
| sweep-dead-mutate-directives-from-test-corpus | **hand-landed 2026-09-09** — spec [#3651](https://github.com/cbrenner04/jarvis/pull/3651), sweep [#3654](https://github.com/cbrenner04/jarvis/pull/3654) | — | — | [#3654](https://github.com/cbrenner04/jarvis/pull/3654) |
| bind-verifier-spawns-to-run-termination | seeded [#3489](https://github.com/cbrenner04/jarvis/pull/3489) (the recurring CPU-orphan mode) | — | — | — |
| ci-test-scope-treats-root-docs-as-full / outcome-token-parsing-matches-blocked-in-prose / boundary-split-emits-near-duplicate-subspecs | **landed** — [#3660](https://github.com/cbrenner04/jarvis/pull/3660) and [#3663](https://github.com/cbrenner04/jarvis/pull/3663) (hand, 2026-09-09), [#3628](https://github.com/cbrenner04/jarvis/pull/3628) (2026-09-08) | — | — | — |
| dismiss-accepts-a-bulk-terminal-selection / notification-incidents-roll-up-to-the-invocation | not-started (small, independent); the roll-up seed now carries #3400's third session of evidence | — | — | — |
| intent-resume-consumes-its-seed | **hand-landed 2026-09-09** — spec [#3662](https://github.com/cbrenner04/jarvis/pull/3662), impl [#3665](https://github.com/cbrenner04/jarvis/pull/3665) (`landingInputs` persisted on the snapshot; legacy snapshots refuse resume) | — | — | [#3665](https://github.com/cbrenner04/jarvis/pull/3665) |
| plan-bases-off-a-declared-prerequisite-branch | not-started (#3437); with [[pipeline-fan-out-lanes-serial-chained-bases]] it is the structural answer to stage parallelism | — | — | — |
| abandon-refuses-unlanded-work-with-no-pr | not-started (#3437) | — | — | — |
| pipeline-fan-out-lanes-serial-chained-bases / pipeline-fan-out-per-lane-terminal-settlement | not-started | — | — | — |
| concurrent-load-suite-margin-check | **reaped 2026-09-09** — its prerequisite lane never landed, `LOAD_SENSITIVE_FILES` supersedes the isolation half, and the load-flake class it would have measured is owned by [[coscheduled-test-pair-strands-runs-terminally]] | — | — | — |
| superseded-pipeline-pr-hygiene | **merged carrier 2026-09-05** (former supersede trio); re-scope post-settlement | — | — | — |
| rename-pipeline-lane-* (×4) | **PRUNED 2026-09-05** — 474 `branchKey` sites / 0 `laneKey`, blocked behind settlement; terminology churn | — | — | — |
| linked-run-rows-resume-and-settle-uniformly | **P0 — head lane landed 2026-09-06** ([#3514](https://github.com/cbrenner04/jarvis/pull/3514)): shared matcher, execution-loop adoption, #3395 resume repair, linked-row paused reconstruction. Daemon `reconstructWriteResume` intake for `~link-N` is deferred by the spec's own scope → `daemon-linked-run-row-resume-admission` RI still open (#3463 may persist) | write-sibling-step-id-matcher [#3497](https://github.com/cbrenner04/jarvis/pull/3497) | [#3503](https://github.com/cbrenner04/jarvis/pull/3503) | [#3514](https://github.com/cbrenner04/jarvis/pull/3514) |
| | | daemon-linked-run-row-resume-admission | plan hand-landed [#3524](https://github.com/cbrenner04/jarvis/pull/3524); implement salvaged by hand 2026-09-07 — pipeline gate approved, plan stage died on [[fan-out-plan-resolution-is-all-or-nothing-across-lanes]]; standalone plan then blocked `contract_miss` on [[plan-contract-classifies-the-rules-out-clause]] | in-flight |
| pipeline-restart-discards-disposable-stage-state | **P0 operator ask — head lane landed 2026-09-06** ([#3512](https://github.com/cbrenner04/jarvis/pull/3512)): shared stale-reset gains the path-scoped unlanded-commits refusal and `disposableLane`, which bypasses only descendant + landed-criteria. Restart caller wiring still open | stale-reset-disposable-lane-retirement-gates [#3496](https://github.com/cbrenner04/jarvis/pull/3496) | [#3501](https://github.com/cbrenner04/jarvis/pull/3501) | [#3512](https://github.com/cbrenner04/jarvis/pull/3512) |
| | | pipeline-restart-discards-disposable-stage-state (caller lane) | [#3523](https://github.com/cbrenner04/jarvis/pull/3523) **2026-09-07** (standalone; pipeline gate approved but plan stage died on [[fan-out-plan-resolution-is-all-or-nothing-across-lanes]]) | in-flight |
| superseded-daemon-releases-run-ownership | NEW 2026-09-05 (#3464) | — | — | — |
| intent-split-covers-sibling-repo-surfaces | NEW 2026-09-05 (#3439) | — | — | — |
| implement-respects-target-repo-doc-layout | NEW 2026-09-05 (#3426) | — | — | — |
| implement-base-fails-closed-on-stale-local-main | **hand-landed 2026-09-09** — spec [#3671](https://github.com/cbrenner04/jarvis/pull/3671), impl [#3673](https://github.com/cbrenner04/jarvis/pull/3673) closed #3381 | — | — | [#3673](https://github.com/cbrenner04/jarvis/pull/3673) |
| quota-classification-covers-every-step-role | **half landed 2026-09-09** — precedence (auth → quota → transient) [#3669](https://github.com/cbrenner04/jarvis/pull/3669) via spec [#3668](https://github.com/cbrenner04/jarvis/pull/3668); the stranded-`paused` incident half stays seeded (#3372 defect 2) | — | — | [#3669](https://github.com/cbrenner04/jarvis/pull/3669) |
| detached-pipeline-plan-stage-consumes-ready-intents | NEW 2026-09-05 (#3041); the `provenIntentPrune` slug half carved out and shipped 2026-09-09 ([#3656](https://github.com/cbrenner04/jarvis/pull/3656) → [#3657](https://github.com/cbrenner04/jarvis/pull/3657)); plan-stage consumption half open | — | — | — |
| per-project-config-overrides-seam | NEW 2026-09-05 (umbrella: #3026, #3150-residue, the readyCommand cascade class) | — | — | — |
| implement-admission-persists-its-run-row | **NEW 2026-09-06** — detached implement printed a run id whose row was never persisted; workflow died `Run … not found` in the daemon log only, no durable row, no incident, lane silently lost | — | — | — |
| notification-delivery-cursor-is-exclusive | **hand-landed 2026-09-09** (spec [#3649](https://github.com/cbrenner04/jarvis/pull/3649), impl [#3652](https://github.com/cbrenner04/jarvis/pull/3652)). Was: **NEW 2026-09-06** — `notifications wait --since <deliveryCursor>` re-delivers the incident at that cursor forever, so the documented wake-chaining protocol is a fixed point; `list` shares the inclusive bound | — | — | — |
| pipeline-cli-discovers-daemons-like-run-list | **NEW 2026-09-06** ([#3505](https://github.com/cbrenner04/jarvis/pull/3505), amended [#3506](https://github.com/cbrenner04/jarvis/pull/3506)) — every `pipeline` verb and `cleanup --abandon` hit the invoking digest socket only, so a merge blinds them while the owning daemon is healthy; the CLI then auto-starts a daemon that supersedes it. Hit four times this session | — | — | — |
| pipeline-recover-resolves-past-approval-gates | **NEW 2026-09-06** ([#3507](https://github.com/cbrenner04/jarvis/pull/3507)) — recover reads position `n-1`, always an approval gate on `full-review`, so it can never run there | — | — | — |
| skipped-successor-strands-a-recovered-lane | **NEW 2026-09-06** ([#3515](https://github.com/cbrenner04/jarvis/pull/3515)) — operator evidence: chess `a00ca258` / `board-game-end-view-model`. A branch re-driven to `succeeded` keeps its `skipped` successor; branch resume admits only through a `failed` row | — | — | — |
| fan-out-plan-resolution-is-all-or-nothing-across-lanes | **NEW P0 2026-09-07** ([#3522](https://github.com/cbrenner04/jarvis/pull/3522)) — approving a fan-out lane re-verifies *every* sibling ready-intent; a sibling consumed by its own successful plan can never verify again, so no fan-out pipeline advances past its first lane. Symmetric on `d2029fbd` and `14108361`, <25s, no run row | — | — | — |
| gate-allowset-derivation-fails-on-external-spec-home | **NEW 2026-09-07** ([#3525](https://github.com/cbrenner04/jarvis/pull/3525)) — #3423 root-caused, shares a root with #3417: `resolveSpecScopeRoot` succeeds on an external absolute `specPath`, so the graceful `scopeRoot === null` fallback is unreachable and every enumerated path is rejected `../`-prefixed; eight `undefined` returns collapse to one unlogged string | — | — | — |
| plan-contract-classifies-the-rules-out-clause | **NEW 2026-09-07** ([#3525](https://github.com/cbrenner04/jarvis/pull/3525)) — the surface classifier reads a Decisions bullet's *mandated* `rules out X` clause as a second surface, so the gate is hostile to the format it requires; structural sibling of #3383's lexical over-match | — | — | — |
| agent-confinement-is-per-vendor-and-unexpressed | **NEW 2026-09-07** ([#3531](https://github.com/cbrenner04/jarvis/pull/3531)) — re-seeds #1453, the last unseeded intake issue; narrows the 2026-07-21 triage, which understated cursor's own sandbox and claude's permission mode. Confinement is an emergent property of which quota rung answered and is recorded nowhere | — | — | — |
| pr-probe-failure-must-not-authorize-destruction | **NEW P0 2026-09-07** ([#3532](https://github.com/cbrenner04/jarvis/pull/3532)) — `gateOnOpenPrs` reads a failed `gh` probe as "no open PRs", so an inconclusive answer becomes a permissive one. Silently disables `--abandon`'s ready-PR guard and now gates disposable-lane worktree destruction; reachable by default because `gh` false-negatives under the agent sandbox | — | — | — |
| daemon-start-reclaims-its-own-leftover-socket | **landed** — subspec 00 2026-09-07, subspecs 01-03 hand-landed 2026-09-09 [#3637](https://github.com/cbrenner04/jarvis/pull/3637). Was: **NEW P0 2026-09-07** ([#3533](https://github.com/cbrenner04/jarvis/pull/3533)) — an abrupt daemon death leaves an unbound socket file and every later `daemon start` fails `EADDRINUSE`, permanently. `absent` and "free to bind" are treated as one claim. `jarvis cleanup` classifies the same socket correctly, so the judgement exists but is not on the startup path | — | — | — |
| subspec-inventory-relativizes-against-the-wrong-root | **NEW P0 2026-09-07** ([#3536](https://github.com/cbrenner04/jarvis/pull/3536)) — `buildSubspecCompletionInventory` relativizes worktree-resolved subspecs against `projectRoot`, so every entry is dropped and both lists are always empty. `hasCompletedSubspec` gates `resumable`, so `iteration_timeout` is non-resumable by construction for every implement lane — the documented recovery can never fire | — | — | — |
| v1-and-v2-read-agent-order-from-different-config-keys | **RETIRED 2026-09-08 (v1 frozen)** — the whole premise was two engines reading different keys. Only v2 remains and it reads the flat `agents` list; stale `modes.*.agentOrder` keys are inert leftovers already documented in the runbook. Seed deleted | — | — | — |
| daemon-stop-refusal-checks-run-liveness | **hand-landed 2026-09-09** (spec [#3653](https://github.com/cbrenner04/jarvis/pull/3653), impl [#3655](https://github.com/cbrenner04/jarvis/pull/3655): liveness-aware `stopDaemon`, reconciliation after exit). Was: **RE-SEEDED 2026-09-07** ([#3530](https://github.com/cbrenner04/jarvis/pull/3530)) — was `a-daemon-lost-run-row-deadlocks-the-daemon`, lost to the same purge; `daemon-lifecycle.ts:212-216` still refuses on non-terminal status with no liveness check, and the only recorded recovery is a destructive shared `kill -9` | — | — | — |
| retire-claude-pool-contention-folklore | **RETIRED 2026-09-08 (v1 frozen)** — the warning lives in `v1/src/modes/patch/pool-contention.ts`, which is frozen and never runs; its ACs required the deleted `test:v1` scripts. Seed deleted; the measured guidance stays in the runbook's § Choosing an actuator | — | — | — |
| coscheduled-test-pair-strands-runs-terminally | **NEW P0 2026-09-06** ([#3542](https://github.com/cbrenner04/jarvis/pull/3542), amended [#3544](https://github.com/cbrenner04/jarvis/pull/3544)) — `workflow.test.ts` and `diff-derived-mutation-verifier.test.ts` pass alone and fail together at load 1.7; the base-ref probe re-runs the same pair, correctly concludes "reproduces on base", and settles `ready_gate_out_of_scope` terminal. Baseline showed `main` itself fails `test:v2` (2) and a branch touching neither file fails 3 — a rotating set, all at exactly 30000ms, all green in isolation. CI is green only because it scopes by path | — | — | — |
| ready-command-reaches-the-step-that-runs-the-gate | **landed 2026-09-08** — [#3613](https://github.com/cbrenner04/jarvis/pull/3613) (stamp), [#3614](https://github.com/cbrenner04/jarvis/pull/3614) (persist), [#3616](https://github.com/cbrenner04/jarvis/pull/3616) (finalization resolves); #3598 commented, safe to close. Was: **NEW 2026-09-08** ([#3598](https://github.com/cbrenner04/jarvis/issues/3598)) — `stampWorkflowStepsWithMachineConfig` applies `readyCommand`/`fixCommand` to write steps only, so the review-step finalization gate resolves `undefined` and falls back to `bun run ready`. Any project whose gate is not a bun script finishes all work, opens its PR, then fails the tail as `ready_gate_command_missing` / `fix_config` — pointing at config that is already correct. Confirmed in source and on run `473417b6` (`iterationsConsumed: 0`, a gate-only tail on a non-write row). Exposed only under `terminalAction: "ready"` | — | — | — |
| implement-gate-invocation-outlives-the-iteration-ceiling | **landed** — budget accounting [#3617](https://github.com/cbrenner04/jarvis/pull/3617) (2026-09-08), slot ownership [#3647](https://github.com/cbrenner04/jarvis/pull/3647) (hand, 2026-09-09). Was: **NEW P0 2026-09-06** — concurrent implement lanes each run the full suite inside their own 45-minute iteration budget; two lanes died at 45m01s/45m02s with only the `bun run test:v2` AC outstanding. Because that AC is ticked last and consumes the budget, `hasCompletedSubspec` is false and the timeout is non-resumable — complementary to [[subspec-inventory-relativizes-against-the-wrong-root]], not fixed by it | — | — | — |
| run-kill-reports-success-without-killing | **NEW P0 2026-09-06** — `run kill` and `run kill --force` both print `killed` and exit 0 while the row stays `in-progress`/`live` and the hung child keeps running; settlement waits on quiescence and quiescence waits on the child termination was meant to reap. Removes the documented recovery for a stalled run | — | — | — |
| mutation-verifier-hangs-forever-on-a-non-terminating-mutant | **NEW P0 2026-09-06** — a `guard-flip: === → !==` on the exit condition of a `while (true)` loop is non-terminating; the verifier waits for it forever, pegging a core 2.5h, wedging the run `live` with no agent, defeating `run kill`, and leaving an inverted production guard in the worktree because restore only runs after the test returns. The `*-anchors` scanners are all written in this loop shape | — | — | — |
| self-parsing-structural-tests-can-bind-to-their-own-fixtures | **NEW 2026-09-06** — autofix renamed a locator's inventory constant (correctly: it is only read by parsing the file's own source, so lint sees it unused); the regex then fell through to a fixture at line 529, parsing 4 anchors instead of the real 6. Both loud guards (not-found, empty) stay silent because a fixture is well-formed and non-empty | — | — | — |
| pipeline-list-prints-an-id-no-verb-accepts | **hand-landed 2026-09-09** — spec [#3664](https://github.com/cbrenner04/jarvis/pull/3664) (index and seed reap landed with the impl), impl [#3667](https://github.com/cbrenner04/jarvis/pull/3667): daemon-side prefix resolver, `pipeline_id_ambiguous`, unique-prefix listing | — | — | [#3667](https://github.com/cbrenner04/jarvis/pull/3667) |
| killing-test-budget-is-shorter-than-the-test-it-runs / wal-lock-handshake-deadline-strands-runs | **hand-landed 2026-09-09** — spec [#3646](https://github.com/cbrenner04/jarvis/pull/3646); budget derives from the unmutated baseline [#3650](https://github.com/cbrenner04/jarvis/pull/3650), WAL handshake deadline removed [#3648](https://github.com/cbrenner04/jarvis/pull/3648) | — | — | [#3650](https://github.com/cbrenner04/jarvis/pull/3650), [#3648](https://github.com/cbrenner04/jarvis/pull/3648) |
| test-seam-guard-verified-against-real-source | **hand-landed 2026-09-09** — spec [#3643](https://github.com/cbrenner04/jarvis/pull/3643), AST-based guard [#3645](https://github.com/cbrenner04/jarvis/pull/3645); [#3588](https://github.com/cbrenner04/jarvis/pull/3588) merged behind it | — | — | [#3645](https://github.com/cbrenner04/jarvis/pull/3645) |
| gate-invocation-slot-has-no-owner | **hand-landed 2026-09-09** — spec [#3644](https://github.com/cbrenner04/jarvis/pull/3644), impl [#3647](https://github.com/cbrenner04/jarvis/pull/3647) (lease-owned slot, `MAX_CONCURRENT_AGENT_GATE_INVOCATIONS` enforced) | — | — | [#3647](https://github.com/cbrenner04/jarvis/pull/3647) |
| restart-reconciliation-preserves-paused-runs (from #3030, no seed) | **hand-landed 2026-09-09** — spec [#3668](https://github.com/cbrenner04/jarvis/pull/3668), impl [#3672](https://github.com/cbrenner04/jarvis/pull/3672): `beginRunReconciliation` admits `paused`/`budget-soft-stopped` only with an open attempt | — | — | [#3672](https://github.com/cbrenner04/jarvis/pull/3672) |
| chained-implement-cannot-tick-its-own-criteria | **NEW P0 2026-09-09** ([#3688](https://github.com/cbrenner04/jarvis/pull/3688), amended [#3690](https://github.com/cbrenner04/jarvis/pull/3690)) — a chained implement stage reads its spec from the prior stage's worktree while the agent writes in the implement worktree; the criteria-ticked contract reads the former. Three lanes, three settlement shapes (`missing_blocker`, `gate_invocation_refused`, `contract_miss`), one cause. Lane 3 ticked 15/15 in the implement worktree while the plan worktree read 0/15. Standalone implement of the same spec completes end-to-end, so the failure is the chained stage alone | — | — | — |
| pipeline-implement-stage-cannot-clear-base-behind-origin | **NEW 2026-09-09** ([#3685](https://github.com/cbrenner04/jarvis/pull/3685)) — #3673's `base_behind_origin` refusal names `--base origin/main`, which a pipeline stage has no way to pass. Any merge while a pipeline is live fails its implement stage at admission, and merging stage PRs is how a pipeline lands its own work | — | — | — |
| implement-publishes-its-review-verdict-into-the-spec-tree | **landed 2026-09-09** — spec [#3684](https://github.com/cbrenner04/jarvis/pull/3684), impl [#3689](https://github.com/cbrenner04/jarvis/pull/3689) (`completionStageArgs` excludes `verdict-*.md` at any depth, never `rm --cached`s a tracked one); the sibling `implement-review-verdict-resolves-outside-the-spec-tree` lane stays open | — | [#3684](https://github.com/cbrenner04/jarvis/pull/3684) | [#3689](https://github.com/cbrenner04/jarvis/pull/3689) |
| verifier-group-sigkill-escalation-dies-with-its-parent | **landed 2026-09-09** — spec [#3683](https://github.com/cbrenner04/jarvis/pull/3683), impl [#3691](https://github.com/cbrenner04/jarvis/pull/3691): referenced escalation timer plus awaited `ESRCH` confirmation. Closes the orphan leak #3578 did not (it bounded the waiting, not the leak). Residual risk recorded on the PR: the confirmation poll is unbounded by spec decision, trading a leak for a hang if SIGKILL ever fails. The sibling `prove-mutation-verifier-reaps-bounded-tests` lane stays open | — | [#3683](https://github.com/cbrenner04/jarvis/pull/3683) | [#3691](https://github.com/cbrenner04/jarvis/pull/3691) |
| pipeline-cli-discovers-daemons-like-run-list | **head lane landed 2026-09-09** — spec [#3686](https://github.com/cbrenner04/jarvis/pull/3686), impl [#3692](https://github.com/cbrenner04/jarvis/pull/3692) (`pipeline_owner` RPC + resolution across live sockets; verbs adopt it in the two open sibling lanes). Reproduced twice live this session, both times right after merging a `v2/src` change | — | [#3686](https://github.com/cbrenner04/jarvis/pull/3686) | [#3692](https://github.com/cbrenner04/jarvis/pull/3692) |
| invocation-error-reports-the-prompt-and-records-nothing | **head lane landed 2026-09-09** — spec [#3687](https://github.com/cbrenner04/jarvis/pull/3687), impl [#3693](https://github.com/cbrenner04/jarvis/pull/3693) (pre-result binding failures normalized so every entered rung leaves an attributed telemetry row). Two sibling lanes (persist evidence, report attributed errors) stay gated behind it. Note the 2026-09-08 `invocation_error` was **not deterministic**: the same ready-intent planned normally today | — | [#3687](https://github.com/cbrenner04/jarvis/pull/3687) | [#3693](https://github.com/cbrenner04/jarvis/pull/3693) |
| cleanup-archives-hand-landed-specs | **NEW 2026-09-09** ([#3658](https://github.com/cbrenner04/jarvis/pull/3658)) — stranded archival keys on a durable run row, so a spec written and implemented by hand is skipped every pass (`no durable implementation branch`); 17 specs archived by hand in three PRs this session | — | — | — |

## Orphan / standalone ready-intents

Ready-intents whose originating seed is no longer on disk. `Origin intent` gives the intent PR that produced the ready-intent.

| Ready-intent | Status | Origin intent | Plan | Implement |
| --- | --- | --- | --- | --- |
| daemon-terminal-run-settlement | merged | [#3074](https://github.com/cbrenner04/jarvis/pull/3074) | [#3140](https://github.com/cbrenner04/jarvis/pull/3140) | [#3145](https://github.com/cbrenner04/jarvis/pull/3145) |
| execution-terminal-run-settlement-invariant | 00 #3157, 01 hand-salvaged #3167; 02 deferred | [#3074](https://github.com/cbrenner04/jarvis/pull/3074) | [#3149](https://github.com/cbrenner04/jarvis/pull/3149) | [#3157](https://github.com/cbrenner04/jarvis/pull/3157), [#3167](https://github.com/cbrenner04/jarvis/pull/3167) |
| inject-spec-guidance-agent-core / split-spec-guidance-documents | landed (#3253/#3255) | [#3094](https://github.com/cbrenner04/jarvis/pull/3094) | — | [#3253](https://github.com/cbrenner04/jarvis/pull/3253), [#3255](https://github.com/cbrenner04/jarvis/pull/3255) |
| daemon-start-sweeps-orphan-gate-children | **landed #3416/#3431** (gate group only; sibling spawns → [[bind-verifier-spawns-to-run-termination]]) | [#2828](https://github.com/cbrenner04/jarvis/pull/2828) | — | [#3431](https://github.com/cbrenner04/jarvis/pull/3431) |
| daemon-resume-honors-injected-config-path | held (P3; plan #3067 closed sandbox-unrunnable) | [#3063](https://github.com/cbrenner04/jarvis/pull/3063) | #3067 (closed) | — |
| cli/daemon/execution-loop-structural-invariant-test-anchors (×3) + structural-invariant-test-writing-docs | blocked on the locator spec (active, 0/8) | [#3438](https://github.com/cbrenner04/jarvis/pull/3438) | — | — |
| add-v2-dead-export-hygiene-gate / sweep-v2-unreferenced-exports / exclude-test-support-from-production-glob / generalize-production-test-seam-guard | not-started (hygiene family from [[dead-export-and-test-seam-gates]]) | — | — | — |
| dedupe-cli-cruft / dedupe-daemon-cruft / dedupe-execution-loop-cruft | not-started (one shape, three surfaces — plan as one chain) | — | — | — |
| implement-owned-prompt-artifacts → v1-implement-prompt-id-migration → v2-implement-prompt-wiring → implement-prompt-operator-documentation | not-started, strictly serial; operator-doc RI **absorbed document-review-role-prompt-families 2026-09-05** | — | — | — |
| declarative-fragment-policy-single-assembler | not-started | — | — | — |
| merge-publication-resume-twins-compute-resumable | **pure dedup** — its bug landed as #3327; demoted | — | — | — |
| remove-ready-gate-repair-fence-bypass-from-production | sequenced behind [[ready-gate-repair-out-of-diff-edits]] (2026-09-05 fence decision) | — | — | — |
| canonical-pipeline-execution-state-and-stage-claims | rewritten 2026-09-05; next settlement slice | — | — | — |
| cleanup-external-spec-home-lifecycle | not-started | — | — | — |
| align-docs-after-write-retirement | not-started (docs sweep deferred from the retire-write spec) | — | — | — |
| mutation-verifier-prompt-registry-surface | not-started | — | — | — |

## Compaction (2026-09-05)

Executed the 2026-09-05 queue audit plus the operator's restart-pain report: merged the supersede trio → [[superseded-pipeline-pr-hygiene]], the publication-tail pair → [[implement-publication-tail]], the two cleanup-reap seeds + repo-wide-archival gap → [[cleanup-improvements]]; folded the two superseded settlement seeds' evidence into [[pipeline-settlement-derives-from-run-rows]] and deleted them; pruned the four rename-lane seeds; rewrote [[canonical-pipeline-execution-state-and-stage-claims]]; decided the ready-gate fence direction (absolute + honest settlement) and sequenced its chain; merged the two prompt-doc RIs; ticked + archived the retire-jarvis-write spec (#3445); added nine seeds (see table); closed issues #3461, #3465, #3397, #3039, #3368 as verified fixed. Brief rewritten to a standing document — session history lives in `reports/`.

## Session 2026-09-06

**Parallelization.** Up to seven concurrent lanes (four implements plus three pipeline stages) on an idle-start machine, load 6-21, zero watchdog false-kills. Two ceilings found, neither a lane-count limit:

- A `shared/**` implement's ready gate runs all six slices and cannot share the machine. The locator gate went red on `v1/test/run.test.ts` and `snapshot-update-retest-runner.test.ts` timing out at 30s while CI passed the identical commit in 11m43s.
- `ready_gate_out_of_scope` can be manufactured by load: the base-ref reproduction probe flaked the same test it was checking, classifying a healthy run's failure as pre-existing and settling it terminal `stop` ([#3513](https://github.com/cbrenner04/jarvis/pull/3513)'s lane).

**Merging during live work poisons every in-flight lane.** Four lanes branched before a mid-session merge and their review/repair steps then rewrote files those merges had landed; two would have reverted [#3499](https://github.com/cbrenner04/jarvis/pull/3499). The ready-gate repair fence caught one independently (`Ready-gate repair stages path outside run diff and spec tree: …daemon.ts, …write-loop-binding-source-guard.test.ts`) — the fence is doing real work today. Every such lane was salvaged by re-applying only the files it actually changed onto current main.

**Landed (16 PRs).** P0s closed: importer-cap fuse [#3513](https://github.com/cbrenner04/jarvis/pull/3513), linked-row matcher head lane [#3514](https://github.com/cbrenner04/jarvis/pull/3514), disposable-lane gates head lane [#3512](https://github.com/cbrenner04/jarvis/pull/3512). Also [#3498](https://github.com/cbrenner04/jarvis/pull/3498) locator (8/8, unblocks the three `*-anchors` RIs), [#3499](https://github.com/cbrenner04/jarvis/pull/3499), [#3500](https://github.com/cbrenner04/jarvis/pull/3500), [#3510](https://github.com/cbrenner04/jarvis/pull/3510), [#3511](https://github.com/cbrenner04/jarvis/pull/3511), stage PRs [#3494](https://github.com/cbrenner04/jarvis/pull/3494)-[#3497](https://github.com/cbrenner04/jarvis/pull/3497), [#3501](https://github.com/cbrenner04/jarvis/pull/3501), [#3503](https://github.com/cbrenner04/jarvis/pull/3503), [#3504](https://github.com/cbrenner04/jarvis/pull/3504), [#3508](https://github.com/cbrenner04/jarvis/pull/3508), and seeds [#3502](https://github.com/cbrenner04/jarvis/pull/3502), [#3505](https://github.com/cbrenner04/jarvis/pull/3505)-[#3507](https://github.com/cbrenner04/jarvis/pull/3507), [#3515](https://github.com/cbrenner04/jarvis/pull/3515).

**Queue hygiene.** Cleanup archived all eight specs landed today and pruned 19 stale refs; only `pipeline-external-chained-resolution` (0/5) stays open. Three ready-intents consumed by pipeline plan stages survived two cleanup passes — `provenIntentPrune` looks up `ready-intents/${spec.name}.md` where `spec.name` is the timestamped directory basename while ready-intents are slug-named, so the prune is unreachable on any `plan.specTimestamp: true` project. Pruned by hand and recorded on [[detached-pipeline-plan-stage-consumes-ready-intents]] ([#3518]({U}3518)). 166 terminal runs and 2 terminal pipelines dismissed at close; the two `awaiting-approval` pipelines are kept because each holds the gate for a P0 dependent lane.

**Vacuous-test catch.** The watchdog lane's three pins passed with `.unref?.()` deleted: in Bun, `clearTimeout` alone drives `hasRef()` to false and all three sites clear on settle, so the assertion could never fail. Rewritten to record the `unref()` call and mutation-verified at each site. The plan had specified `hasRef()` after settle, and an earlier operator correction (fake-timer handles lack `hasRef()`) fixed a different problem — neither caught this.

**Circuit-breaker fired once.** The watchdog plan lane needed two interventions (`contract_miss` on an orphaned first draft, then `invocation_error` on the adjudicator) and was hand-landed ([#3508](https://github.com/cbrenner04/jarvis/pull/3508)). Because [[pipeline-recover-resolves-past-approval-gates]] makes recover unusable, `resume` redrafted and discarded a correct decision a human had to re-derive.

## Session 2026-09-07

**Fan-out pipelines are structurally single-lane.** Both P0 dependent lanes were sitting at `approve-intent` on `d2029fbd` / `14108361`. Approving each gate dispatched its plan stage, which failed in **under 25 seconds** with no run row and no agent invocation, each naming the *head* lane's ready-intent — consumed into a spec tree when that lane planned. `resolveChainedReadyIntentPaths` (`pipeline-stage-resolve.ts:344`) verifies the whole `downstreamInputs` list and returns the first failure for the entire fan-out; `resolvePlanStage` never reads a `branchKey`, and lane↔input binding is an unguarded positional index (`pipeline-execution.ts:1988`). Seeded [#3522](https://github.com/cbrenner04/jarvis/pull/3522). Both lanes' `approve-plan` rows then flipped `skipped` — [[skipped-successor-strands-a-recovered-lane]] — so neither was reachable by any verb. Both driven standalone instead.

**The plan-contract gate condemns its own mandated format.** Standalone plan on `daemon-linked-run-row-resume-admission` (`ed1b6471`) blocked `contract_miss` on a single-surface daemon decision. Classified by hand: the whole bullet is `["daemon","execution-loop"]`, the decision clause alone is `["daemon"]`, and `execution-loop` comes *only* from `rules out write-loop re-entry`. Every well-formed Decisions bullet ends with a `rules out` clause, so every one is exposed. Seeded [#3525](https://github.com/cbrenner04/jarvis/pull/3525); distinct from #3383's lexical over-match. The draft was sound (three atomic subspecs, prerequisites correct, ACs naming tests that exist at the cited lines) and was hand-landed [#3524](https://github.com/cbrenner04/jarvis/pull/3524) under the circuit-breaker — second consecutive session the plan lane tripped this gate.

**#3423 root-caused, and it is #3417.** `resolveSpecScopeRoot` succeeding on an external absolute `specPath` makes the graceful fallback unreachable, so spec-tree enumeration rejects every path as `../`-prefixed and the fence dies opaquely *after* the branch is pushed and the PR published. Seeded [#3525](https://github.com/cbrenner04/jarvis/pull/3525).

**Machine.** Session opened at load 33.6 with two orphaned `bun test` processes (3h20m and 2h33m, 98% CPU each, parented to `launchd`) in worktrees whose specs had long landed — the [[bind-verifier-spawns-to-run-termination]] mode again. Heavy fan-out was held until they were cleared.

## Session 2026-09-06 (late)

**Fan-out pipelines are structurally single-lane.** Approving either P0 dependent lane's gate failed its plan stage in under 25s with no run row, each naming the *head* lane's consumed ready-intent. `resolveChainedReadyIntentPaths` verifies the whole `downstreamInputs` list all-or-nothing and `resolvePlanStage` never reads a `branchKey`; lane binding is an unguarded positional index. Seeded [#3542](https://github.com/cbrenner04/jarvis/pull/3542)'s sibling [#3522](https://github.com/cbrenner04/jarvis/pull/3522). Both lanes driven standalone instead; both landed ([#3535](https://github.com/cbrenner04/jarvis/pull/3535), [#3538](https://github.com/cbrenner04/jarvis/pull/3538)).

**Implement resume has never been reachable** ([#3536](https://github.com/cbrenner04/jarvis/pull/3536)). `buildSubspecCompletionInventory` relativizes worktree-resolved subspecs against `projectRoot`, so every entry is dropped and both lists are always empty; `hasCompletedSubspec` gates `resumable`, making `iteration_timeout` non-resumable by construction for every implement lane. Isolated by varying only that argument against a real stranded worktree. All five lanes stranded this session had to be hand-salvaged as a direct result.

**The daemon died mid-session and could not be restarted** ([#3533](https://github.com/cbrenner04/jarvis/pull/3533)) — `EADDRINUSE` on an unbound leftover socket, visible only in `~/.jarvis/daemon-<digest>.log`. `jarvis cleanup` classified the same socket correctly and fixed it on the first try; the judgement exists, it just is not on the startup path.

**Five distinct ways a healthy implement lane failed to publish**: unreachable resume after timeout, daemon death, terminal out-of-scope from a flaky pair, a genuinely uncovered guard, and a red `check` the agent never ran.

**Two false mutation pins, both inside ticked ACs.** `parityGuardedPaths()` collapses to `[]` under its mutant while every assertion over it is `.not.toEqual` or emptiness-tolerant; and an `@mutate` annotation on `addSuppressedInvocationForFailedStage` did not kill, because `pipelineAttributedRunIds` already covers the terminal path — the line is reachable only through the `blocked`/`budget-soft-stopped` branches. Both fixed and verified to kill.

**Runbook rot was 13 citations, not the four recorded** ([#3530](https://github.com/cbrenner04/jarvis/pull/3530)); three fixes had never shipped and were re-seeded.

**Queue hygiene.** Cleanup retired all three implement worktrees and pruned their refs; the three landed specs keep subspecs 01+ open, so none archived. Hand-published index boxes ticked in [#3540](https://github.com/cbrenner04/jarvis/pull/3540).

## Session 2026-09-06 (operator, pipeline-dogfood + parallelization)

**Single-lane `full-review` pipelines run end-to-end.** Two pipelines driven seed→intent→gate→plan→gate→implement with both gates approved after review and no harness intervention: `316bb8f2` ([[subspec-inventory-relativizes-against-the-wrong-root]]) and `9b430ca0` ([[daemon-start-reclaims-its-own-leftover-socket]]). Intent, plan, and gate stages are all healthy; the failures were confined to the implement stage. Plans landed [#3550](https://github.com/cbrenner04/jarvis/pull/3550), [#3551](https://github.com/cbrenner04/jarvis/pull/3551), intents [#3548](https://github.com/cbrenner04/jarvis/pull/3548), [#3549](https://github.com/cbrenner04/jarvis/pull/3549); standalone plan on the P0 settlement seam landed [#3552](https://github.com/cbrenner04/jarvis/pull/3552).

**The real concurrency ceiling is concurrent full-suite gate invocations, not lane count.** Four concurrent implements ran at load 6–11 with **zero** watchdog false-kills and a load alarm at 22 that never fired in an hour. Two lanes nonetheless died at **45m01s and 45m02s** — the iteration ceiling — each with real work committed and **exactly one AC unticked, always `bun run test:v2`**. The agent invokes the full suite inside its own iteration budget and nothing staggers that across lanes. `f229f8d7` is the sharp case: iteration 2 spent the entire budget with `iteration_commit skipReason: "no_file_changes"`. Seeded [[implement-gate-invocation-outlives-the-iteration-ceiling]]. Load average does not predict this failure — attribute by what each lane is doing, not by load.

**The keystone bug demonstrated itself.** `8155c8e9`, the implement lane *for* [[subspec-inventory-relativizes-against-the-wrong-root]], settled `iteration_timeout` / `resumable: false` with `completedSubspecPaths: []` and `remainingSubspecPaths: []` on a spec with one linked subspec — the empty-inventory signature of the very bug it was fixing. Honest caveat: subspec 00 was 15/16, so `hasCompletedSubspec` would be false even post-fix; the defect is proven (the subspec should have been listed as *remaining*), the resume rescue would not have applied.

**Correction to the brief's P0 framing.** [[subspec-inventory-relativizes-against-the-wrong-root]] does **not** by itself make `iteration_timeout` recoverable for these lanes. Because the test AC is both ticked last and the budget consumer, a lane killed mid-suite has zero *fully* complete subspecs, so `resumable` stays false regardless of the inventory fix. The two seeds are complementary, not redundant.

**Fan-out split, then re-merged by the plan stage.** `9b430ca0`'s intent split the socket seed into two hard-coupled lanes (`cleanup-shares-daemon-socket-classifier` listed four prerequisites that were all the head lane's decisions). Only the head lane was approved, per the serial practice — and its plan then absorbed the sibling's whole scope as subspec 01, same test name. The redundant ready-intent was deleted rather than left to be planned twice. Independent confirmation of the known gotcha: a hard-coupled fan-out wants to be one spec with chained subspecs.

**A shipped test seam that covered nothing.** The socket lane's `startIpcServer` gained `useProductionProbe = probe === probeSocketLiveness` — a reference-identity check forking production and test into two structurally different bind paths. All four new reclaim tests injected a probe, so every one ran the *test* branch; deleting the entire extended-reprobe block left them all green. Found by subagent diff review, not by any gate. Hand-fixed to a single `DetailedSocketProbe` seam and mutation-verified at both sites (deleting the extended-reprobe block, and widening reclaim to `absent`, each now kill exactly one test). Same class as the open [[generalize-production-test-seam-guard]] ready-intent.

**Two CPU spinners, different causes.** `bun test …daemon-run-control-handler-guard.test.ts` at 100% for 1h14m parented to the **daemon** (a hung gate/verifier child, not an orphan), and a `bun test` at 100% for 1h32m parented to **launchd** in a still-live lane's worktree — the [[bind-verifier-spawns-to-run-termination]] mode again, now demonstrably leaking from a lane that is still running rather than only across sessions. The daemon process itself was also observed at 99.6% CPU.

**The verifier can hang forever on its own mutant, and that defeats `run kill`.** A `guard-flip: === → !==` on the exit condition of a `while (true)` loop in `daemon-run-control-handler-guard.ts` is non-terminating: an absent symbol yields `index === -1` every pass, so the loop never breaks and `from` never advances. The verifier waited for it 2.5h at 100% CPU. The run stayed `in-progress`/`live` with no agent process, past every watchdog; both `run kill` and `run kill --force` printed `killed` and changed nothing; and the worktree was left holding the inverted guard, which reads exactly like agent-authored corruption. Seeded [[mutation-verifier-hangs-forever-on-a-non-terminating-mutant]] and [[run-kill-reports-success-without-killing]].

**Lint autofix silently repointed a structural locator at a fixture.** Biome renamed `RESUME_PATH_INVENTORY_ANCHORS` to `_`-prefixed — correctly, since it is only read by parsing the file's own source. The parser's regex then matched a fixture at line 529 (4 anchors) instead of the real inventory at line 28 (6). Neither loud guard fires, because a fixture is well-formed and non-empty. Seeded [[self-parsing-structural-tests-can-bind-to-their-own-fixtures]]; the pattern underlies the whole `*-anchors` corpus.

**Four distinct vacuous mutation passes in one session.** `kind: "pass"` with 0 accepted / 13 skipped (fixes uncommitted, candidate positions stale); 0/0 against a stale local `main`; 0/0 on a file with 348 changed lines that had yielded candidates minutes earlier; and a genuine 1 accepted / 0 skipped. `kind` alone does not distinguish a verifier that killed everything from one that evaluated nothing. Separately, the gate is structurally blind to **argument-swap** defects — the keystone's own fix (`projectRoot` → `worktreePath`) derived zero candidates, because the changed condition lines already existed.

**Notification honesty, third session running.** `run-ad-hoc-terminal` fired `terminal:completed` for entry rows whose workflows were still live, and for `28347c81` while its durable row still read `in-progress`/`not-live`. Covered by [[notification-incidents-roll-up-to-the-invocation]]; not re-seeded, but this is now its third session of evidence and it undermines the wake path the runbook designates as primary.

### Late 2026-09-07 additions

**Verified still open, against a ledger row implying otherwise.** This ledger records #2996 and #3030 as "absorbed into the settlement seed"; that seed landed as [#3569](https://github.com/cbrenner04/jarvis/pull/3569) and neither behavior changed. `reconciliationTerminalStatus` (`daemon.ts:125`) returns `killed` for **any** non-terminal status, so a `paused` resumable run is still flipped (#3030); `resumeDeferredRefusalApplies` (`pipeline-execution.ts:225`) still returns true for `interrupted`, so resume still refuses it (#2996). #2996 then reproduced live from an ordinary daemon death, widening its trigger beyond "operator-killed". None of the 22 open issues were closable.

**New seeds.** [[index-link-check-rejects-annotated-subspec-lines]] — `INDEX_LINK_PATTERN` anchors `$` at a subspec link's closing paren, so a trailing `(after 00 and 03)` makes a linked subspec read as absent; blocked one plan three times, hand-landed [#3584](https://github.com/cbrenner04/jarvis/pull/3584). [[harness-failures-must-be-falsifiable-without-source]] ([#3587](https://github.com/cbrenner04/jarvis/pull/3587)) — the generalization: three unrelated failures this session each cost 20+ minutes and **every one was diagnosed by reading `v2/src`**, which external-project operators cannot do. Scoped to one shared failure record (expectation, observed, near-miss, retryability) across every workflow and pipeline stage, not per-check fixes. The brief's P1 row was retitled and recorded as one class ([#3586](https://github.com/cbrenner04/jarvis/pull/3586)).

**Daemon death and recovery.** The daemon was killed as a suspected leaked test worker (in `ps` it is indistinguishable from a leaked `bun test`; the leaked ones are `launchd`-parented). Its abrupt death left a bound socket, so `daemon start` failed `EADDRINUSE` and both `run list` and `pipeline list` went blind — monitoring read `live=0` with five lanes mid-flight. `jarvis cleanup` did not reap this socket, unlike the prior session; removing the file directly did. Reconciliation then settled all five `resumable_kill` and all four resumed lanes published. This is #3533's shape recurring with subspec 00 of its fix already on `main`.

**Queue hygiene.** 154 terminal `jarvis` run rows dismissed (~200 → 4), scoped `--project jarvis` and verified to exclude a concurrently-live `sudoku` lane. All pipelines dismissed but the live one.

**Friction, unseeded (recurred six times).** Stale plan worktrees pinned at an older `main` refuse redispatch and need `cleanup --yes --abandon` each time; implement auto-resets this, plan does not. Seed if it recurs next session.

**`full-light-review` landed 2026-09-07** ([#3593](https://github.com/cbrenner04/jarvis/pull/3593), operator hand-landed) — the gated middle tier: intent(light) → approve → plan(light) → approve → implement(light), `ready`. Before it the registry held only `full-review` (debate on plan and implement) and `fast` (no gates), with nothing between. Operator decision the same day is that **jarvis itself stays on `full-review`**; the new tier is for projects wanting gates without debate cost. Relevant to this session's measurement: `full-review`'s debate stages, not its write steps, dominated the four-lane wave's 68-minute wall clock.

## Gaps / low-confidence

- Four runbook bullets cite seeds that do not exist (`reap-ready-gate-test-children-on-run-termination`, `mutation-checkpoint-verifier-trust`, `a-daemon-lost-run-row-deadlocks-the-daemon`, `gate-repair-fence`) — fix on next runbook pass.
- #3374/#3417 need re-repro after #3483 before their spec drives to implement.
- Statuses in the seed table for pre-2026-09-01 landings are traced from the brief's history, not re-verified by artifact this pass; the 2026-09-05 audit verified the reap set by artifact.
- **Corrected 2026-09-06:** this ledger previously recorded that the watchdog `.unref()` calls were "still absent". They were present at all three sites on `main` (`write-loop.ts:275`, `write-loop.ts:2056`, `review-role-invocation.ts:66`); only the regression pin was missing. The intent split caught the error; verify such claims against the source before trusting a status cell.

## 2026-09-08 session

**Intake #3598 seeded as a propagation defect, not a config gap.** [[ready-command-reaches-the-step-that-runs-the-gate]] — `stampWorkflowStepsWithMachineConfig`'s non-write branch adds review bounds and nothing else, and `workflow-runner.ts:1539-1540` persists `fixCommand`/`readyCommand` under the write branch only, so a review step can neither be stamped nor rehydrate one. The finalization tail runs the gate on that review row, `resolveReadyGateCommand` defaults to `bun run ready`, and the settlement blames operator config. Verified in source and against run `473417b6` before seeding. Scoped to propagation plus settlement honesty; the broader per-project override block stays with [[per-project-config-overrides-seam]].

**v1 artifacts retired from `v2/spec/` (2026-09-08).** With v1 frozen (#3597), three queue artifacts were dead on arrival and are deleted: [[v1-implement-prompt-id-migration]] (pure v1 call-site work), [[v1-and-v2-read-agent-order-from-different-config-keys]] (premise was two engines), and [[retire-claude-pool-contention-folklore]] (v1-only code that never runs). All three carried acceptance criteria requiring `bun run test:v1` / `test:integration:v1`, scripts that no longer exist — any implement run would have stranded on an un-tickable AC. Separately, nine `## Documentation updates` targets pointed into the frozen `v1/docs/` tree; those were redirected to their live `v2/docs/` equivalents or dropped, for the same reason. The prompt-artifact chain was rescoped from cross-engine to v2+shared. `v2/docs/v1-behaviors.md` is a live v2 doc and its citations were deliberately left intact.

**`pipeline recover` rewritten against what actually reproduces (2026-09-08).** The seed's prior premise — recover refuses every `full-review` plan stage with `stage_resolution_failed` because resolution reads the approval gate at position `n-1` — **no longer reproduces**: recover admitted (`kind: "admitted"`, exit 0) on lane `stamp-gate-commands-on-gate-running-steps` of pipeline `443a6cd9`, a plan stage sitting directly behind `approve-intent`. The live defect is worse and had been masked by the refusal: recover **discards the operator's hand-correction**, restores the agent's original draft, and validates that. Corrected the staged tree per the runbook (renamed the misnamed subspec, deleted a vacuous sibling, rewrote `index.md`), recovered, and the stage settled `plan_stage_invalid` naming `00-daemon.md` — a file deleted before invocation — with both original files back on disk at fresh mtimes. Ruled out git reset (`.jarvis-plan-stage/` is gitignored), a second run row (`run list` shows one), and a further iteration (none in the run log). The `contract_miss` that triggered all this was itself **correct**: the ready-intent declares one surface (CLI workflow admission) and the agent emitted a misnamed `00-daemon.md` plus a vacuous `01-cli.md`. Lane hand-landed under the circuit-breaker.

**The generalized test-seam guard detects nothing (2026-09-08).** [#3588](https://github.com/cbrenner04/jarvis/pull/3588) broadened `guard-production-test-flags.ts` beyond the `invert*ForTest` family, shipping 74 green tests and a `step-rules.ts` instruction to every implement agent — and flags **zero** seams in production. Its own spec names `write-loop.ts:347` and `workflow-runner-resume.ts:1642` as reachable on `main`; the guard exits `0` with both present. Three independent regex faults (type-alias class omits `&`, so intersection dep bags never match; `[^}]*` cannot cross a nested `}`; the generic fallback cannot cross `>`), plus no rule at all for an exported `*ForTest` helper without a `set` prefix. Every rejection fixture is a single-line zero-nesting construct, so the criteria cannot distinguish a working guard from an inert one — the standing green-gate-over-a-no-op mode. Caught by independent diff review, then verified by running the branch's own guard. Seeded [[test-seam-guard-verified-against-real-source]]: evidence must come from fixtures lifted from real `v2/src`, detection should be an AST pass, and a run-time meta-test must assert the guard flags every seam currently present. PR held unmerged; its stale pre-freeze base (still scanning `v1/src`) was rebased onto current `main` separately.

**Intake #3595 root-caused in source (2026-09-08).** [[supersede-reaches-a-socketless-resident-daemon]] — `enumerateOtherDaemonSockets` (`daemon-peer-socket.ts:6`) discovers peers by globbing `daemon-<16hex>.sock`, so a daemon that has unlinked its socket but not exited receives no `supersede`, keeps admitting, and keeps its worktree leases. Cost: run `33a03ee5` was admitted by the old daemon at the changeover instant and produced exactly one `iteration_started` and nothing else; re-dispatch refused `worktree is in use by process 98680` (`WorktreeBusyError` keys on the recorded pid being live — it was, it was just unreachable); `run kill` on the reachable daemon returned `run_not_active`. Only an out-of-band `kill` resolved it. Distinct from [[superseded-daemon-releases-run-ownership]] and #3030, both of which presuppose supersede was delivered — and note supersede alone only stops *admission*, so the seed also requires superseded daemons to exit and leases to be reclaimable.

**A shipped P0 read as open for a day (2026-09-08).** `bound-diff-derived-mutant-execution` — the verifier-hang keystone — landed in full as [#3578](https://github.com/cbrenner04/jarvis/pull/3578) on 2026-09-07: `MAX_KILLING_TEST_MS = 30_000` wired with `timeoutMs` plus detached `processGroup` at `diff-derived-mutation-verifier.ts:119,304`, `non_terminating_mutation_failed` settlement through `write-loop.ts` and `run-operator-error.ts`, and the sidecar record dir. All five subspecs' criteria and index boxes are ticked on `main`. But the spec was never archived to `completed/` and its ready-intent was never consumed, so the queue advertised it as unstarted — I selected it as the next P0 to plan and found a stranded plan worktree drafting a duplicate. Archived, ready-intent pruned, leaked `verdict-patch.md` removed from `main`, brief's P0 row corrected. **Lesson: the runbook already described this bound as shipped while the brief listed it as an open P0; the source, not either document, settled it.** Recurrence of the [[detached-pipeline-plan-stage-consumes-ready-intents]] / archival pair, now with a measured cost: one operator picking up finished work.

**Cleanup would archive a harness staging dir to the repo root (2026-09-08).** [[cleanup-archives-harness-staging-outside-the-spec-home]] — `jarvis cleanup jarvis --dry-run` reproducibly previews `archive: <repo>/.jarvis-intent-stage -> <repo>/completed/.jarvis-intent-stage` for a retired intent worktree, while every other line in the same run correctly targets `v2/spec/completed/`. Three faults compound: a `.jarvis-*` staging dir is admitted as a spec artifact despite the documented ignore contract, the destination lands outside the spec home, and the source does not exist on disk at all. Cause is post-retirement archival, not the stranded scan: an intent write step records its staging dir as `run.specPath`, `sourceForRun` (`cleanup.ts:769`) passes it through the containment guard, and `resolveArtifactSpec`'s `?? sources[0]` fallback (`:747`) accepts it unproven, making `home: dirname(source)` the repository root. Caught before applying — this is on the session-close path, where it would either create a stray root `completed/` or fail an otherwise-fine retirement.

**The mutant bound returns on time but never kills the process (2026-09-08).** [[verifier-group-sigkill-escalation-dies-with-its-parent]] — caught live: `bun test v2/src/daemon/daemon-run-control-handler-guard.test.ts`, `PPID 1`, `PGID == PID`, 100% CPU for 10+ minutes, `cwd` = `T/mutation-verifier-while-true-guard-zKqdXB`, the fixture dir `diff-derived-mutation-verifier.test.ts:1225` had already deleted. The orphan was spawned by **the regression test that proves non-terminating mutants are bounded**: it asserts elapsed time and result kind, both of which pass, while the child it started runs forever. `killGroup` (`shared/subprocess.ts:107`) sends SIGTERM then schedules SIGKILL on a `setTimeout(…, 50).unref()`; the timeout path rejects immediately after SIGTERM, so a parent that exits inside the 50 ms grace cancels the only escalation that would kill a child spinning in `while (true)`. This is the mechanism behind the brief's 2h55m pegged core — [#3578](https://github.com/cbrenner04/jarvis/pull/3578) fixed the *waiting*, not the *leak*, which is why orphans persisted after it shipped. Distinct from [[reap-verifier-process-groups]] / [[record-verifier-spawn-process-groups]], which bind *harness* spawns to an owning run row; a test-fixture spawn has no run row and is unreachable from that sweep.

**Implement publishes its own review verdict (2026-09-08).** [[implement-publishes-its-review-verdict-into-the-spec-tree]] — `excludeVerdictFromStaging` (`review-intent-enforcement.ts:290`) states it is called before every landing "so no landing kind ever publishes it as durable output", but implement builds `verdictPath` as `dirname(launchSpecPath)/verdict-patch.md` (`implement-workflow-steps.ts:675`) — inside the spec tree — and lands via the ordinary `git add -A` completion commit, which never calls that helper (its only caller is the resume path). `verdict-*.md` is not gitignored either. Recurrence, not a one-off: [#3578](https://github.com/cbrenner04/jarvis/pull/3578) committed one to `main` (removed by hand in [#3609](https://github.com/cbrenner04/jarvis/pull/3609)), and the `stamp-gate-commands` implement published another, stripped by hand before merge. `lint:md` cannot catch it — the shared ignores exclude `**/verdict-*.md` — so the gate stays green. The runbook's "strip any `verdict-*.md` sidecar" hand-publish instruction is the manual workaround this replaces.

**Plan lane emitted near-duplicate subspecs twice in one session (2026-09-08).** [[boundary-split-emits-near-duplicate-subspecs]] recurred on `persist-review-step-gate-commands`: the draft shipped `00-persistence` / `01-execution-loop` with **identical problem statements** and `02-persistence` / `03-execution-loop` likewise, where `01` carried **zero** acceptance criteria and `03` carried only a stays-green check. It blocked `contract_miss` on an orphaned `04-…` the index never linked — the gate was **correct**, as it was on the `stamp-gate-commands` lane earlier (a misnamed `00-daemon.md` plus a vacuous `01-cli.md` against a single declared surface). Both lanes were hand-landed: collapsed to the real behaviors (3 and 1 subspec respectively) with the orphan linked and duplicate module-boundary names replaced by behavior names. Note the circuit-breaker's premise does **not** apply here — the gate rejected genuinely bad drafts both times, so the tax is plan-agent output quality, not a gate that rejects well-formed work.

**The keystone P0 landed a serialization guarantee that does not hold (2026-09-08).** [[gate-invocation-slot-has-no-owner]] — `implement-gate-invocation-iteration-budget` shipped 16/16 criteria with every gate green, and its slot is an unowned module-global boolean (`write-loop.ts:512`). `releaseIterationGateSlot` (`:2166`) clears it unconditionally whenever the settling lane has no active gate, and `settleBoundedIteration` calls that on both exit paths at the end of **every** iteration of **every** write loop — so any lane's ordinary settle frees another lane's held slot and two full suites run concurrently, the exact condition the spec exists to prevent. `MAX_CONCURRENT_AGENT_GATE_INVOCATIONS` is exported but never read, so the cap is decorative. Caught by independent diff review, then verified in source; **held unmerged**. Also flagged and not yet confirmed: claude's tool-completion frame is matched as a top-level `tool_result`, but `claude -p --output-format stream-json` nests it under `{"type":"user","message":{"content":[…]}}`, which would hold the slot for the whole iteration with the primary agent — every fixture uses the invented shape, so the tests cannot catch it.

**Mutation verification is impossible for any file whose killing test exceeds 30s (2026-09-08).** [[killing-test-budget-is-shorter-than-the-test-it-runs]] — run `34b26330` settled `non_terminating_mutation_failed` on a pure boolean helper with no loop (`workflow-runner-resume.ts:426`). Measured on an idle machine: the co-located `workflow-runner-resume.test.ts` takes **32.06s unmutated** (61/0) against a **30s** `MAX_KILLING_TEST_MS`, and with the reported mutation applied it fails in 30.67s (2 failures, including the exact behaviour the mutation breaks). The mutant is killed; the verifier kills its own killing test first and reports non-termination. Deterministic, so `resume` is a fixed point — the lane was hand-finished ([#3618](https://github.com/cbrenner04/jarvis/pull/3618)). The budget is a fixed constant against a growing corpus, so every suite approaching 30s becomes silently unverifiable.

**Third near-duplicate plan split in one session (2026-09-08).** [[boundary-split-emits-near-duplicate-subspecs]] recurred a third time on `run-review-finalization-with-resolved-gate-commands`: `01-persistence` and `02-execution-loop` carried **byte-identical problem statements** with `02` at zero acceptance criteria, the index linked only 2 of 6 files, and five subspec titles disagreed with their filenames (`# 02 - …` in `03-*.md`, and so on). The run settled `invocation_failure` / `invalid_token` after a token reprompt, so the contract gate never got to reject it. All three of this session's plan lanes were hand-collapsed to their real behaviours (1, 3 and 5 subspecs). The pattern is consistent and mechanical — module-boundary names substituted for behaviour names, then emitted twice — and it now costs a hand-landing on every plan lane rather than occasionally.

**The killing-test budget defect is wider than first seeded (2026-09-08).** Second reproduction on run `25af3e6e` (`run-review-finalization-with-resolved-gate-commands`), at `workflow-runner.ts:1669` — a conditional object spread. `workflow-runner.ts`'s nearest co-located test runs in **7.28s**, well inside budget, but it has **ten** sibling `workflow-runner-*.test.ts` files and the resolved killing set includes the 32.06s `workflow-runner-resume.test.ts`. So the exposure is not "a file whose own test is slow" — **any file that resolves a slow sibling into its killing set is unverifiable**, however fast its own test is, which on this corpus is every file sharing the `workflow-runner-` stem. [[killing-test-budget-is-shorter-than-the-test-it-runs]] amended accordingly, including an acceptance criterion that the bound derive from the whole resolved set rather than one file. Both occurrences were hand-finished; `resume` is a fixed point in each.

**Recurring flake worth watching.** `StateStore WAL concurrency > second writer on separate connection commits without database is locked` failed twice today under loaded full-suite runs (10s lock-report timeout), on two different branches, neither touching `v2/src/persistence/`, and passed 167/0 in isolation both times. Same family as [[coscheduled-test-pair-strands-runs-terminally]] but a distinct mechanism (a lock-handshake deadline, not a 30s per-file timeout). Not separately seeded; re-seed if it strands a run rather than just costing a re-run.

**Lane 4 of #3598 is blocked on an undiagnosable dispatch failure (2026-09-08).** [[invocation-error-reports-the-prompt-and-records-nothing]] — two consecutive `plan` dispatches of `distinguish-configured-and-default-missing-gates` (`056da7b7`, `d6e7a552`) settled `invocation_error` / non-resumable at ~8s, with `error.message` containing the **plan draft prompt itself** (ending mid-sentence in `## File output`) and **zero telemetry rows**. Ruled out agent health (all three CLIs exit 0 immediately after), quota (quota exits produced 53 rows earlier the same session; these produced none), input size (2.3 KB, smaller than several that planned fine), and stale workspace (`cleanup --abandon` between attempts). Other plan lanes dispatched fine on the same daemon, so it is not a blanket outage. The seed is scoped to the reporting path — a binding failure must leave an attributable telemetry row and must not present the prompt as the diagnosis — and deliberately does **not** claim to have found the underlying spawn defect. Lane 4 left as open work: its ready-intent is on `main` with all three prerequisites satisfied by [#3613](https://github.com/cbrenner04/jarvis/pull/3613), [#3618](https://github.com/cbrenner04/jarvis/pull/3618), [#3622](https://github.com/cbrenner04/jarvis/pull/3622) and [#3624](https://github.com/cbrenner04/jarvis/pull/3624).

**Correction: the three plan-lane hand-collapses were harness damage, not agent output (2026-09-08).** Earlier entries in this session recorded that the plan lanes' near-duplicate splits were "plan-agent output quality, not a gate that rejects well-formed work", and that the circuit-breaker's premise therefore did not apply. **That is backwards.** `normalizePlanDraftSpecDir` (`shared/module-boundary-surfaces.ts:417`, called unconditionally from `write.ts:213`) re-splits the agent's authored draft into one subspec per surface, copying the parent body and replacing only three bullet sections — so identical `## Problem` text, bare surface titles (`# Persistence`, `# CLI`), zero-criteria children and index/title drift are all produced by the harness. The agents' underlying content was sound every time, which is why collapsing to the real behaviours landed all three. The circuit-breaker premise **does** apply: a harness stage was mangling good work and the gate was correctly rejecting the result.

**The taxonomy is jarvis-only and applied to every registered project.** `MODULE_BOUNDARY_SURFACES` is a hardcoded `persistence`/`daemon`/`cli`/`execution-loop` list with no per-project override, and several patterns are ordinary software vocabulary (`/\bflags?\b/i`, `/\b(?:database|sqlite|storage)\b/i`, `/\bpersist…\b/i`, `/\bsocket\b/i`). Verified against the real classifier: "persists across relaunch" + "the board flags an illegal move" → `[persistence, cli]`, and three more product-shaped pairs split identically. It is **already half-firing on `chess-mvp-yolo`**, a SwiftUI iOS app, where `persistence` matches shipped specs repeatedly and one Stockfish subspec matches `daemon`; those survived only because each matched a single surface against a two-surface threshold. The multi-surface *rejection* leaks the same way — a product bullet like "the flag is persisted" blocks the draft. Seed rewritten with the full diagnosis; spec `20260908T110410Z-retire-module-boundary-surface-split` hand-authored (the plan lane is the broken component, so routing its own fix through it risks self-corruption — circuit-breaker practice).

**The WAL handshake flake graduated from nuisance to strand (2026-09-08).** [[wal-lock-handshake-deadline-strands-runs]] — `waitForLockHeld` (`state-store-wal-concurrency.test.ts:197`) gives a spawned lock-holder subprocess a fixed **10s** to boot Bun, open the database, take a write lock and print its marker. `test:v2` runs files 10-wide by design, so that deadline is measured under the contention the suite itself creates. Five failures this session across four branches, none touching `v2/src/persistence/`; in isolation the file is 4/4 three runs consecutively on the branch and 4/4 on `main`. The fifth occurrence **stranded run `6dc830ec`**: the ready gate failed on it, the base-ref probe re-ran it on the same loaded machine, it flaked there too, and finalization correctly concluded "reproduces on base" and settled `ready_gate_out_of_scope` — `retryable: false`, `nextAction: stop`, no resume path — for a run whose own work was complete and correct. So one test's private startup deadline is upgraded into a terminal failure of unrelated work. The seed removes the trigger; the class (a load flake becoming non-resumable because the base-ref probe shares the load) stays with [[coscheduled-test-pair-strands-runs-terminally]].

**A misclassified cursor exhaustion cost another project a full day (2026-09-08).** [[cursor-out-of-usage-is-not-classified-quota]] — the operator's `sudoku` session hand-landed every stage all day because each one died in seconds. Root cause: cursor reports exhaustion as `ActionRequiredError: … You're out of usage`, which matches none of the nine `cursorQuotaPatterns` (`agents.ts:1066`) — the closest wants "you've hit your usage limit". It therefore classifies generic `error`, and since the agent order **advances on quota only**, the chain stops instead of falling through. Eight `invocation_error` stages across five lanes, each with exactly two telemetry rows (codex `quota` → cursor `error`), and **zero** claude invocations despite claude being the configured third rung and working normally for another project on the same daemon. Both upstream rungs were genuinely exhausted — codex's banner matched and cascaded, cursor's did not. Since 12:00 cursor was 0 ok / 9 attempts while codex ran 13/22 on a recovering window, so stages survived only when codex happened to have quota. The ~3.0-3.4s constant cursor duration is the tell. Distinct from [[quota-classification-covers-every-step-role]] (role coverage and codex's multi-line tail) and from [[invocation-error-reports-the-prompt-and-records-nothing]] (the reporting half). **I had told the operator their session "looked fine" after checking a single live run without looking at the failure history — the data was available and I had not read it.**

## 2026-09-09 session (hand-landing)

**Forty PRs by hand, zero through the harness.** The operator opted out of dogfooding for the session ("fuck dogfooding") and had the backlog landed directly: the 2026-09-08 carry-over (#3617 gate budget, #3588 seam guard, the four subspec-01+ lanes #3635–#3640), the deadline pair, slot ownership, the notification cursor, `daemon stop` liveness, the directive sweep, the prune-by-slug fix, and four issue-driven fixes (#3669, #3670, #3672, #3673). Every implement PR ran typecheck plus the touched-surface slices locally and waited for green CI before admin-merge; one merge (#3648) went out on a stale CI read while the run had actually failed on the WAL flake — main stayed green, but the rule since is to read `statusCheckRollup[].conclusion`, never a watch exit code.

**Hand-landed specs are invisible to cleanup.** Stranded archival keys on a run row's recorded branch; a spec no run touched prints `Skipped artifact: … no durable implementation branch` forever. Seventeen specs were archived by hand across [#3641](https://github.com/cbrenner04/jarvis/pull/3641), [#3658](https://github.com/cbrenner04/jarvis/pull/3658), [#3666](https://github.com/cbrenner04/jarvis/pull/3666), [#3674](https://github.com/cbrenner04/jarvis/pull/3674). Seeded [[cleanup-archives-hand-landed-specs]]. The sibling prune defect ([[detached-pipeline-plan-stage-consumes-ready-intents]]'s slug half) shipped as [#3657](https://github.com/cbrenner04/jarvis/pull/3657), so cleanup now reaps a slug-named ready-intent when it does archive.

**Salvage beat re-run three times.** Three spec branches held finished, unpublished subspec work (`git diff origin/main...<branch>`); each was 3-way applied onto a fresh branch and merged ([#3635](https://github.com/cbrenner04/jarvis/pull/3635)–[#3638](https://github.com/cbrenner04/jarvis/pull/3638)). Check the spec branch before hand-implementing "remaining" subspecs.

**Issue triage.** #3381 closed by #3673. #3598 was already fixed by #3613 the day before (commented). #3372 defect 1, #3029 mechanism 1 and #3030 fixed and commented; #3400 pointed at [[notification-incidents-roll-up-to-the-invocation]]. Seventeen issues untouched.

**Recurring flake.** `StateStore WAL concurrency` failed in three of nine local `test:v2` runs on branches not touching persistence, 3/3 green in isolation each time — after [#3648](https://github.com/cbrenner04/jarvis/pull/3648) removed the private deadline, so that fix did not close it. Still the lock-handshake shape; not re-seeded.

**Process slips worth recording.** A broken `&&` chain in a heredoc-heavy spec-authoring command dropped `index.md` and the seed reap from [#3664](https://github.com/cbrenner04/jarvis/pull/3664) (fixed in [#3667](https://github.com/cbrenner04/jarvis/pull/3667)); running biome on touched files instead of `bun run check` let a complexity-cap error reach CI on [#3673](https://github.com/cbrenner04/jarvis/pull/3673). Both are in the runbook's existing rules; neither is new.

## 2026-09-09 session (pipeline dogfood + parallelization)

**Four `full-review` pipelines driven seed→ready.** Agent order `codex,claude` (cursor and opencode out). 108 agent invocations, $43.91. Codex hit quota 42 times and cascaded to claude (41 ok) without intervention — the fallback order did its job unattended, which is the point of setting one.

**The chained implement stage cannot complete, and the mechanism is now known.** [[chained-implement-cannot-tick-its-own-criteria]] ([#3688](https://github.com/cbrenner04/jarvis/pull/3688), amended [#3690](https://github.com/cbrenner04/jarvis/pull/3690)). A chained implement stage reads its spec from the prior stage's worktree (`specReadRoot`) while the agent writes in the implement worktree. Three lanes, three settlement shapes, one cause:

| Run | Settlement | Spec dir in implement worktree | What the agent had done |
| --- | --- | --- | --- |
| `51838502` | `missing_blocker` | absent | complete, committed |
| `88c144e4` | `gate_invocation_refused` | absent | partial, uncommitted |
| `a6720fc7` | `contract_miss` | **present** | complete, **15/15 ticked** |

Lane 3 is the proof: its plan PR had merged, so the spec was present and the agent ticked all 15 criteria — in the implement worktree, which held 15 ticked / 0 unticked, while the plan worktree the contract read held 0 ticked / 15 unticked. So the defect is the **split read/write root**, not a missing directory, and merging the plan PR first is not a workaround.

**The standalone path is unaffected — controlled comparison.** Re-dispatching one of the same specs as a standalone `implement --base origin/main` completed end-to-end unattended: 17/17 criteria, gate green, draft→ready flipped, no operator step ([#3692](https://github.com/cbrenner04/jarvis/pull/3692)). A second standalone did the same ([#3693](https://github.com/cbrenner04/jarvis/pull/3693)). Verified at dispatch that the standalone worktree contained all four spec files where the pipeline worktrees contained none. This isolates the failure to the chained stage — not to `implement`, not to the agents, and not to concurrency.

**Plan lanes cleared the contract gate 4/4 unaided.** Every prior session hand-collapsed every plan lane. All four drafts were single-behavior, correctly decomposed, with acceptance criteria naming real test titles and what they fail against; none needed correcting. The [#3628](https://github.com/cbrenner04/jarvis/pull/3628) taxonomy retirement is holding.

**Fan-out parallelism is content-bound, not harness-bound.** Approving fan-out gates simultaneously dispatched sibling plan lanes in parallel — [[fan-out-plan-resolution-is-all-or-nothing-across-lanes]] did **not** reproduce. But all four intent splits produced strict serial chains, and the two lanes approved simultaneously both settled `agent_blocked` naming their unlanded sibling. Reading `## Prerequisites` before approving spent 1 dispatch instead of 3 on the later two. The ceiling on plan-stage parallelism is what the seed decomposes into, not what the pipeline can run.

**The gate slot works.** `gate_invocation_refused` on a second concurrent implement is [#3617](https://github.com/cbrenner04/jarvis/pull/3617)/[#3647](https://github.com/cbrenner04/jarvis/pull/3647) doing its job: one lane took the owned slot, the other was refused fast and resumably instead of colliding and dying at 45m. Four concurrent intents and four concurrent plans ran clean at load 2.4-7.3 with zero watchdog false-kills.

**A merge during a live pipeline fails its implement stage at admission.** [[pipeline-implement-stage-cannot-clear-base-behind-origin]] ([#3685](https://github.com/cbrenner04/jarvis/pull/3685)) — [#3673](https://github.com/cbrenner04/jarvis/pull/3673)'s `base_behind_origin` guard names `--base origin/main` as a remedy, but a pipeline stage has no flag surface. Merging stage PRs is how a pipeline lands its own work, so the pipeline manufactures its own failure. Recovery: `git pull --ff-only` then `pipeline resume <id> <branch-key>`.

**[[pipeline-cli-discovers-daemons-like-run-list]] reproduced twice, live.** Both times immediately after merging a `v2/src` change: `pipeline list` returned `connect ENOENT` while `run list` answered normally. Its head lane landed the same session ([#3692](https://github.com/cbrenner04/jarvis/pull/3692)). The daemon-restart continuation sweep settled all three wedged `implement` stages on the first restart, as documented.

**Landed (12).** Specs [#3680](https://github.com/cbrenner04/jarvis/pull/3680), [#3683](https://github.com/cbrenner04/jarvis/pull/3683), [#3684](https://github.com/cbrenner04/jarvis/pull/3684), [#3686](https://github.com/cbrenner04/jarvis/pull/3686), [#3687](https://github.com/cbrenner04/jarvis/pull/3687); seeds [#3685](https://github.com/cbrenner04/jarvis/pull/3685), [#3688](https://github.com/cbrenner04/jarvis/pull/3688), [#3690](https://github.com/cbrenner04/jarvis/pull/3690); implementations [#3689](https://github.com/cbrenner04/jarvis/pull/3689) (verdict staging), [#3691](https://github.com/cbrenner04/jarvis/pull/3691) (process-group death), [#3692](https://github.com/cbrenner04/jarvis/pull/3692) (pipeline daemon resolution), [#3693](https://github.com/cbrenner04/jarvis/pull/3693) (pre-result binding failures).

**Every salvaged implementation was mutation-verified by hand** before its criteria were ticked: reverting the production change failed 3 tests on [#3689](https://github.com/cbrenner04/jarvis/pull/3689) and 6 on [#3691](https://github.com/cbrenner04/jarvis/pull/3691). Both autonomous implements got an independent subagent diff review; [#3692](https://github.com/cbrenner04/jarvis/pull/3692)'s surfaced a test-only `startDaemon` field in a production deps type making its `never auto-starts` test near-tautological — a smell, recorded on the PR, not the forbidden injection-fork.

**Sandbox false red, worth remembering.** `bun run test:v2` inside the agent sandbox fails 4 tests in `v2/src/ipc/server.test.ts` with `EPERM` on unix-socket bind; 16/16 outside it. Any hand-finish gate run from an agent session will see this and it is not the diff.

### 2026-09-09 session, second block (six sibling lanes)

After the four pipelines' head lanes landed, all six sibling ready-intents were driven to `main` — four plan lanes fanned out at once, then implements paired or run alone by gate scope.

**Standalone `implement` is reliable; the chained pipeline stage is not.** Six standalone implements ran this block. Five completed end-to-end unattended — criteria ticked, gate green, draft→ready flipped, PR published — with no operator step at all. That is the same preset that failed 3/3 as a chained pipeline stage earlier the same day, on the same specs, against the same agent order. [[chained-implement-cannot-tick-its-own-criteria]] is the whole difference.

**Plan lanes: 4/6 clean.** Both failures were the plan-contract gate rejecting well-formed prose, not bad drafts — seeded as [[one-artifact-per-bullet-rejects-well-formed-bullets]] ([#3702](https://github.com/cbrenner04/jarvis/pull/3702)), the fourth instance of the brief's "strict lexical patterns fail closed" class. [[index-link-check-rejects-annotated-subspec-lines]] also fired again, on run `3852c294`.

**A false mutation survivor reached a terminal settlement.** Run `1e1f893c` settled `surviving_mutation_failed` at `pipeline-daemon-resolution.ts:123`, spent its whole repair budget, and settled `mutation_repair_exhausted` — which is never re-admitted. Hand flip-and-test per the runbook: the reported mutation failed **1** test at the settlement commit and **3** after repair. The guard was covered the whole time. That settlement also left the PR a **draft** with its three repair commits **unpushed**, so its green CI described a tree the branch no longer had — a shape the read-`statusCheckRollup` rule does not catch, because the rollup was honestly green for the commit it described. Seeded [[mutation-verifier-reports-a-survivor-that-three-tests-kill]] ([#3705](https://github.com/cbrenner04/jarvis/pull/3705)).

**40GB of orphans, and the fix confirmed.** Mid-block the machine hit load 16 with two `bun test` processes at **20.3GB and 19.5GB RSS**, both `PPID 1`, both in `T/mutation-verifier-while-true-guard-*` — enough for the OS low-memory killer to start reaping. Killed; load fell to 4.4. Then verified empirically on an idle machine that the merged state does not leak: [#3706](https://github.com/cbrenner04/jarvis/pull/3706)'s regression test runs 113/113 in 34s leaving **zero** orphans, and the leftover fixture dirs dated Sep 8 00:32 / Sep 8 23:30 / Sep 9 15:43 / Sep 9 16:15 — the two killed plus two from prior sessions, none from the clean run. So the orphans came from intermediate iterations, and [#3691](https://github.com/cbrenner04/jarvis/pull/3691) + [#3706](https://github.com/cbrenner04/jarvis/pull/3706) close the mode that has opened three sessions at load 33.

**Diff review caught two things gates did not.** A dead tie-break in `merge-pipeline-snapshots.ts` — `candidateSocketPath < currentSocketPath` is unreachable under sorted iteration, so an acceptance criterion claimed coverage of a branch that cannot be taken; fixed in-branch and the spec corrected to claim what is true. And a latent routing bug in the verb path: it resolves a prefix to a full id, routes by it, then sends the **raw argument** to the daemon, while `hasMalformedResponse` is dropped — so a skipped socket narrows the CLI's id set and a prefix can resolve differently at the owner. Seeded [[pipeline-verb-path-discards-its-own-resolution]] ([#3711](https://github.com/cbrenner04/jarvis/pull/3711)); `approve`/`reject` are among the affected verbs.

**Landed this block (15).** Plans [#3698](https://github.com/cbrenner04/jarvis/pull/3698)-[#3701](https://github.com/cbrenner04/jarvis/pull/3701), [#3708](https://github.com/cbrenner04/jarvis/pull/3708), [#3709](https://github.com/cbrenner04/jarvis/pull/3709); implementations [#3703](https://github.com/cbrenner04/jarvis/pull/3703), [#3704](https://github.com/cbrenner04/jarvis/pull/3704), [#3706](https://github.com/cbrenner04/jarvis/pull/3706), [#3707](https://github.com/cbrenner04/jarvis/pull/3707), [#3710](https://github.com/cbrenner04/jarvis/pull/3710), [#3712](https://github.com/cbrenner04/jarvis/pull/3712); seeds [#3702](https://github.com/cbrenner04/jarvis/pull/3702), [#3705](https://github.com/cbrenner04/jarvis/pull/3705), [#3711](https://github.com/cbrenner04/jarvis/pull/3711).

**Process slip.** [#3712](https://github.com/cbrenner04/jarvis/pull/3712) was admin-merged while its `statusCheckRollup` was still empty — the command printed CI status but did not gate on it. `main` verified green at `3e8138538` afterwards, but this is the same rule #3648 established.

## 2026-09-10 session (hand burn-down)

Ready-intents consumed and PRs (all admin-merged after green CI + local slices unless noted): structural-invariant-test-writing-docs [#3715](https://github.com/cbrenner04/jarvis/pull/3715); align-docs-after-write-retirement [#3716](https://github.com/cbrenner04/jarvis/pull/3716); exclude-test-support-from-production-glob [#3717](https://github.com/cbrenner04/jarvis/pull/3717); add-v2-dead-export-hygiene-gate + sweep-v2-unreferenced-exports [#3718](https://github.com/cbrenner04/jarvis/pull/3718); implement-owned-prompt-artifacts + v2-implement-prompt-wiring + implement-prompt-operator-documentation [#3719](https://github.com/cbrenner04/jarvis/pull/3719); pipeline-never-landed-probe-fails-closed [#3720](https://github.com/cbrenner04/jarvis/pull/3720); mutation-verifier-prompt-registry-surface [#3721](https://github.com/cbrenner04/jarvis/pull/3721); daemon-resume-honors-injected-config-path [#3722](https://github.com/cbrenner04/jarvis/pull/3722); merge-publication-resume-twins-compute-resumable [#3723](https://github.com/cbrenner04/jarvis/pull/3723); record-verifier-spawn-process-groups + reap-verifier-process-groups [#3724](https://github.com/cbrenner04/jarvis/pull/3724); make-run-kill-rpc-report-settlement + render-run-kill-settlement-outcomes [#3725](https://github.com/cbrenner04/jarvis/pull/3725); dedupe-cli-cruft + dedupe-daemon-cruft + dedupe-execution-loop-cruft [#3726](https://github.com/cbrenner04/jarvis/pull/3726); declarative-fragment-policy-single-assembler [#3727](https://github.com/cbrenner04/jarvis/pull/3727); cleanup-external-spec-home-lifecycle [#3728](https://github.com/cbrenner04/jarvis/pull/3728); cleanup-reclaims-and-publishes-terminal-artifacts [#3729](https://github.com/cbrenner04/jarvis/pull/3729) (**open at handoff**: merge once CI is green). Each PR prunes its consumed ready-intent, so `v2/spec/ready-intents/` on `main` lists only the four still open.

Remaining ready-intents: daemon-terminal-run-stage-settlement (in flight, WIP branch above), settle-workflows-with-falsifiable-failures, serve-canonical-failures-from-daemon, render-operator-failures-consistently. Seeds untouched this session.
