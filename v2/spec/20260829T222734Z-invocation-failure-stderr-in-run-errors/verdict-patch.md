1. Make `jarvis run list` expose the optional persisted invocation stderr, as `run wait` already does. Preserve tab-separated row integrity when messages contain tabs or newlines, add CLI regression coverage, and align durable documentation with the resulting columns and encoding. The current CLI formatter drops `error.message`, contradicting the spec and operator runbook.

2. Project `InvocationFailureDetail.message` only for the scoped binding-chain `invocation_error` case. Other failure kinds—such as quota, timeout, stall, model configuration, and landing—must retain their existing operator-error shapes. Continue omitting `message` when absent and add coverage preventing broader projection. The unconditional mapping changes unrelated operator semantics and may expose synthetic text that is not final-binding stderr.
