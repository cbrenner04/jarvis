Checking repo context for cleanup behavior and doc requirements to ground the review.
## Verdict

**Documentation:** Both intents change existing `jarvis cleanup` operator behavior. Per spec guidance, add `v2/docs/v1-behaviors.md` to **Documentation updates** (eligibility gate, no-listener continue/exit contract, multi-socket discovery)—not only `operator-runbook.md` / `daemon-host.md`.

**`cleanup-eligibility-uses-live-socket-discovery`:** Decisions say “merged like `jarvis run list`” but omit load-bearing run-list choices already shipped: query set = `discoverLiveDaemonSockets` ∪ invoking digest socket; skip per-socket failures without failing the whole command. Spell those in Decisions so plan does not re-litigate or under-spec merge semantics.

**Preservation AC:** “Normal cleanup … unchanged; existing cleanup coverage stays green” paraphrases behavior. At plan/subspec, cite pinning tests (refactor AC pattern), e.g. matching-key daemon paths in `cleanup.test.ts` / `cleanup-cli.test.ts`.

**Failing-test ACs (plan follow-through):** Intent ACs refer to “a regression test” without naming files. Subspec draft should name concrete tests (as in `run-list-aggregates-live-daemons` intent).

**Sequencing:** Prerequisite on the discovery intent correctly serializes same-seam work after no-listener continue. Plan/implement in that order.

**Otherwise:** Problem/decisions/scope split, stderr/exit/`listRuns()` abort contract, invert-guard ACs, and doc targets for runbook/daemon-host are clear. No blocker if the above are addressed at plan promotion.