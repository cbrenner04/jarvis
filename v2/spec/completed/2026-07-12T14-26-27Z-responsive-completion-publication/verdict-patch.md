**Required outcome:** `defaultWritePrBody` in `v2/src/execution/pr-body-refresh.ts` must not let an `'error'` event on `child.stdin` crash the process. Attach an error handler to `child.stdin` (the `'close'` handler already surfaces failures via exit code/stderr) so an EPIPE from `gh` closing stdin early doesn't throw unhandled. This is a genuine regression introduced by this spec's sync→async conversion and directly undermines the responsiveness/no-crash goal the spec is meant to deliver — in scope to fix before merge.